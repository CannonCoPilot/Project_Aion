## FORENSIC RECORD

### Context Refresh
- `.compressed-context-ready.md` was read to refresh the current state.
- `.scratchpad.md` was updated to reflect the latest progress and next actions.

### R2.1g Execution
- **R2.1g — the RunningHead/MainText separator** was executed and passed with **0.8760 accuracy**, **20/20 RunningHead recall**, and **17/19 MarginNote recall**.
- The mechanism pre-registered in the roadmap was falsified — the running head was not identified by a leading gap but by justified alignment and token-level typing.
- Leaf 412's mis-diagnosis was corrected — `'Temporal'` was a marginal note, not a running head.
- The region scorer was validated against a gold standard of 121 hand-labeled tokens in `head_regions_OT1-1609-B_400-419.json`.

### R2.1h Execution
- **R2.1h — the head tokenizer** was diagnosed and redesigned to address blob tokens caused by a 2-means clustering approach.
- A quantile-based splitter was tested and showed **0.2500 exact accuracy** against a pre-registered bar of **0.75**, failing its acceptance.
- The oracle threshold proved the signal was present in the print, but the estimator was insufficient.
- The quantile splitter was not adopted due to its failure to meet the pre-registered bar.

### R2.1i Execution
- **R2.1i — re-keying the region gold** was completed to address tokens by their page position rather than by ordinal.
- The re-keyed gold was validated to match the live geometry exactly, with **0 span drift**.
- The re-keyed gold was tested under perturbations and showed **0 weak bindings** and **26=26 accounting**.
- The region score improved to **0.9474** under the R2.1h quantile splitter, with **1.0000 RunningHead recall**.

### Register Defects
- A defect in the register was identified — **strikethrough is not removal**, leading to over-counting of open steps.
- A new guard `test_open_register_consistency.py` was added and injection-proven.
- A pre-existing stale claim in the verification standard (`18/44`) was corrected to reflect the current **1/59** strict count.

### Verification
- All 13 guards passed with **exit 0**.
- The meta-guard `test_verification_standard.py` passed with **exit 0** and 26 commands.
- The register now accurately reflects **59 open steps**.

### Next Actions
- **R2.1h redesign** — taking the split from the recogniser's own character positions.
- **R2.2b** — anchoring the head band to the type block rather than a fixed fraction of page height.