## RESUME HERE — R2.2d is built, measured, and not adopted

**R2.2d is complete** — the ARGUMENT region was built, pre-registered, measured, and **not adopted**. The rule is sound but the gold is incomplete, and the gold is the binding constraint.

| criterion | result |
|---|---|
| **D3** no regression | ✅ **PASS, exactly** — 0.8760 / RH 1.0000 / MN 0.8947 / MT 0.8375, identical with the rule on |
| **D4** consumer | 🔴 FAIL — argument tokens still typed MainText fall **46/89 → 2/89** |
| **D1** recall | 🔴 FAIL — 16/24 gold argument rows |
| **D2** precision | 🔴 FAIL — 2 false positives on the adjudicated leaves |

**Three findings:**

1. **I rebuilt the exact assumption `region_head` was founded on rejecting.** That module opens by establishing *"a row is NOT homogeneous and region typing must be per TOKEN"* — then I wrote a **row-level** fount test. This edition sets side-notes in italic beside roman scripture, so a row-level slant is an average: leaf 405's verse `† How beautiful are thy tabernacles ô Iacob` shares its baseline with `Manie do prophecie, and cast out diuels` and averages to 8°, firing on scripture. Moving to `region_segments` grain plus `in_block` cleared all five such cases. A row is not homogeneous in **fount** either.
2. **Gating the fount test on `body_rows` was the same error a second time** — it reached only 9 of 24 rows, because an argument's short last line (`Collection.`) fails R3's span test and never reached the test. A fount is a property of the *setting*, not of whether a line is justified. Ungating took recall 9 → 16 at the cost of 2 false positives; both numbers reported, neither bar moved.
3. **The gold is the binding constraint — and the rule proved it.** The slant census found a *fifth* chapter opening (leaf 406's `CHAP. XXV.`) that the recogniser's own whole-page census had missed. A whole-page sweep puts arguments on ~10 of 20 leaves; the gold covers 4. So D2 is measured on a subset, and the 46 further rows the rule labels ARGUMENT on unlabelled leaves are recorded as **unadjudicated** — counted neither as correct nor as errors, because either would misstate the rule.

D3 deserves a note: that criterion could only ever hurt the rule, since the 121-token gold holds no argument rows. Passing it exactly means a fifth region type was added without moving a single recorded number.

Next, in order: extend the argument gold to every chapter opening (the two censuses corroborate on 9 of 12 leaves, so enumeration is nearly complete); recover the short segments D1 misses via **contiguity** — a short segment between two italic segments belongs to the block — rather than by lowering the component floor, which would re-admit the side-note false positives; then re-run D1–D4.

Work remains uncommitted in the palimpsest tree.