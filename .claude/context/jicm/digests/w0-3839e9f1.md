## FORENSIC RECORD

### Context Refresh
A refresh is imminent, so I've updated `.claude/context/.scratchpad.md` to capture the current state of work. The scratchpad now reflects the status of the regression suite (suite h is still running), the uncommitted changes in `region_head.py`, and the progress made on the MN gap and related documentation updates.

### Suite Status
- **Suite h** is still running (pid 66423). I will not call it green until it completes and the results are verified.
- **Suite g** has completed successfully, confirming that the changes made to `collation_read.py` and `score_head_regions.py` are regression-free.

### Uncommitted Changes
- **`region_head.py`** has been updated with the new `SPAN_MODE="flush"` and `R4_DEMOTE_UNQUALIFIED` flags, both set to default off.
- **Documentation updates** have been made to the Roadmap, Master Plan, and DEVLOG to reflect the progress and findings related to the MN gap and the four refuted candidates.
- **DEVLOG** has been updated to include the completed work, including the four refuted candidates and the new R2.2o section.

### MN Gap and Related Work
- Four candidate rules were tested to address the MN gap, but all were refuted due to the same underlying issue with the `region_segments` primitive.
- The root cause was identified as the `region_segments` function cutting rows wherever a gap exceeds the line pitch, which is problematic in justified early-modern setting where word spaces are stretched to fill the measure.
- The next step is to implement R2.2o, which involves labeling every intra-row gap from the gold as word space or region gap and publishing the two distributions.

### Documentation Updates
- **OCR-ROADMAP.md** has been updated with the R2.2o section, including pre-registered bars and the four-candidate table.
- **OCR-MASTERPLAN.md** has been updated to reflect the current state of the geometric boxing model and the character recognition model.
- **OCR-DEVLOG.md** has been updated to include the completed work, including the four refuted candidates and the new R2.2o section.

### Open Issues
- **R2.2o.4** may invalidate the bar itself, as the gold's MN 0.8947 is resting on two cancelling errors.
- The **Walkthrough, Overview, and Executive Summary** documents were not fully reviewed due to time constraints, but no claims were found that my findings falsify.
- **Suite h** is still running, and I will not call it green until it completes and the results are verified.

### Next Steps
- Implement R2.2o.1, which involves labeling every intra-row gap from the gold as word space or region gap and publishing the two distributions.
- Re-audit the 19 MN entries to determine if the bar itself is valid.
- Continue monitoring the status of suite h and update the scratchpad accordingly.

### Summary
The current state of work has been saved to `.claude/context/.scratchpad.md`, and the context is ready for the imminent refresh. The MN gap remains unresolved, and the next steps involve implementing R2.2o and re-auditing the MN entries. The regression suite is still running, and the results will be verified once it completes.