# RESUME — OriginalDR re-OCR pipeline HARDENING (2026-07-22)

You are Jarvis, driving Sir's authorized OriginalDR re-OCR program. The Rung-2 recognizer is BUILT and the
pipeline has been HARDENED to run gold-free on the whole corpus. Resume immediately; do NOT re-greet. Full
autonomy; main thread. Read these FIRST, in order:
  1. Projects/palimpsest/core/.scratch/originaldr-project/ocr-spike/REOCR-HARDENING-PLAN-2026-07-21.md  (full phase status + findings)
  2. memory: project_originaldr_reocr_path.md  (the 2026-07-22b hardening UPDATE block)
Workdir: `/Users/nathanielcannon/Claude/Projects/palimpsest/core/.scratch/originaldr-project` (venv `ocr-venv/bin/python`).

## WHERE WE ARE (both of Sir's principles satisfied)
Sir's two governing principles: (1) every stage MUST work on DR pages with NO gold (13 gold pages = 0.4% of
3,028 core; goal = transcribe corpus without page-by-page human work); (2) any "single biggest functional gap"
⇒ not ready. **THE pivotal finding: the real gap was the METRIC, not the recognizer.** DR gold is VERSE-SCOPED
(page gold = target verses only) but the pipeline correctly transcribes the WHOLE page (verses+commentary) —
whole-page edit_ratio punished that and faked a "psalms broken" gap. Correct metric = **containment** (fraction
of gold verses found in the full transcription); even BASE OCR contains 96–97% of psalms verses — never broken.

**Done:** gold-free split (`reocr_core.py` production, addressed by `(ocr_dir,page_index)`; `reocr_eval.py` the
only gold consumer); gold-peeking R2.5 voter REMOVED; layout body-typing (`layout.py`, geometric header/margin/
catchword drop, genesis-24 surface 0.45→0.86); held-out retrain (`reichenau_dr_ho.mlmodel`, 3 whole pages held
out) → **R2 containment 0.9758 / ſ-faithful 0.9579 on UNSEEN pages** (genesis-24 0.993, psalms-118 0.992);
batch runner proven on 3 NON-gold pages; Rung-3 vision module (`reocr_r3.py`) built. Artifacts:
hardening report https://claude.ai/code/artifact/9280306a-ea85-4699-b359-3b33a9a20999 ·
engine comparison https://claude.ai/code/artifact/bc1fb0b1-ea11-4a14-8d3d-8eec1bd74d96.

## NEXT (the 2 open items — both partly need Sir's input)
1. **R3 auto-exec BLOCKED on creds.** `reocr_r3.py` is correct (Opus-4.8 vision, ſ-safe diplomatic prompt) but
   the `/llm/anthropic` key in credentials.yaml returns **401** (expired/rotated; no env key, no `ant` profile).
   When Sir provides a valid Anthropic key: test `ocr-venv/bin/python ocr-spike/reocr_r3.py archive-ot2-1610 227`
   (psalms-118; prior manual validation 0.9915), confirm containment ≥ R2, then run a gated batch with `run_r3=True`.
2. **Confidence gate is WEAK (top real code task).** Mean per-char confidence barely separates good vs bad pages
   (recognizer is confidently wrong on hard pages; all gold pages read conf 0.98–0.99). Design a stronger
   GOLD-FREE anomaly signal — candidates: body-length/line-count ratio, low-conf-line fraction, cross-SOURCE
   disagreement (we have S1/S3/S4/S6/S8/S9 OCR of the same DR content). CALIBRATE the threshold against
   containment on the 13 gold pages OFFLINE (gold for calibration only; runtime gate stays gold-free).
Lower-priority: `2john` marginalia over-drop (layout tune, don't over-drop body); `2esdras-07` gold spans
multiple scan pages (mislabelled one page → containment 0.10, a gold-data artifact to fix, not a pipeline bug).

## KEY FACTS / COMMANDS
- Models `ocr-spike/models/`: `reichenau_lat.mlmodel` (ſ-faithful base — USE; reject catmus ſ→s),
  `reichenau_dr.mlmodel` (full R2, val 0.9396), `reichenau_dr_ho.mlmodel` (held-out R2, val 0.923),
  `calamari-dr/dr_best.ckpt` (Calamari from-scratch 0.839 — LOST to Kraken).
- Held-out eval: `ocr-venv/bin/python ocr-spike/reocr_eval.py --r2-model ocr-spike/models/reichenau_dr_ho.mlmodel`
  (reads `.rung2-data/_manifest-ho.json` → labels genesis-24/psalms-118/2john HELD-OUT). Output shows `contain`
  (primary) + `s_cont` (ſ-faithful) + `wholeC` (deflated, secondary).
- Corpus batch (gold-free): `ocr-venv/bin/python ocr-spike/reocr_core.py batch <ocr_dir> <lo>-<hi>` → writes
  `.reocr-out/<ocr_dir>/page_NNNN.json` + `_qa_summary.json`; flags low-conf pages for R3.
- Base OCR page-indexed at `sources/our-ocr-diplomatic/<ocr_dir>/*_NNNN.json`; scans via `jp2_page.load`.
- Kraken 7.0.2 `ketos train` is BROKEN (im_transforms=None raw-bbox → swallowed AttributeError); use the
  Python-API trainer `rung2_finetune_kraken.py` (`--train-arrow/--val-arrow/--model-name`). See
  [[reference_kraken_calamari_finetune_gotchas]]. Calamari lives in separate `calamari-venv`, needs
  TF_USE_LEGACY_KERAS=1 + PROTOCOL_BUFFERS_PYTHON_IMPLEMENTATION=python on every call.

## NON-NEGOTIABLES
Production stays gold-free (gold only in reocr_eval). Score at **containment** (gold is verse-scoped), never
whole-page edit_ratio for psalms. ſ surface-safe: NFC not NFKC, no dictionary/LM, ſ-count on every output.
Consensus OUT of the ladder (improve each source singly). No Silent Degradation (below-bar stays OPEN/flagged,
never silently accepted — the batch records `r3_error` and keeps the page flagged rather than accepting it).
