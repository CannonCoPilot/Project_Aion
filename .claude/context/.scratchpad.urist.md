# Scratchpad — W2:Urist

**Saved**: 2026-08-27. Updated, not rewritten.

## Nothing in flight

Working tree CLEAN. No servers, no background jobs, no VM session.
`mtg-sell-optimizer` at **`2a35a44`**, **698 tests**, coverage **83%**.
DwarfCron at `0bb68fa` (only the deliberate untracked `.claude/`, `data/logs/`).
Real `data/collection.db` verified untouched: **205 copies, 0 sales**.

## This session: seven commits, all on mtgsell

Sir's asks in order: fix the FakeHolding-style fixtures → full progress review
→ rebuild the binary → wire the sell flow into the web → fix condition pricing
→ add the keep-list Why column → CLI tests for the thirteen untested commands
→ move the quote fixtures into the repo. **All delivered.**

- `b1b2009` last anonymous `type()` stubs retired (`deck_type`, named FakeAnalysis)
- `56b9bd2` 🔴 **`--from-collection --quote` CRASHED** — quotes were collected by
  re-analysing a scan path, passing the literal string `"collection"` when
  there was no file. Fixed by splitting resolution out
  (`Pipeline.resolve_scan` / `resolve_collection`): resolve → match quotes
  against those lots → price. First CLI tests written here.
- `1923076` quoted vendor no longer reported "not scored"; web `/analyze`
  resolves once, not twice
- `bfb6723` sell flow in the web UI + **downloads now match the plan**
- `9146e7f` 🔴 **played cards were priced at the NM bid**
- `c09c84c` keep list says WHY each card stays home
- `5bc2ab9` CLI tests for the thirteen untested commands
- `2a35a44` quote fixtures moved into the repo, scrubbed of PII

## 🔴 Traps found this session — do not reintroduce

- **`_session_analysis` / downloads.** Every web download used to re-analyse
  from scratch: no quotes, no protected decks, no credit flag, no vendor cap.
  Page said "split across 2 vendors", `shipment.zip` held ONE box, in a
  perfectly well-formed zip. Sessions now persist `session.json` recording
  what the plan was built from. Never rebuild a download from the source alone.
- **Reconcile must exclude the vendor being measured.** Comparing an offer
  against an estimate that already contains it reports "offered exactly the
  estimate" every time. `_session_analysis(sid, without=key)`.
- **Buylist feeds have NO condition dimension.** One price per card/finish/
  provider, and it is the Near Mint bid. Played copies are priced ONLY from a
  vendor's published deduction (`VendorTerms.condition_policy`, empty today) or
  `--condition-policy cardkingdom=LP:0.85,MP:0.7`. Otherwise left unscored and
  said so. A test asserts the registry policy stays empty until someone cites a
  source. Published CREDIT price takes the same deduction, else `--credit`
  recommends played cards.
- **MHTML redaction must happen on the DECODED payload.** Quoted-printable
  soft line breaks fall inside tokens; matching encoded bytes reported a file
  clean while a CSRF token was still in it.
- **A scrubber that hardcodes the identifier publishes it.** First draft of
  `scrub_quote.py` named the email in its own docstring. Caught only by
  scanning `git show :file` (the INDEX), not the working tree.
- **`iter_holdings()` drops rows it cannot hydrate** — using it to count a
  collection reports 0 copies when the card index is missing. Count in SQL.
- **PyInstaller: `mtgsell.browser` pulls in Playwright, 134 MB**, which cannot
  work frozen anyway (needs its Node driver). Excluded in the spec; the binary
  is 39 MB, not 81. 14 lazily-imported modules had to be added to
  `hiddenimports` or the frozen build dies when the user clicks.
- `selllist` gave a green ✓ to a file it could not parse ("0/0 rows"); `search`
  printed "bad query" and exited 0. Both now raise.

## mtgsell state

**698 tests, 83% coverage** (was 605 / 76%). `cli.py` 30% → 64%, proxy 33% →
98%. Every one of the 22 CLI commands is now invoked by a test.
`dist/mtgsell` is 39 MB and CURRENT with `2a35a44`.

Test infrastructure added — reuse it, do not invent a fourth index fake:
- `tests/cli_env.py` — a real tiny `data/cache` (both production schemas, 7
  cards, real ScryfallIndex + PriceIndex). Buylist `as_of` is TODAY so the
  staleness guard does not make tests fail on a calendar.
- `tests/fixtures/quotes/` — scrubbed real vendor pages + `resolved-lots.json`
  (162 lots), so quote tests need no 800 MB index and no external directory.
- `tests/test_cli_sell_commands.py`, `test_cli_commands.py`,
  `test_web_sell_flow.py`, `test_condition_pricing.py`.

## Open, for whenever Sir returns

1. **The name.** Still `mtgsell` (package, CLI, binary) while it is 22 commands
   and 9 builder tabs. Sir's call; rename before public.
2. Unimplemented, reported and not asked for: price history/alerts, sale
   reporting over time (no totals, no cost basis), sealed product, EUR /
   Cardmarket, Cardsphere trade-out as a strategy.
3. `identity/scryfall.py` stuck at 52% — the bulk index BUILD path, which needs
   an 800 MB download to exercise honestly.
4. `upload.py` 51% — the rest drives a real browser.

## Method that keeps paying — do not skip

1. **RUN it against real data.** Both crashes and the condition bug were found
   that way, never by reasoning. The condition bug hid for the whole project
   because the real collection is 205/205 Near Mint — I had to re-grade a COPY
   to see it.
2. **Verify against the PACKAGED BINARY over real HTTP**, not the source tree.
3. **Mutation-test every guard.** Every fix this session was confirmed by
   reverting it and watching the named tests fail.
4. **Never touch `data/collection.db`.** Use a copy; verify 205 copies after.

## DwarfCron — DONE and SET ASIDE per Sir

Phase 4 COMPLETE: suite repaired (`2347210`), packaged binary (`0bb68fa`).
Persona `CLAUDE.md` "Current state" is up to date. Do not resume unless Sir says so.

## Facts NOT to re-derive

- Retail providers in MTGJSON: tcgplayer, cardmarket, manapool, cardkingdom.
  Buylist: **cardkingdom ONLY**. SCG comes from its own published sell list
  (`selllist.py`) — a price feed, not a quote.
- Precon corpus: 1,674 decks, ~190 Commander. Similarity must survive both
  huge set products and 10-card land sets (coverage + absolute overlap floor).
- 🔴 Scryfall `legalities` are per CARD, not per printing — gate on `set_type`.
- 🔴 SQLite: `OR` kills index use; `LIKE 'x%'` cannot use one either (use
  explicit `>=` / `<` bounds). Check `EXPLAIN QUERY PLAN`. This was a 97x win.
- 🔴 `db.ThreadLocalDB`: FastAPI runs sync endpoints on a threadpool; a shared
  conn 500s on the SECOND request. Web test indexes must be FILE-backed with
  `check_same_thread=False` (`:memory:` thread-local gives each thread an
  EMPTY db). See `tests/test_web_sell_flow.py::WebIndex`.
- Scryfall index is `all_cards` (~540k rows), SCHEMA_VERSION 3.
- 🔴 Never `cp` a binary over one that is running. Kill first.
- Frozen builds: pass the app OBJECT to uvicorn, never an import string.
- TCGplayer ToS forbids crawling. No vendor has a buylist API.
- Bash cwd resets to DwarfCron between calls; `cd` explicitly for mtgsell work.
- zsh eats an unquoted `~1`; version offsets use `^n`.
- 🔴 Backticks in a `git commit -m` shell string get EVALUATED — the command
  names vanished from one message this session. Use `-F file` for prose.
- MCP: every Archon gets every server/connector — policy. Never re-narrow.

## Memory files
`~/.claude/projects/-Users-nathanielcannon-Claude-Projects-DwarfCron/memory/`
`tmux-window-identity-check` · `urist-lane-wiring` · `mcp-policy-all-servers` ·
`df-projects-space` · `chronicler-test-suite-drift` · `mtg-domain` ·
`mtg-data-sources` · `sqlite-threadpool-trap`
