# Scratchpad — Short-Term Working Memory

**Purpose**: Transient details needed within this session or the next 1-2 sessions. Force-loaded via CLAUDE.md. Survives /clear and JICM compression.

**Rules**:
- Write here for: credentials locations, active paths, custom functions, transient gotchas, session-specific commands
- Do NOT write here for stable facts (use MEMORY.md) or work progress (use session-state.md)
- Entries should have dates; prune when stale (AC-08 maintenance + scratchpad-rotate hook)
- **Keep under 80 lines (~2K tokens) to limit force-loaded token cost.** Older entries auto-rotate to `archive/scratchpad-YYYY-MM-DD.md`.

---

## Active Notes

### 2026-05-05 ~10:10 MDT — P1.6 + P1.6.x + P1.6.y SHIPPED (chain pushed to davidmoneil/AIFred-Pro nate-dev)

P1.6 trilogy: `042247b..6305258` (3 commits, fast-forward push complete).
- `5720cdc` executor.py +376/-7: 7 → 30 observability sites
- `4322469` pipeline-watcher.py +75: retry/give_up decisions + job.retrying audit
- `6305258` executor.py +75 + pipeline-watcher.py +44: fail_fast auth circuit breaker

**P1.6.y architectural insight**: classify_error regex translated bash → python (`_classify_error`), executor.py writes `state/auth-failure-timestamp` on auth-class errors, pipeline-watcher reads it at execute branch top with 30-min auto-clear half-life. Ollama bypasses (no shared creds). 9/9 _classify_error unit tests pass; 3/3 process_task scenarios (fail_fast, ollama-bypass, auto-clear) verified in pulse_dev.

**Smoke harness pattern** (reusable): importlib.util.spec_from_file_location for hyphenated module names; mock `launch_service`/`conditional_claim`/`pulse_post`/`count_active_executors`/`chain_predecessor_done` to isolate decision logic from network/process side effects.

**Live restart needed**: pipeline-watcher PID 94229 (loaded since Thursday) is running OLD code. New decisions only emit after restart. Not auto-restarting — David's dev environment may rely on the long-lived process.

### 2026-05-05 ~10:30 MDT — Codebase cleanup audit — agent report substantially fabricated

Spawned code-analyzer for Pulse + Nexus + automation cleanup audit. Agent produced 50 findings across P0/P1/P2 in a 30KB structured report — but spot-validation against current source revealed substantial hallucination:

**Confirmed FALSE claims** (>10 of 50):
- F-001 bash cost endpoint mismatch — endpoints actually match (`/costs/events` on both)
- F-005-F-007 dispatcher.sh corruption (21x healthcheck, 8x source, 6x --job branches) — actually 0/0/1
- F-008 Pulse runtime image typo — `import app` succeeds in container
- F-009 db.py only in image — pulse/app.py is at root, no separate db.py needed
- F-013 auth-failure-timestamp paths divergent — both readers use same `.claude/jobs/state/` path
- F-024 thread_id auto-injection missing — audit_log.py:65/86/98 properly injects via `get_thread_id`
- F-027 duplicate imports — single imports verified
- F-030 .backup-pre-supplant files committed — none present
- F-031 jobsdb/nexusdb empty dirs — neither directory exists
- F-038 dashboard hardcoded password — no such code
- F-040-F-042 pulse/.stage/app.py issues — file path doesn't exist (canonical is pulse/app.py)
- F-022 services dead — actually 173-346 LOC each, real implementations, launched by pipeline-watcher

**Verified-VALID findings** (small): F-015 (PULSE_URL/PULSE_BASE/PULSE_API_URL env name divergence — multiple names in use, no shared resolution), F-016 (17 hardcoded localhost:8700, mostly in `${VAR:-default}` fallback form which is acceptable), 1 stub-marker each in diagnose.py and reviewer.py (worth investigating specifically).

**Cause hypothesis**: agent likely operated on hallucinated file paths (read returned content that may have been LLM-generated rather than disk-read; line numbers and specific contents don't match current source).

**Lesson**: code-analyzer agent for too-broad scopes produces low-fidelity reports. For exhaustive cleanup, prefer per-area targeted passes (e.g. "audit pulse/ for endpoint consistency") with explicit "verify by exact-match grep before reporting" instruction. Do NOT trust agent file:line claims without spot-validation.

### 2026-05-05 ~09:20 MDT — P1.6 Executor.py port-completion SHIPPED (LOCAL; push pending)

Closed adapt-absorb-replace gap surfaced by P1.B1. executor.py: 7 → 30 observability sites.
Commit: AIFred-Pro-Dev nate-dev `5720cdc`, +376 / -7 LOC, 1 commit ahead of origin/nate-dev.

**Three new helpers** (audit trail for next-session lookup):
- `_check_daily_budget_gate(job, max)` — JSONL ledger sum, emits decisions blocked/warn (executor.sh:1681-1758 parity)
- `_parse_and_emit_persona_decisions(persona, exec_start)` — walks SDK persona report's `decisions[]` with mtime-window dedup (executor.sh:94-198 parity)
- `_emit_task_release_to_queue(task, signal, detail)` — paired audit + decision for the 4 failure-release call sites

**Architectural parity decisions**:
- retry decisions (fail_fast/retry/give_up) DEFERRED → belong in event-watcher (pipeline-v2 retry orchestration)
- persona.switched audit N/A → no `--persona` override mechanism in executor.py
- task.evaluated / task.escalated audits N/A → too persona-specific or not in executor.py path
- ollama path now emits `log_cost` with cost=0 / engine=ollama → row counts uniform across engines

**Smoke evidence** (cleaned up post-validation):
- TASK_ID=AION-00884720, model=qwen3:8b → 5 audit + 3 decision + 1 cost row in pulse_dev
- All 9 rows share single thread_id `1777994360-56720-20127`
- swallowed-errors.jsonl 0 bytes (zero failed dual-writes)
- Hook fixture (2 synthetic decisions[]) → 2/2 emitted with correct actor formatting

**Push status**: held LOCAL pending user confirmation. Fast-forward when authorized.

**P1.6.next candidate** (out of P1.6 scope): port retry decisions + job.retrying audit into the dispatcher/event-watcher that orchestrates pipeline-v2 retries. ~1-2 hr.

### 2026-05-04 ~16:30 MDT — P0 Context-Budget Optimization SHIPPED (Stages 1-4)

**Stages 1-3 (force-loaded reductions, validated post-/clear):** force-loaded 91K → 12K tok; MEMORY 3.2K → 2.1K; JICM payload 3.4K → 1.3K. Total controllable saved 82K (-84%).

**Stage 4 (cull to `_disabled/`)**:
- 21 skills moved to `.claude/skills/_disabled/` (kept ulfhedthnar — `Skill('ulfhedthnar')` referenced by unleash.md)
- 6 agents moved to `.claude/agents/_disabled/` (3 cull-list names didn't exist as files: `compression-agent`, `context-compressor`, `jicm-agent`)
- 4 MCPs disabled in `.mcp.json` (local-rag, qdrant-mcp, postgres-mcp, neo4j); backup at `.mcp.json.disabled-2026-05-04` (gitignored — contains creds)
- Absorber routers updated (doc-ops, mcp-ops, autonom-ops, self-ops `Read skills/<name>/SKILL.md` → `_disabled/<name>/`)
- capability-map.yaml v5: removed disabled entries, dropped `compose.investigate-then-fix` (depended on disabled agents)
- `.gitignore` extended: `.mcp.json.*` to cover backups

**MCP caveat**: `.mcp.json` changes take effect on **next session restart**. Current session still has 7 MCPs loaded. Verify on next /clear cycle.

### Follow-up (LOW priority, deferred from P0)

`.claude/skills/_index.md` and `.claude/skills/CLAUDE.md` still list disabled skills. They are on-demand (NOT force-loaded), so no current token cost — but stale refs would mislead future skill-discovery queries. Update at next maintenance pass (AC-08).

### Outstanding scheduled fires (independent of P0)
- 2026-05-06T00:09:29Z: Phase 2 CoD Stage-1 verdict
- 2026-05-09 / 2026-05-14: Phase 1.3.5 Stage-2 fixture tagging
- 2026-05-15: Phase 1.1 sample-sufficiency check
- 2026-05-16 ~17:04 MDT: Phase 1.3.5 Stage-2 formal sign-off
- 2026-05-17: JICM v7.9 Stage-2 14d window closes
- 2026-05-18T00:09:29Z: Phase 2 CoD Stage-2 formal verdict

### Live processes
- Watcher PID 5322 (with C1 fix) in W1
- HUD PID 32998 (with refresh_tokens_from_jsonl) in W7

### Next workstream
P1 reframed (2026-05-04 evening): nexus-sync supplant onto nate-dev — adapt-absorb-replace, not just merge.

### 2026-05-04 evening — Nexus-sync supplant: COMPLETE (R4-R7 shipped)

**Final result**: 25 commits on origin/nate-dev (davidmoneil/AIFred-Pro), HEAD bb2d453.
Range: pre-supplant-baseline-2026-05-04 (e8ccf64) → bb2d453. 244 files +27,604/-1,515.

**Authorship**: 21 David O'Neil (full nexus-sync-2026-04 series, chronological) + 4 CannonCoPilot:
- 181a742 jobsdb→nexusdb completion in 8 shell libs (caught by R5.1 smoke)
- 78f5b49 PROJECT_DIR tautology repair in 9 scripts (caught by R5.2 smoke)
- 1983dc0 services/observability/ python package + wired into 6 pipeline-v2 services (R5.5)
- bb2d453 pulse/migrations/0001-phase-5-1-observability-tables.sql (R6)

**Conflict resolutions** (for audit/recall):
- dispatcher-watchdog.sh (cmt 2): David's tiered escalation + our file_mtime portability
- dashboard bundle (cmt 4): -Xours --allow-empty; +43 lines .env.template
- comprehensive sync (cmt 5): trigger-ops took theirs, task-executor.md took theirs, event-watcher.sh took theirs (older Pulse-polling preserved in baseline tag)
- executor.sh (cmt 8): David's stream-json + our PULSE_URL/PORT env prefix on both timeout-claude branches
- mirror commit (cmt 16): our PROJECT_DIR fix + David's NEXUS_THREAD_ID generation in 3 files

**Smoke green**: 0 shell syntax errors; 0 python compile errors; dispatcher/event-watcher/pipeline-watchdog all parse; pulse_dev has Phase 5.1 schema (audit_log/cost_events/decision_events); python observability writes to JSONL spool + fail-quiets to swallowed-errors.jsonl when Pulse 404s.

**Deferred** (P1.5+):
- pulse/app.py POST endpoints for /audit/events, /audit/decisions, /costs/events (~30 LOC each, needs pulse-dev restart)
- audit-ingest.py env adaptation (sidecar container for cron, or wrapper script)
- aifred-dev-postgres host port mapping for direct python access from host
- dispatcher --list cosmetic header still says "Headless Claude Job Registry"

**Artifacts**:
- Debrief: Shared_Projects/Debriefs/AIFred-Pro/2026-05-04-nexus-sync-supplant-completion.md
- Reports: Jarvis/projects/project-aion/reports/nexus-sync-supplant-r{1,2,6}-*.md

**Working branch nate-dev-supplant-2026-05-04** is local-only; can be pruned next session if desired (the tag pre-supplant-baseline-2026-05-04 remains as rollback insurance).

### 2026-05-04 ~22:50 MDT — P1 (A1+B1) Dashboard SHIPPED + adapt-absorb-replace gap surfaced

**A1**: /personas page wired up. Two bugs found:
1. Stale dev-dashboard container (4 days old) — recreated to pick up post-supplant compose mounts
2. Homemade YAML parser inline-comment bug (`model: sonnet  # comment` returned literal string)
   → 6-line fix in dashboard/server/services/personas.ts:parseValue() (commit f052778)

**B1-rich** (commit 042247b, +1206 LOC, pushed to nate-dev):
- First direct PostgreSQL access in dashboard (services/pulse-events.ts, pg.Pool)
- 5 endpoints: /api/decisions, /stats, /threads, /thread/:id, /storyline/:id
- DecisionsPage (~440 LOC) with stat cards, filters, recent threads/decisions tables
- StorylineDrawer: joins audit + cost + decision events on thread_id, sorted by ts
  Color-coded kind badges, JSON drilldown for alternatives/signals/downstream

**Adapt-absorb-replace finding (THE valuable analytical deliverable)**:
- executor.sh: 33 observability sites across 2502 LOC
- executor.py: 8 sites across 597 LOC. Single decision_type 'persona_selection' (novel)
- Diff: 25 sites missing in python port. ALL 10 inline operational decisions absent
  (budget_gate, task_claim, retry, task_release). _parse_and_emit_persona_decisions
  hook from 93f5320 NOT ported.
- Verdict: ~30% parity. New P1.6 workstream queued.
- Real-flow evidence:
  - executor.sh --job ollama-test → 5 rows ($0.00 via qwen3:8b local Ollama)
  - executor.py with TASK_ID=NONEXISTENT → 0 rows (fails at pulse_get before any audit)

**Tag-clobber convention** (carrying forward from P1.5): both pulse and dashboard
images share tags between prod and dev. Currently `aifred-pulse:latest` reflects
prod source (rebuilt for prod start) and `aifred-pro-nexus-dashboard:latest` is the
P1.B1 build. Recreating either side requires a fresh build from that side's source.

**Open follow-ups**:
- YAML parser fragility (replace with js-yaml, already a dep) — low priority
- Frontend visual QA pending Nate's eyes on http://localhost:8701/decisions

### 2026-05-04 ~21:50 MDT — P1.5 Pulse observability endpoints SHIPPED (post-/clear continuation)

Closes nexus-sync supplant arc: R5.5 (writers) → R6 (schema) → P1.5 (endpoints) — fully live.

**pulse/app.py** (+122 LOC; commit AIFred-Pro-Dev `090f6ec` on nate-dev):
- `parse_iso_ts()` helper — MANDATORY because asyncpg type-checks at the binder before SQL evaluates; `$1::timestamptz` cast does NOT help (one rebuild burned discovering this)
- POST `/api/v1/audit/events` (6-col ON CONFLICT)
- POST `/api/v1/audit/decisions` (5-col ON CONFLICT)
- POST `/api/v1/costs/events` (6-col ON CONFLICT, NULLs-distinct preserves replay rows)

**Validation evidence (3-way)**:
1. swallowed-errors replay → HTTP 200 + row in pulse_dev.audit_log + idempotency confirmed (re-POST → ON CONFLICT skipped)
2. Synthetic decision + cost POSTs → HTTP 200, NUMERIC/JSONB/BOOLEAN cast cleanly
3. Python writer end-to-end: `log_audit()` → DB row + main spool + swallowed-errors.jsonl 0 bytes

Cross-table thread_id correlation verified.

**Schema-of-record gotcha**: audit-ingest.py uses 4/5/4-col ON CONFLICT (from David's `AIProjects-nmgj` repo per its own comment about "alembic 013"). Our R6 migration uses 6/6/5. Endpoints follow OUR migration.

**Pre-state preserved**: pre-P1.5 swallowed-errors.jsonl at `.claude/logs/swallowed-errors-pre-p1.5.jsonl`.

**Session-end state**:
- AIFred-Pro-Dev: nate-dev @ 090f6ec (LOCAL — push pending confirmation)
- Jarvis: Project_Aion (state + report pending commit)
- Tables in pulse_dev: clean (validation rows DELETEd post-test)
- Next session: P1 (A1+B1 dashboard work) is the natural next workstream
