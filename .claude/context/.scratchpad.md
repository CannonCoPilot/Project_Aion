# Jarvis Scratchpad (L1 session) — RESUME DOC
Updated 2026-08-27. Prior prune `.scratchpad.md.pre-prune-20260827`. Earlier 08-26/25/23/22/21.
Durable elsewhere (do NOT re-summarise): marginalia-source hunt, the four corpora, R14.0/.1/.2/.7/.8/.9,
R9.7, R11.2e, R15, R16 → `OCR-DEVLOG.md` (08-26 ×2 + 08-27) + `OCR-ROADMAP.md` · bar rules → **L2**
`reference_instrument_independence_and_bars` · bounded search → `audit_label_sources.py` docstring.
Repo `Projects/palimpsest` main; OCR `projects/originaldr/ocr-spike/`, venv `../ocr-venv/bin/python`.
## ⏸️ 2026-08-28 — **58 cmds, SUITE NOT RE-RUN** (last green 56 cmds @08-27i). NOTHING COMMITTED since `09e45ac`.
📌 OPEN **98** · ceilings **24/98** · gates 12/25. Tree now also carries R14.10b + R14.11 (audit) +
R14.11-18 doc work + 4 named literals in `visual_agent`. ⚠️ Never `git add -A`.
Unpushed `db013a0`·`884e9c0`·`1c87a64`·`09e45ac` **plus a large uncommitted tree**: 6 docs +
`witnesses.py` + `test_verification_standard.py` modified; NEW `visual_agent`, `agent_see`,
`build_foreedge_gold`, `score_foreedge`, `audit_gate_register`, `test_glyph_role_bar`, 2 gold JSONs,
`.gitignore`; **+R14.10a: NEW `build_fount_record.py`, `score_argument_agent.py`,
`gold/fount_OT1-1609-B_400-419.json`; MOD `visual_agent.py`, `agent_see.py`, `score_foreedge.py`.**
⚠️ **The suite claim line for `score_argument_agent.py` had to move fences** — it exits 0, so it
belongs with the GUARDS, not the audits block (which requires non-zero). Block membership decides. ⚠️ `audit_label_sources.py` dirt predates me. 📌 (superseded — see header for live counts) OPEN 90 · ceilings 16/90 at 08-27f (R11.2f +1 denom, numerator HELD = ceiling DEBT, prose class
like its R11.2b/c neighbours) · gates **12/25**. **+R14.10b: NEW `build_reading_record.py`,
`score_pagenumber_agent.py`, `gold/pagenumber_*.json`, `gold/reading_*.json`, `gold/reading-crops/`;
MOD `visual_agent.py`, OCR-ROADMAP (row + result + 2 enrolments + ceilings claim + R11.2f).** NEW files R2.1b/R13.1: `audit_recog_holdout` `build_recog_gold` `score_recognisers` `recogniser` `test_recogniser_provenance` + `recog-gold/` (51 keyed) + `recog-selection.json`.
### 🟢 SIR'S STANDING PRIORITY (08-26, governs sequencing)
**AGENT FIRST** — every gate/step must answer *"how does this help build and test the adaptive visual
agent?"* Typeset/census (**R16.2·R16.3**) **SUPPLEMENTAL until Sir judges the agent operational.**
🚫 **HAND-FIXES BANNED** — code + a proven negative, never a doc edit.

### ✅ AGENT STATE (`witness/visual_agent.py` · `agent_see.py`)
Head band **115/121 = 0.9504** · MN 16/19 · RH 20/20 · MT 77/80 · CH 2/2 · forbidden 0 · abstention
3/121 each with a cause; bars applied to the **WORSE** of two declared addressing rules.
Fore-edge (GOLD-FOREEDGE) **40/42** (34 → 38 AR → 40 PN), **MN 18/18 EXACT**, MT 9/9, CH 6/6,
**AR 4/4**, **PN 2/2**; residual = `AN` 0/1 (R14.10c) + one `SG`→`CW` (R14.10d).
Classes: RH·MN·MT·CH·**AR**·**PN**·SG·CW·?? — ⚠️ declared ONCE in `visual_agent.CLASSES`; `score_foreedge`
IMPORTS it (its restated copy went stale instantly: scored `AR 4/4` **and** printed "NO NAME IN THE
AGENT" about the same boxes). ⚠️ frame→archetype→name is ONE `settle()` — was 2 call sites, nearly 3.
🔑 **A FRAME, NOT A THRESHOLD** — a note is not FAR from the body, it is **BESIDE** it: a fact about
**the measure**, derived per leaf from that leaf's own boxes. 🔑 **BESIDENESS GENERALISES** (R14.8,
pre-registered + confirmed): fore-edge notes **CLEAR** the measure, head-band notes **STRADDLE** it ⇒
**every MN figure ever quoted here (Surya 0/19, `region_head` 17/19, agent 16/19) was measured on the
cue's WORST case.** 🔑 **A CLASS WITH NO NAME IS MISFILED, NOT SKIPPED**, and the error is **confident,
not silent**: both ends of the leaf gave signature→"chapter heading" and Argument→"chapter heading".
⚠️ **ONE `_cue()` ONLY** — the archetype classifier used to reimplement a subset of the namer's cues and
made the agent contradict itself (402/413/415). ⚠️ **NO CUE MAY TURN ON A LAST PIXEL** (`y1<=head_y`
failed by 0.0015 of a page, cost 11/20 RH) — judge the box's **MASS**. ⚠️ **Head band holds TWO things**:
RH is CENTRED on the measure, a head-band note is pushed to a side. ⚠️ **Foot band has the head band's
NESTING problem** (the body box *contains* the signature, so position never fires) — Surya's own
`PageFooter`/`PageHeader` is the second cue, position-clamped. ⚠️ **BESIDENESS OUTRANKS SIZE**.
### ✅ **R14.10a CLOSED 08-27b** — `AR` ADOPTED. A1 6/6 (disjoint six) · A2 0 FP · A3 GOLD-HEADBAND
EXACT (115/121, MN 16/19, RH 20/20, MT 77/80, CH 2/2) · A4 withheld-record negative 0 AR/20 causes ·
A5 pre-registered out-of-sample **HELD 4/4, GOLD-FOREEDGE 34/42 → 38/42**. New: `build_fount_record.py`
(perception: row-segment slants + page skew, `gold/fount_*.json`, ~85s to rebuild — ⚠️ REBUILD IF THE
LEAF SET CHANGES) · `score_argument_agent.py`. Suite **48 cmds, exit 0**.
🔑 **THE DEFECT WAS 10/10, NOT 4** — `SMALL_AREA` ALONE picked the wrong name: ≥0.05 → `MT` (400 403 404
407 411 417, **SILENT**, since MainText is containment), <0.05 → `CH` (406 412 414 416, the FE ×4).
🔑 **`AR` IS A FOUNT CUE, NEVER POSITIONAL** — "below a chapter heading" was STRUCK as circular: on 4 of
10 leaves the argument box **IS** the `CH` call. Archetype now reads `AR` as *evidence a chapter opens*.
⚠️ **BESIDENESS STILL OUTRANKS THE FOUNT** — this edition sets SIDE-NOTES in italic too; italic *on the
measure* is the cue. ⚠️ Guards (0.50 majority · 2 segs · 0.60 span) sit in an EMPTY band — 0.00 vs 1.00
on the disjoint six; scorer PRINTS the slack.
### 🔴 **R14.10c BLOCKED 08-27c — NOT adopted, 3 measured blockers. `AN` stays OPEN.**
(1) **Cues REFUTED**: type height 417-annot **0.89** vs marginalia **0.64-0.69** ⇒ flags MN not AN;
pitch **0.84** == leaf 418's *MN* 0.84. Small type = APPARATUS IN GENERAL. Populations OVERLAP ⇒ no
constant exists (R2.2o.1 shape). (2) **Anchor needs a READ** = **R13.1**: 10 `SectionHeader` boxes in
window, **9 `CHAP. N.` + 1 `ANNOTATIONS.`**, geometry can't split ⇒ **R14.10b AND R14.10c block on the
SAME thing — R13.1 now GATES THE CLASS INVENTORY.** (3) **Population = 1**, and leaf 417 is one of
GOLD-FOREEDGE's five ⇒ can't validate outside the revealing gold; no 2nd exemplar exists.
🔑 **2nd FALSE ABSENCE in `audit_label_sources.py`** — it answered from `apparatus_blocks[kind]` (0;
all 1334 are `argument`) and never read odr-com's top-level **`annotations`** field in the SAME files:
**246 blocks w/ printed heads, but 232 NT; OT = 14 chapters / 2 books (Genesis, Exodus); NUMBERS=0.**
⇒ NEW row + 4th state **`🟠 PARTIAL`** (BLOCKS like ABSENT). Bound was a DIRECTORY, now a FIELD NAME.
➡️ **REDESIGN**: (1) R13.1 first (unblocks AN+PN together) · (2) move annotation window to
**Genesis/Exodus** (only OT books the source reaches — ⚠️ REBUILD perception AND fount caches) ·
(3) pre-register the **WIDTH** cue there: 417's `ANNOTATIONS.` head spans **0.325** vs chapter heads
**0.203-0.241** — ⚠️ ONE exemplar, a HYPOTHESIS, never fit it here.
### ✅ **R2.1b CLOSED 08-27d — `dr_v3_armB` SELECTED (7 class wins of 7). R13.1 UNBLOCKED.**
`audit_recog_holdout.py` → `build_recog_gold.py` → `score_recognisers.py` · `recog-selection.json`.
🔑 **THE HEADLINE RANKING IS BACKWARDS — MEASURED, not suspected.** On ONE set held out from all five:
`dr_v3_armB` **0.9575** (7/7 wins) · `reichenau_dr` 0.8902 (1 win — *the 0.9396 every doc cites*) ·
`dr_v3_armA` **0.8597** (0 wins — *the HIGHEST headline, 0.9739, is the WORST un-vetoed*).
🚫 **ſ-VETO (0.90, applied FIRST, never a tiebreak)** killed `dr_armA` 0.6744 and **`reichenau_dr_ho`
0.8372** — the *honest-generalisation* model MODERNISES the long s. ⚠️ 0.9739/0.9396 STILL never a ranking.
📌 **Per-class or nothing**: all 5 near-perfect on MT (0.987-0.995), COLLAPSE on direction line
(SG 0.47-0.75, CW 0.53-0.80); `dr_armA` 0.5543 on AR (italic breaks modernisers).
🚫 **NEVER score against GOLD-HEADBAND's `text`** — it is the INCUMBENT OCR's output (402 RH=`NVMENE`
not NVMERI; 400 MN=`X. Og Alaine.` not `K. Og ſlaine.`) ⇒ same circularity as `scan_marginal`.
⚙️ Set: **51 keyed lines / 7 classes / leaves 400-419**, 1 witness, 1 operator, NOT blind; diplomatic
(long-ſ, page typos KEPT e.g. `to fight iu Edrai`). **12 of 63 crops EXCLUDED w/ reasons — 7 are MN**,
so MN is thinnest (5). ⚠️ **CUT PER `region_segments`, NEVER PER ROW** (a row cut put PN+RH+MN in one
`RH` crop); PAD_X 0.009 or leading sorts clip.
### 🟠 **R13.1 HALF DONE 08-27e** — mechanism + proof LANDED; **arm conversion NOT** ⇒ filed **R13.1a**.
`witness/recogniser.py` = THE one entry point. Model READ FROM `recog-selection.json`, **never named in
the module**; **RAISES, never defaults** (an arbitrary model wearing the selected model's authority is
worse than none). Stamp carries **sha256 digest**, not just path (a path can point at a retrained file).
✅ `test_recogniser_provenance.py` **5/5**: hiding selection raises · name moves · **digest** moves ·
**THE READING moves (5/12 crops)** ← the load-bearing check (a stamp can be plumbed to a recogniser
never consulted; when broken output == healthy output, validate the MECHANISM) · restore works.
🔴 **STILL `gen1_r3.py:666 old_text = sp["text"]`** = stored corpus OCR. Conversion CHANGES CAMPAIGN
ARTEFACTS ⇒ deliberate act, needs Sir. 🚫 **R13.2 untouched — 1,142 s-surface cells NOT recoverable yet.**
⚠️ **SUITE RUNS EVERY COMMAND WITHOUT ITS ARGS** (`run()` = `[PY, witness/<script>]`). Enrolling
`build_recog_gold.py --check` made the suite run it BARE → took the CUT path → **blanked all 51 keyed
.gt.txt**. Now no-arg = `--check`; cutting is `--cut` and refuses to clobber without `--force`.
**A script whose no-arg behaviour is destructive WILL be run destructively by that block.**
### 🟠 **R14.10b ADOPTED 08-27f, STEP STILL OPEN — `PN` is the FIRST CLASS DECIDED BY A READ.**
`build_reading_record.py` (perception, `--build`; no-arg = report) → `score_pagenumber_agent.py`
(`--withheld` = B4). Cue is **CUE 0b, BEFORE besideness** (a page number IS beside the measure, which
is why 15 became `MN`). Guards ROUTE (`PN_MAX_AREA` 0.0016, `PN_EDGE` 0.20); **the READ decides**.
🔑 **THE DEFECT WAS 20/20 AND ENTIRELY FREE** — 15 `MN` + 4 `MT` + 1 `??`, and **NO GOLD ENTRY BINDS
TO ANY PN BOX**, so `MN` **recall cannot fall when the agent invents notes** and `MT` is containment.
⇒ **`MN` PRECISION was never measured by any instrument here.** B5 is that figure.
🔑 **THE CENTRE overlaps** — PN 0.000-0.995 vs notes -0.055-1.072 (36 inside); so does `out_frac` and
`height`. Real, NOT a bbox artefact: PN swaps sides with verso/recto (leaf 405 PN right / MN left;
412 PN left / MN right). ⚠️ **BUT 08-27g RETRACTION — I generalised ONE statistic into "position":**
**WIDTH SEPARATES** PN 0.0442-0.0546 vs other 0.0757-0.3028, **empty band 2.0× the PN spread**
(area 1.5×, aspect too). 🔴 **COUNTERFACTUAL: geometry alone = 20/20, 0 FP; the READ = 14/20 + 5
abstain + 1 MN ⇒ THE READ DEGRADES IT HERE**, and B2's zero is NOT evidence it discriminates — it was
never asked to reject a note. 🔑 **A page number is a SHORT SQUAT OBJECT, not a position** — a
book-sets-it fact, the R14.10a `AR` shape, walked past because I'd decided the answer was a read.
➡️ **PRE-REGISTERED REDESIGN (do NOT tune after the fact): width IS the cue, read is the GUARD.**
Width ≈ **0.0165/digit** ⇒ 4-digit PN ≈ 0.059-0.073 vs note floor 0.0757 ⇒ **the band closes later in
the volume**; that is where the read earns its place. Artifact: /artifact/0e11ea2b-532d-47d9-8b0c-2363e1c1391e
✅ B1 **14/20** (floor 12) · B2 **0 FP** · B3 GOLD-HEADBAND **EXACT** (115/121, MN 16/19) · B4 **0/20**
withheld · 🔴 **B5 1/20 FAILS — NOT relaxed** (leaf 403 reads `37T`; widening to "predominantly digits"
would be a rule edited after seeing which box fails). ⇒ **step stays OPEN.**
📌 OUT-OF-SAMPLE (not built for this step): **GOLD-FOREEDGE `PN` 2/2, 38/42 → 40/42.**
🔴 **MY OWN FALSE ABSENCE, caught by that gold** — I reported "4 leaves have no PN box (detector gap)".
**FALSE. 20/20 leaves have one.** My candidate test was bounded by `mass_y<=head_y`; on 401/402/409/417
the number sits ~0.005 page BELOW the head line (which the BODY BLOCK defines) ⇒ all 4 silently `MT`.
Fix = **CUE 2b's own repair one end up**: `surya in FURNITURE` as 2nd cue, position-clamped.
⚠️ **NO CUE MAY TURN ON A LAST PIXEL** — violated again.
🚫 **THE READ IS A MEASURED LIMIT, 3 REDESIGNS REFUTED**: +pad/3× upscale → **0/16**; tighter crop
(predicted from line-height normalisation) → **worse** (6/16 vs 10/16); **bbox seg matching the model's
declared type → 0/16** (⇒ the baseline path IS right; **R2.1b's numbers stand**). EXACT peaks **4/16**.
⇒ corroborates R2.1b's own per-class collapse on the direction line (SG 0.47-0.75, CW 0.53-0.80).
**R14.10b is the FIRST CONSUMER to hit that collapse in production** = ALERT on the approach.
### 🔴 **R11.2f FILED 08-27f — 4th FALSE ABSENCE, and this one is IN THE CHECKER.**
`test_verification_standard.py`'s `GUARD_GLOB = "test_*.py"` ⇒ it **cannot notice a new `score_*.py`**.
Both new scripts were added and it reported NO gap, while its rule 3 promises the opposite.
Filed NOT fixed (changing that parser changes the instrument every other number is verified by).
⚠️ **STATUS WORDS IN PROSE ARE PARSED** — my row read "ADOPTED but NOT CLOSED" and
`test_open_register_consistency.py` matched the bare token `CLOSED`. Reworded to "STEP STILL OPEN".

### 🔴 **08-27h SIR'S REVIEW — R14.11 · R14.12 · R14.13 FILED. Suite 56 cmds exit 0. Ceilings 19/93.**
🔑 **WHY EXACTLY 20 PN CANDIDATES? 1 PER LEAF × 20 LEAVES.** The predicate selects *the* smallest
extreme head-band box per leaf; every leaf prints a page number ⇒ **THE WINDOW HAS NO NEGATIVE CASE**.
B2's "0 FP" measures an ABSENCE OF OPPORTUNITY, not discrimination. Sharpens the 08-27g retraction.
🔴 **R14.11 — `audit_fixed_measures.py`: 5 of 12 fixed numbers DECIDE.** Sweeps each constant, compares
the FULL label vector. `CENTRED_LO/HI` **0.00× slack** (the RH-vs-head-band-note band = the most
load-bearing cue in the head band, sitting ON A CLIFF) · `FOOT_CATCHWORD_REL` 0.03× · `OUTSIDE_FRAC`
0.04× · `THIN_MARGIN` 0.06× · `PN_MAX_AREA` 0.25×. GUARDs: SMALL_AREA 0.70×, AR_* 1.0-1.4×, PN_EDGE
1.45×, HEADING_LO/HI 1.48×, COLUMN_OVERLAP 1.24×.
⚠️ **`visual_agent` docstring says "nothing here is a corpus-fitted number" — MEASURED FALSE.**
Masterplan §3.0 forbids a constant to DECIDE. ⚠️ **4 literals had to be NAMED first** (COLUMN_OVERLAP,
CENTRED_LO/HI, HEADING_LO/HI, FOOT_CATCHWORD_REL) — **an unnamed literal cannot be audited**; naming
changed nothing (115/121 held). ➡️ Derivation for CENTRED: use THIS LEAF's own head-band centre
distribution, not a fixed fraction.
🔴 **R14.12 — THE LAMINATION (Sir's layers idea).** Census: **158 boxes, 99 OVERLAPPING PAIRS, EVERY
leaf** (worst 417: 12 pairs/15 boxes). Rule: z-ordered regions; a region OWNS ink no higher region
claims; **no region may contain ink inside another at same/higher z**; invariant over **INK NOT
RECTANGLES** (boxes may overlap in WHITE). ⇒ subsumes 3 existing patches: 2 rival binding rules ·
attach_fount→smallest containing box · foot-band needing surya as 2nd cue. 🔑 **ACCEPTANCE INCLUDES
THE PROOF: BIND_OVERLAP and BIND_TIGHTEST must COLLAPSE TO ONE.** Also re-opens DropCap (§3.2 item 6).
**R14.13 — full-leaf gold review**; plates BUILT (`.scratch/r14/render_full.py`), **verdicts NOT done**
⇒ cannot be closed by "plates produced". Already found: **leaf 417 annotation block boxed `MT`** =
R14.10c data loss, scores as CORRECT (containment), invisible to every instrument.
➡️ ORDER: **R14.13 → R14.11 → R14.12** (lamination over wrongly-typed regions hides the naming defect).
⚠️ **PARSER TRAP**: `audit_prereq_ceilings.own_rows` matches `|` + **BARE** step id — rows written
`| **R14.11** |` read as INHERITED. **A declaration the instrument cannot parse == one nobody made.**
📜 Artifacts: R14.10b report /artifact/0e11ea2b-532d-47d9-8b0c-2363e1c1391e ·
plate book (all 20 leaves) /artifact/15c7caaf-7acb-4df9-9b0e-5e05d44dc0b0
⏸️ **AGENT NAME PROPOSED TO SIR: `LECTOR`** — awaiting his call. (Printing-house *reader*; reads the
PAGE, where `recogniser.py` reads the LINE.)

### 🔴 **08-27i SIR'S 2nd REVIEW — R14.14-18 FILED. Suite 56 exit 0. Ceilings 24/98.**
🔑 **RETRACTED: "PN is the smallest box" is FALSE** — leaf 401 CW 0.00072 & 409 SG 0.00073 < PN.
Correct claim: smallest **IN THE HEAD BAND**. And **20 candidates = 1/leaf × 20 leaves ⇒ NO NEGATIVE
CASE in the window** (every leaf prints a PN) ⇒ B2's 0-FP measures absence of opportunity.
🔴 **R14.14 — THE AGENT HAS NO ANGLE.** Real baseline tilt **-2.39°..+2.75°**, per-leaf (400 -0.34°,
409 +1.64°, 419 +1.60°); at 1.6° the drop across a page ≈ **ONE LINE-HEIGHT**. ⇒ **horizontal head
line CUTS 41 BOXES**, foot line 7. ⚠️ `fount slant_mode` is **GLYPH slant NOT page skew**, integer-
quantised, reads 0.00 on all 20 — **using it as skew = dead metric**. Boxes are axis-aligned (Surya).
🔴 **R14.15 — DETECTION FLOOR**: verse numbers set INLINE in the body block ⇒ **not boxes at all**;
tiny MN / 1-char SG may not be emitted. A class Surya doesn't localise is **ABSENT, not misfiled**.
🔴 **R14.16 — LECTOR IS NOT A MODEL.** Verified by grep: **ZERO prompts, ZERO instructions, no LLM,
no inference** anywhere in `visual_agent/region_head/collation_read/recogniser`. Surya call is
`predictor([image])[0]` — image in, boxes out, no text. It is a hand-written ordered cascade.
**THIS IS THE BIGGEST DESIGN GAP** ⇒ R14.16 evaluates a learned classifier (position-in-rotated-frame
+ size + aspect + fount + surya class + **TEXT features**, conditioned on archetype).
**R14.17** interactive review toolkit (Sir's request). **R14.18** gold expansion: **188 leaves
verified present**, list at `.scratch/r14/gold-expansion-request.txt` (OT1 vol1 S03=`P` 60 · OT2 vol2
S03=`P` 73 · NT S09=`B` 55). ⚠️ **CURRENT GOLD IS `OT1-1609-B` (S09); Sir's OT1 leaves are `P` (S03)
— 1160 vs 1146 leaves ⇒ NO FIXED OFFSET**; mapping needs the PRINTED PAGE NUMBER ⇒ **R14.10b's `PN`
is the addressing key**. **DECISION NEEDED: single- or multi-witness gold?**
⏸️ **NAMES**: `LECTOR` (page) proposed; **need a name for the line/character reader** — awaiting Sir.

### ⏸️ **TWO DECISIONS PUT TO SIR 08-27e, BOTH UNANSWERED — do NOT proceed on either unprompted**
1. **Convert the attesting arm (R13.1a)?** It moves the campaign board. 2. **Commit this run?**
Nothing committed since `09e45ac`; tree now = R14.10a + R14.10c audit + R2.1b + R13.1-half on top of
the pre-existing uncommitted set. ⚠️ Never `git add -A`.
### 🟠 **R14.14 BUILT 08-28 — IMPLEMENTED, LABEL-NEUTRAL, AND ITS OWN PREMISE REFUTED. STAYS OPEN.**
NEW `build_skew_record.py` (perception, `--build`; no-arg=report) → `gold/skew_*.json` ·
`score_skew_frame.py` (`--withheld` = S4). MOD `visual_agent.py`: `math`, `PIVOT_X=0.5`, `SKEW`,
`Leaf.skew/skew_seen/skew_why`, `_skew_record()`, `attach_skew()`, **`ry(x,y,lf)`** = THE one place
rotation is applied, `box_mass_ry()`; `frame(boxes, lf)` now takes the leaf; **`settle()` calls
`attach_skew` BEFORE `frame`** (frame is expressed in rotated y — order is NOT interchangeable).
9 vertical tests rewired through `ry`.
✅ **S1** angle measured **20/20**, range **-0.901°…+1.636°**, median +0.131°, |θ|>0.8° on 9 leaves ⇒
**varies per leaf, a single corpus angle cannot describe it.** ✅ **S2 GOLD-HEADBAND EXACTLY
unchanged** 115/121, MN 16/19, RH 20/20, MT 77/80, CH 2/2; fore-edge 40/42 PN 2/2; PN 14/20, B5 1/20
⇒ **rotation is LABEL-NEUTRAL** (breaks nothing; NOT evidence it helps). ✅ **S4** withheld 0/20.
🔴🔴 **S3 — I FILED R14.14 ON A CAUSE THAT IS FALSE.** I claimed the horizontal head line cut 41 boxes
*because the agent has no angle*. **Tested: rotating moved it 41 → 50 (WRONG WAY)**, and
**corr(|skew|, straddles) = +0.051** over 20 leaves; nearly-flat leaves 2.50 straddles vs tilted 2.44
— indistinguishable. ⇒ **THE 41 BOXES BELONG TO R14.12 (LAMINATION), NOT TO TRIGONOMETRY.** The head
line is the extreme edge of the BODY BLOCK and furniture sits at overlapping heights, so ANY scalar
boundary is straddled at any angle. **Acceptance clause "41→0" RETIRED as unreachable, NOT
reinterpreted.** Old clause (3) was **CIRCULAR** (the estimator IS the row tilt).
🔑 **S5 = THE DEFECT THE TILT ACTUALLY CAUSES, AND NO GOLD HERE CAN SEE IT.** Surya boxes are
AXIS-ALIGNED ⇒ a box round a tilted line is taller by `width·tanθ`: **17% of a median box height on
leaf 409**, 16% on 419, 0% on 406. That is a **BOUNDARY error (Gate 9.3)** and **every gold scores
LABELS**, which is why S2 can be exactly unchanged while a real defect is present.
⚠️ `slant_mode` (fount) is **GLYPH lean, not skew** — integer-quantised, 0.00 on all 20; read as skew
it reports every page square. Kept distinct deliberately.
⏸️ **NOT VERIFIED: full suite `--run` was DECLINED by Sir mid-run.** Non-`--run` standard PASSES and
both new cmds are enrolled + parse (`build_skew_record` guard exit 0 · `score_skew_frame` audit exit
1, claim binds 115/121). **RE-RUN `test_verification_standard.py --run` (~15 min) BEFORE claiming
suite-green; header still says 56 cmds / 08-27i — it is now 58 and UNVERIFIED.**

### 🟠 **R14.11 PART 1 DONE 08-28b — `CENTRED_LO/HI` RETIRED. Deciding 5/12 → 3/12. STEP STILL OPEN.**
🔴 **MY 08-28 WORK ORDER WAS WRONG AND THE ROADMAP CAUGHT IT.** I promoted R14.12 to first because
R14.14 measured the 41 cut boxes are lamination's, not the angle's. That retires *angle-before-
lamination* ONLY. **R14.11-before-R14.12 is a SEPARATE argument** (Roadmap L4318): lamination over
regions whose TYPE turns on undeclared constants enforces ownership of **wrongly typed** regions.
Order is **R14.13 → R14.11 → R14.12**; R14.13 needs Sir at the plates ⇒ took R14.11.
🔑 **DERIVATION = `centre_offset(b, ml, mr)` = |b.cx − measure_centre| / BOX'S OWN WIDTH.** `<=0.5`
is NOT a threshold — it is exactly *"the measure's centre-line falls INSIDE the box"*, which is what
"set on the measure" means. **Zero fitted numbers ⇒ RETIRED, not re-derived.**
🚫 **THE ROADMAP'S OWN SUGGESTED DERIVATION WAS REJECTED** ("judge against this leaf's own head-band
centre distribution") — a leaf has 2-5 head-band boxes, so n≈3, and on a notes-only head band it
would centre the band ON THE NOTES. **A statistic that small is a fitted number in derivation's
clothes.**
📌 Measured, 46 head-band boxes: **RH 0.004-0.419 · CH 0.008-0.165 · MN 1.023-6.576 · PN 5.738-7.740**
⇒ empty band **0.604 = 1.44× the whole RH spread**, where `CENTRED_LO/HI` had **0.00×**.
🔴 **PREDICTION REFUTED (I pre-registered "115/121 EXACTLY unchanged"): got 117/121, MN 16→18/19.**
RH 20/20, MT 77/80, CH 2/2, forbidden 0 all held; abstention 3/121 → 1/121. The +2 is **genuine
discrimination, not a narrower band**: old rule ABSTAINED on leaf 400 (`off` **1.476**) and leaf 411
(`off` **0.877**) — boxes the centre-line plainly misses. **Leaf 411 abstained by 0.079 vs
THIN_MARGIN 0.080 = ONE THOUSANDTH** — the last-pixel failure mode, third time in this file.
🔑 **RETIRING ONE CONSTANT RELEASED A SECOND, UNTOUCHED**: `THIN_MARGIN` **0.06× DECIDING → 2.25×
GUARD**; its only remaining deciding use WAS the head-band centred boundary. Nothing said they were
coupled until the sweep re-ran. New `CENTRED_ABSTAIN=0.04` (box-width units) slack **9.12×** = widest
guard in the agent; ⚠️ **UNEXERCISED on this window ⇒ UNTESTED, and it says so.**
🔴 **STILL DECIDING 3/12: `OUTSIDE_FRAC` 0.04× · `FOOT_CATCHWORD_REL` 0.03× · `PN_MAX_AREA` 0.25×.**
Acceptance needs **0** ⇒ R14.11 OPEN. ⚠️ `PN_MAX_AREA` sits exactly ON the 0.25× definition edge.
### 🔴 **08-28b — FOUR STALE PINNED COPIES OF ONE NUMBER, ALL FOUND BY MOVING IT**
`115/121` was restated in 3 scorers + `16/19` in a 4th. Improving the agent exposed all four at once.
· `score_skew_frame.S2_EXPECT` — **FALSE FAIL**: S2 claimed to test *"does the rotation move a
label"* but tested *"does the vector equal a literal frozen 08-28"* ⇒ blamed the ROTATION for
R14.11's improvement. **FIXED to the honest instrument: label vector rotation-ON vs rotation-OFF in
ONE run** (`_labels_unrotated()` hides `VA.SKEW`). Now **0/160 moved, S2 PASS** — and it compares
**160 boxes, not the 121 gold-bound ones**, so it is also STRICTER.
· `score_foreedge.HEADBAND_MN = 16/19` — silently compared the fore-edge against a figure the head
band no longer scored. · `score_pagenumber_agent.B3_EXPECT = "115/121"` — **only ever PRINTED, never
compared ⇒ nothing could catch it.** A stale number that is only printed is worse than a tested one.
✅ **FIX = `visual_agent.headband_score()`, THE ONE PLACE IT IS COMPUTED**; both scorers import it.
✅ **`score_argument_agent.A3_BARS` LEFT AT 115/121 DELIBERATELY** — it is a `>=` FLOOR, not equality
(117≥115, 18≥16 still pass). **Ratcheting a pre-registered bar after the fact changes what it tested.**
⚠️ R14.8's besideness margin NARROWED: fore-edge MN 1.0000 vs head-band **0.9474** (was 0.8421).
⚠️ **A CRASHED `audit_fixed_measures.py` WAS REPORTED exit 0 by the background runner** (real exit 1,
AttributeError on the retired `CENTRED_LO`). Verify exit codes in the FOREGROUND.
✅ **ALL 3 STALE CLAIM BLOCKS REWRITTEN + R14.11 RESULT BLOCK ADDED.** ⚠️ The suite found EXACTLY
those 3 and nothing else — every exit-code expectation held on the first run.
✅ 🟢 **SUITE GREEN AND VERIFIED: `test_verification_standard.py` 58 cmds, ALL EXECUTED, exit 0**
("all checks passed"). ⇒ **the 08-28 "58 cmds UNVERIFIED" item is CLEARED.** Register still 98,
ceilings audit unchanged, `test_open_register_consistency` + `test_counts_vs_doc` exit 0.
⚠️ **NO-ARG `test_verification_standard.py` EXECUTES THE COMMANDS** (~10 min) — it is NOT a parse-only
check. Don't edit `witness/` or the Roadmap while it runs.
⏸️ **NOT YET DONE**: R14.11 remainder (3 deciding constants); nothing committed still.

### 🔴🔴 RESUME HERE — **5 DECISIONS PENDING FROM SIR; do NOT proceed on any unprompted**
1. **R13.1a** — convert the attesting arm to `recogniser.read_stamped`? (moves campaign artefacts)
2. **COMMIT?** Nothing since `09e45ac`; tree is now large.
3. **GOLD: single- or multi-witness?** (R14.18 blocks on this — see 08-27i block)
4. **NAME the page-reader**: `LECTOR` proposed. 5. **NAME the line-reader**: `SCRIBE` proposed.

### ➡️ WORK ORDER (revised **08-28** — supersedes the 08-27i order; R14.14 is DONE-but-OPEN)
✅ **R14.14 built.** Frame is rotated + label-neutral; see the R14.14 block above. Its remainder (S5,
box inflation) is a **BOUNDARY** defect and needs a boundary gold — no existing gold can score it.
🔴🔴 **08-28b RETRACTION — THE PROMOTION BELOW IS WRONG; ORDER IS `R14.13 → R14.11 → R14.12`.**
R14.14's measurement retires *angle-before-lamination* ONLY. **R14.11-before-R14.12 is a SEPARATE
argument** (Roadmap L4318) and it stands: lamination over regions whose TYPE turns on undeclared
constants enforces ownership of **wrongly typed** regions. ⚠️ Retiring one rationale does NOT
license the reordering it was attached to. Roadmap (pre-registered) outranks scratchpad (working
note). **R14.11 part 1 is DONE (see 08-28b block above); R14.13 needs Sir at the plates.**
~~🔴 **R14.12 (LAMINATION) NOW GOES FIRST**~~ — promoted 08-28 because R14.14 **measured that the 41 cut
boxes are R14.12's, not the angle's** (corr +0.051). Lamination owns that defect outright, and its
acceptance already carries the proof-of-concept test (BIND_OVERLAP and BIND_TIGHTEST must COLLAPSE
TO ONE). ⚠️ The old rationale for angle-before-lamination is spent: the rotation is label-neutral, so
laminating on top of it does NOT create work that must be redone.
THEN **R14.13** (walk the 20 plates WITH Sir, record a verdict per leaf — cannot close on "plates
produced") · **R14.17** (toolkit, makes R14.13 produce corrections not notes) · **R14.11** (derive the
5 deciding constants — ⚠️ may be partly MOOT if R14.16 lands, a trained model has no hand-set
thresholds) · **R14.12** (lamination) · **R14.16** (learned classifier).
Then **R14.10d** (foot band `SG`/`CW`, no gold covers either; leaf 411 prints both) · **R13.2**
(s-surface δ, pre-registered) · **R14.15** (detection floor).
✅ **RETRACTED 08-28 — S5 AND S8 ARE ALREADY FILED.** I told Sir they were the "largest planning gap";
**that was FALSE and I asserted it without checking.** They are **R14.3** (S5 relations/attachment,
Gate 9.7) and **R14.5** (S8 re-examination loop, Gate 9.8) — both in the OPEN register, both
ceiling-covered (`ok`). Filing them again would have created two sources of truth. ⚠️ Same failure
mode this session keeps documenting: a claim about absence made from an unchecked memory.
📌 The SUBSTANCE of Sir's abstention objection still stands and belongs to **R14.5**: a cause is
written on the box and **nothing downstream consumes it and Sir is never told**. R14.5 is OPEN and
unstarted, so the objection is un-actioned rather than unfiled.
⚠️ **R14.10b's REMAINDER is B5 alone** — leaf 403 `37T`. Any fix must be PRE-REGISTERED first,
because the failing box is now known.
⚠️ **R14.10c (`AN`) IS STILL BLOCKED** — R13.1 gave it the read, but its other two blockers stand
(population = 1, and the label source is 🟠 PARTIAL: OT reaches only Genesis/Exodus, NUMBERS = 0).
**R14.10d** (NEW, found by DRAWING leaf 411): foot band's `SG`/`CW` split is one `rel_f>=0.60` position
test; 411 prints signature `Z z 2` INSIDE the measure + catchword `Cades` at the fore-edge ⇒ `SG→CW`.
**NO GOLD covers either class.** Same shape as the head band before it was found to hold two things.
**R14.10c** `AN` = annotation block under an `ANNOTATIONS` head. Leaf **417 is an archetype F MIXED LEAF**
(body → ANNOTATIONS → new chapter) inside the very window everything has been scored on, and **R12 records
F as "dropped WHOLE" = active data loss**; apparatus is ~half the book. **R14.10b** `PN` BLOCKED —
position cannot separate a page number from a short head-band note, and *"is this a numeral"* needs §3.0
S2's **confirming read** ⇒ **R13.1 wiring, which does not exist**: the first class that genuinely needs
the recogniser, and a concrete argument for R13.1.
➡️ THEN **R14.3** relations (janvier notes carry their verse ⇒ S5/Gate 10e nearly free) · **R14.4/R13.1** ·
**R12.1 → R16.1** (GOLD-LAYOUT frozen — blocks BOTH models' gates).
### ⚙️ AGENT OPS
**Perception cache** `gold/perception_*.json` holds **S1 boxes ONLY**, never a label — S2–S4 re-run every
call; rebuild `visual_agent.py --cache` (slow), ⚠️ **delete if the leaf set changes**. `agent_see.py`
reads `visual_agent`'s **own** output — never reimplement, or they drift (`witness/see/` 43 MB
**gitignored**, `index.html` is the reader). **Blind-gold workflow**: `build_foreedge_gold.py --blind` →
boxes NUMBERED + UNLABELLED → adjudicate from the page → `--check` verifies each address by GEOMETRY, not
ordinal (R2.2j). ⚠️ Use `FastLayoutPredictor` (`LayoutPredictor` needs an uninstalled `llama-server`).
⚠️ **The agent must see the PAGE, never kraken's lines** — kraken merges margin into body (R14(c)).
## 📌 QUOTING CAVEATS (full result blocks in `OCR-ROADMAP.md` R14.0/.1/.8/.9, R2.2o.1)
⚠️ **MainText is CONTAINMENT, NEVER A WIN** (body block is ONE box; Gate 10b unmeasured). ⚠️ **THE
ARCHETYPE CALL HAS NO GOLD** — `A` never fires here, so FORBIDS does not bind and `forbidden=0` is
**trivially true**; R14.9 removed a *demonstrated* error, it did not demonstrate accuracy.
⚠️ GOLD-FOREEDGE = **5/20 leaves, ONE witness, ONE operator, NOT fully blind**; may NEVER be promoted to
GOLD-LAYOUT. ⚠️ Never quote R2.2o.1's "99.5%". ⚠️ `region_head`'s MN 0.8947 rests on two cancelling
errors (L169) ⇒ R2.2o.4 re-audits the 19.
## ⚙️ SUITE RULES (8m52s, 47 cmds, concurrent since R11.2e)
⚠️ **SCRIPT, not pytest** (`-m pytest` collects NOTHING = **GREEN-LOOKING NULL**). Run `-u`; **`| tail`
BUFFERS TO EMPTY** — redirect to a file. Never two at once; kill by exact PID; don't edit `witness/` code
mid-suite. `grep -c FAIL` false-positives (a HEADING has 'FAIL') — anchor `^ *FAIL|^FAILED`. Liveness →
**L2** `reference_argv_is_not_process_identity`. ⚠️ Claims parse ONLY in ``` fences, only with `->`; a
decimal pair is a TRAP (`0.8760 / 1.0000` → `8760/1`) and **prose is not checkable** (`6/12` ✓, `6 of 12`
✗). **Block membership, not claim text, decides if exit 1 is healthy. Editing the OPEN register ⇒ ALWAYS
re-run `audit_prereq_ceilings` + update its claim** — the numerator can legitimately **FALL** on a close.
⚠️ **REGISTER TRAP**: the OPEN block is parsed for step ids ⇒ naming a **CLOSED** step in its own prose,
even as attribution, **enrols it as OPEN** (bit me twice). ⚠️ **CLAIM-HEADLINE RULE**: the standard binds
a claim's **FIRST** fraction ⇒ lead with the REPRESENTATIVE one. ⚠️ **NEVER `git add -A`**.
## ⚠️ OPEN ENVIRONMENT ITEMS (recorded nowhere else)
🔴 **Pre-commit hooks NOT installed** (ruff `^core/`; `.git/hooks/`=samples): 24 unfixable errors +
429-line reformat (reverted). For Sir. · `.archive-consensus-full.pre-r9.4b/` 263 MB + `OriginalDRnotes`
untracked — deliberately NOT gitignored so the fragility stays visible; believed Sir's, **leave both**. ·
**R11.2b**: standard parses only `witness/`-prefixed cmds ⇒ 2 repo-root R11 entries uncovered. ·
⛔ **R7.5a-2 NOT STARTED** — 261 artefacts; survey producers, then regenerate (never edit, R7.5d). ·
📜 Artifacts, republish SAME path to keep the URL (source is in the SESSION scratchpad, **re-extract via
WebFetch first**): `originaldr-architecture.html` → /artifact/9b5a44c2-6efe-4a51-bd11-7ed0a0904f62 ·
`r14-6a-forensic.html` → /artifact/127029f3-e38a-4574-bcf6-6b6de7e41d12
