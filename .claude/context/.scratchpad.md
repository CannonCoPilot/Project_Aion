# Jarvis Scratchpad (L1 session) — RE-PRUNED 2026-07-27 after the M12 sprint. M5–M11 narrative compressed to
# durable findings + pinned negatives; full detail in ocr-spike/SPRINT-STATUS.md (authoritative) and
# REOCR-MASTER-PLAN-2026-07-22.md §12.5 (rev 4, tier plan). Keep <=90 lines.

## ACTIVE: OriginalDR (Douay-Rheims) re-OCR ladder — Tier C2 + Tier D remain
Product repo: /Users/nathanielcannon/Claude/Projects/palimpsest (branch main; **HOLD commit/push for Sir OK**).
OCR-spike workdir: <repo>/core/.scratch/originaldr-project  (under core/.scratch, NOT .claude).
Authoritative state: ocr-spike/SPRINT-STATUS.md · master plan §12.5 rev 4 · R3-GEOMETRY-VARIANCE-FINDINGS-2026-07-26.md.
Sir directive: complete the ENTIRE reOCR-infra program, my discretion, review at the end. Sir is doing GT-3
calls/corrections in parallel — re-read ground-truth/ before scoring; his edits may have landed.
Curated sources = S1,S3,S4,S6,S8,S9 (S12–S15 DROPPED). ALWAYS jp2 (jp2_page.py). sabates_a == Janvier.

### Two venvs (distinct!)
- Product: core/.venv/bin/python. VERIFY: cd core && .venv/bin/python -m pytest -q --junitxml=.scratch/junit.xml
  (RTK drops pytest summary thru pipes -> parse testsuite xml attrs). Baseline 1388 pass / 0 fail / 21 skip.
- OCR spike: originaldr-project/ocr-venv/bin/python (uv venv; `python -m pip`, NOT bin/pip).
  Suite: `../ocr-venv/bin/python -m pytest tests/ -q [-m "not slow"]` -> **98 pass (89 fast + 9 slow)**.
  MLX/olmOCR in the SEPARATE ../ocr-mlx-venv. Never two olmOCR jobs at once (2x15GB).
  kraken 7.0.2 + ocr-spike/models/reichenau_lat.mlmodel (ſ-FAITHFUL; catmus-print-large MODERNIZES ſ — REJECT).
  Pyright "module unresolved" on every spike module = path-inserted false positive (pytest is the real check).
  Module self-checks: `../ocr-venv/bin/python <mod>.py` for xsrc_gate verse_geom verse_seg r3_route
  verse_locate gold_grid — all PASS; run after any edit.

### CACHES — build/keep these; they make experiments instant
`.page-cache/` kraken per page · `.verse-numbers/` recovered printed numbers (olmOCR gutter reads) ·
`.regime-sweep/` Tier-B pages · `.r3-stats/` R3 run · `.ps118-render/` (49MB, pdftoppm of ot2-1610 pp215-236 —
the visual evidence for the grammar; delete only if space is needed, it re-renders in 40s).
**ot2-1610.pdf is 178MB > the 100MB Read limit — MUST rasterise with pdftoppm before viewing.**

### DURABLE FINDINGS (verified — do NOT re-derive)
- edit_ratio/fold_archaic is ſ-BLIND (folds ſ->s, u/v, i/j, ae, vv) -> ALWAYS also check ſ count vs ref.
- s_dismas ~0.91 faithful to the 1610 print (a witness, not truth). Score vs Sir-reviewed GT.
- DPI/binarization is NOT the lever: grayscale 'none' > otsu/sauvola; 300 DPI <= 150. Config sweep CLOSED.
- w/vv is per-VOLUME+per-FACE (GUIDELINES.md w-regime). OPEN: NT roman-LOWERCASE w awaits **Sir RATIFY**
  (ONE global flip reverts all roman NT). NEVV caps kept VV.
- **The DR prints THREE verse-marker conventions**: OT † (positional, and OVERLOADED — also an intra-verse
  annotation mark), NT-1582 `N.` (SELF-LABELLING), psalms `N †` inline (SELF-LABELLING). Some pages print none.
- **`layout.strip_verse_numbers` documents a FALSE premise** ("the DR body marks verses with †/‡, never
  digits"). It does use digits. Worse, kraken's polygons start at the † so the number is never even read.
- Reference sources DISAGREE on verse NUMBERING: s_dismas 2-Esdras 7 has 70 verses vs sabates_a/madueke_b 73.
  Always score against ALL covering references and keep the best (coverage_alarm does this).

### PINNED NEGATIVES — measured, do NOT re-attempt
- Wider/enveloped crops; best-of-N crop selection (M6: R3 is VARIANCE-limited, not geometry-limited).
- Containment is NOT a proxy for quality (q=0.90: containment 0.456->0.949, score WORSE p=0.018).
- Switching margin for the hybrid selector: bad switches have fit gaps 0.07-0.13, no threshold separates.
- Trailing †/‡ trim: 0 rows change (archaic_id already folds non-alphanumerics).
- **Geometric apparatus removal (6 variants): NONE beats baseline 0.9561.** The hybrid localizer SUBSUMED
  apparatus handling; the old 0.500->0.985 prototype was vs a much weaker baseline.
- **Anchors must NOT rewrite spans**: known-bad 24->45. A lone anchor moves one verse's start but not its
  neighbour's -> mutually inconsistent spans. Needs a JOINT re-solve (all verses vs all anchors), unbuilt.
- A single olmOCR run CANNOT resolve a geometry A/B — use K-variant mean + Wilcoxon (r3_variance.py).

### CURRENT NUMBERS (fair reference = gold_grid printed markers; `--aligner-grid` reproduces legacy)
- Segmentation: hybrid **0.9629 mean, 143/165 pass**, oracle == hybrid, **worsened 0**, p<0.00001.
- Gate: four alarms, **recall 1.000 @ τx=0.90 -> 24% escalation, 18 false alarms**. Alarm 5 EXISTS but is
  DEFAULT OFF (40% esc / 44 FA, zero extra catches — precision bounded by 31% anchor recovery).
- R3: content pass-rate 0 -> 63.3%; **0 of 21 accepted verses worse-vs-gold than R2** (safety holds).
- Regime sweep, all 6 sources, 33 pages: ot-dagger 20 / nt-numeral 7 / no-scripture 3 / **unmatched 3 = 9.1%**.
  **NT numeral regime is NOT Matthew-specific — it appears in S1, S4, S6.**
- OPEN ledger: 55 entries, BLOCKS the deliverable.

### KEY MODULES (spike)
segmentation `verse_locate.py`(best_spans hybrid) `verse_seg.py` `verse_geom.py` · reference `gold_grid.py`
(printed-marker gold; WITHDRAWS labels <0.35 vs janvier) · grammar `block_grammar.py`(regimes, chapter_ranges)
`layout_profile.py` `apparatus_geom.py` · numbers `verse_numbers.py` + `build_verse_numbers.py` · gate
`xsrc_gate.py`(alarm 5 = anchor_disagreement) `gate_calibrate.py`(--engine, --aligner-grid) · alarm
`coverage_alarm.py`(page-level; RECALL=fix regime vs FIDELITY=fix reading) · R3 `r3_route.py` `open_ledger.py`
`mlx_client.py` · harnesses `verse_locate_eval.py` `apparatus_eval.py` `grammar_eval.py` `regime_sweep.py`
`r3_stats.py` `r3_variance.py` · audit `qc_audit.py` `build_reocr_report.py`

### RESUME HERE — Tier C2 then Tier D (tiers A, B, C1 DONE 2026-07-27; see SPRINT-STATUS M12)
 1. **C2 — ſ-faithful in-agent arbiter**: ~21 RESCUED_CONTENT_S_OPEN debts in `.r3-stats/_open_ledger.json`.
    IN-SESSION vision (I read the crop myself), **NO paid API** (memory: feedback_no_anthropic_api_calls).
 2. **D — the deliverable**: REP-2 (R2 stream rendered) · REP-4 (gold column) · REP-5 (matter as first-class
    books; renderer V7 reads a different artifact than matter-scoring-summary.json — reconcile) · re-run
    production audit (qc_audit curated+align) · regen report Artifact · COMPLETENESS REVIEW.
 3. Small gaps found in M12: `verse_numbers.verse_opening_lines` keys on † only -> **matthew recovered 0
    numbers**; NT numeral regime needs its own opening detector. · colossians-3 **ADDR-2 page verification**
    still owed (chapter_ranges is built). · the **9.1% unmatched** sweep pages are candidates for new block
    types — inspect them (render with pdftoppm) and extend the vocabulary.
 4. OUT OF SCOPE alone: M4 recognizer retrain (REVIEW milestone) · Sir-only GT calls (NT roman-lowercase w
    RATIFY; summe-of-nt p29 re-review).

### GUARDRAILS
- No-Silent-Degradation: a below-threshold unit stays OPEN and BLOCKS; a fired safeguard ALERTS for redesign,
  never accepts. "The method can't reach it" ALWAYS means redesign the method. Applies to the MEASURING
  INSTRUMENT too — gold_grid withdraws labels it cannot justify (that is how the last recall failure was found
  to be a reference bug, not a gate bug).
- Report negative results as results; pin them in code+tests so a later session cannot silently re-"fix" them.
- **Anchor edits on a UNIQUE string.** A computed slice range in a python rewrite went REVERSED and
  `str.replace("", new)` inserted between every character — 1.2M-line file, total loss, had to rewrite.
