# Scratchpad — Short-Term Working Memory

**Purpose**: Transient details needed within this session or the next 1-2 sessions. Force-loaded via CLAUDE.md. Survives /clear and JICM compression.

**Rules**:
- Write here for: credentials locations, active paths, custom functions, transient gotchas, session-specific commands
- Do NOT write here for stable facts (use MEMORY.md) or work progress (use session-state.md)
- Entries should have dates; prune when stale (AC-08 maintenance + scratchpad-rotate hook)
- **Keep under 80 lines (~2K tokens) to limit force-loaded token cost.** Older entries auto-rotate to `archive/scratchpad-YYYY-MM-DD.md`.

---

## Active Notes

### 2026-06-04 — Project Aion Monorepo Migration

**Active plan**: `.claude/plans/magical-napping-sifakis.md`
**Phase 0**: COMPLETE — tags `pre-aion-migration-2026-06-04` pushed on both repos
**Phase 1**: IN PROGRESS — housekeeping (personal refs, pulsars, credentials, memory, remotes)
**Phases 2-3**: Pending (filesystem migration, GitHub)
**Phases 4-6**: Session 2 (launcher, docs, verification)

**Pre-migration tags**: Both repos tagged + pushed before any destructive changes
- Jarvis: `3eda447` on `Project_Aion` → `CannonCoPilot/Jarvis:main`
- Alfred-Dev: `533be82` on `feature/personas-rebuild` → `CannonCoPilot/Alfred:main`

**Service inventory** (17 Docker containers, 7 launchd agents, 10 tmux windows):
- Jarvis: postgres:5432, qdrant:6333, neo4j:7474/7687, redis:6379, n8n:5678
- Alfred: pulse:8800, dashboard:8701, vite:8702, proxy:9800, postgres (internal), pipeline
- External: caddy:80/443, authentik:9000/9443, authentik_postgres, authentik_redis
- Launchd: 2 David-specific (to unload), 5 operational (keep)

**DB access**: `docker exec aifred-dev-postgres psql -U pulse_dev -d pulse_dev`

### Reference paths (stable across sessions)

- Credentials: `.claude/secrets/credentials.yaml`
- Pulse API: `localhost:8800` (dev)
- Reverse proxy: `localhost:9800`
- Test catalog: `Alfred-Dev/.claude/jobs/test-suites/_catalog.yaml`
- Migration plan: `.claude/plans/magical-napping-sifakis.md`
