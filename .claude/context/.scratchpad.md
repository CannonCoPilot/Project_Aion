# Scratchpad — Short-Term Working Memory

**Purpose**: Transient details needed within this session or the next 1-2 sessions.

---

## Active Notes

### 2026-06-05 — AnnasTools: Anna's Archive MCP + Alfred Persona (ACTIVE)

**Goal**: Build MCP server for Anna's Archive ebook/article downloads using paid member credentials. Deploy as both Jarvis MCP and Alfred persona with Pulse task tickets.
**Project code**: `/Users/nathanielcannon/Claude/Projects/AnnasTools/`
**Planning/research**: `/Users/nathanielcannon/Claude/Project_Aion/projects/annas_archive/`
**Credentials**: `projects/annas_archive/credentials.txt` (member key: `yqgZHZRM45jA1ZNJKajsMpvLtp8WV`)
**API**: RapidAPI wrapper + direct `annas-archive.gl` endpoints
**Status**: MCP server built and live-tested. 8 tools, search verified. Committed at `1c2a2f6`.

**Completed this session:**
- Deep research report: `projects/annas_archive/research-report.md` (627 lines, 20+ sources)
- MCP server: `Projects/AnnasTools/src/annas_tools/mcp_server.py` (505 LOC, FastMCP 3.x)
- 8 tools: searchBook, searchJournal, info, fastDownload, downloadBook, downloadJournal, memberDownload, lookupDoi
- Live test: searchBook("Clean Code Robert Martin") returned 3 results with MD5s, authors, years
- Venv: `Projects/AnnasTools/.venv/` with fastmcp 3.4.2, mcp 1.27.2, httpx
- MCP config added to `.mcp.json` as `annas-archive` (disabled=true, enable on next restart)
- Key finding: no REST API for search (HTML scraping only), only `fast_download.json` is official JSON endpoint
- ES JSON endpoint (`/db/aarecord_elasticsearch/md5:{hash}.json`) returns 403 for some MD5s — HTML fallback needed

**Completed post-JICM:**
- MCP confirmed live (annas-archive loaded at session start with disabled=false)
- `fastDownload` tested: returns valid download URL with member key ✓
- `downloadBook` tested: downloaded Clean Code 2nd Ed (17 MB EPUB) atomically ✓
- `info` HTML fallback enriched: now extracts 12 fields (title, author, publisher, year, description, cover_url, ISBNs, DOIs, filesize, language, extension, alt publishers) — up from just title
- DOI extraction cleaned (trailing smart-quote artifacts stripped)
- Alfred `book-retriever` persona created (4 files: config.yaml, methodology.yaml, permissions.yaml, prompt.md)
- Workflow file created: `alfred/.claude/jobs/workflows/book-retriever.md`

**Next steps:**
1. Push AnnasTools to GitHub as public repo
2. Restart MCP process to pick up enriched `info` parser (process-level cache)

**Also completed this session (GitHub portfolio):**
- 8 showcase repos public with v2 READMEs (embedded images, corrected tone)
- Palimpsest extracted from ThermoThings and deployed
- Profile README at CannonCoPilot/CannonCoPilot (may need manual repo recreation for profile detection)
- Neural Canvas: fixed swan pipeline images (original → crop → placard, same painting)
- DwarfCron: added gameplay screenshot, architecture diagram, explorer UI screenshot
- DwarfCron was on `Dev` branch; force-pushed to `origin/main`

### 2026-06-04 — Professional GitHub Presence (COMPLETE)

8 showcase repos public with professional READMEs. Profile README at CannonCoPilot/CannonCoPilot (pending GitHub detection). 19 repos set private.

### 2026-06-04 — Launch-Aion v3.1 — COMPLETE

6 commits: `9acb8a3` → `1e665bf`

**Changes shipped:**
- Comprehensive preflight (28+ checks, 7 sections: Docker, Jarvis infra, Alfred stack, optional infra, AI services, MCP servers, LaunchAgents)
- Banner: PROJECT AION · Jarvis (Master Archon) + Alfred (Ops Archon)
- W0: `Aion` → `Jarvis`, W5: `Aion-dev` → `Jarvis-dev`
- `AlfDev-Seed` → `Protos` (launcher, chain-executor, bridge)
- Always-on MLX-Embed, LiteLLM, Ollama monitor windows
- `TMUX_SESSION` env var portability in chain-executor + bridge
- 5 LaunchAgent plists updated to monorepo paths
- Project slug symlink (`-Claude-Project_Aion → -Claude-Jarvis`)
- `~/Claude/Alfred-Dev` → symlink to `alfred/` (Protos gets Alfred identity)
- `--resume` with `--continue` fallback for busy sessions
- 3 new `--restart` targets (ollama, mlx, litellm)

**Tmux layout (verified):**
W0:Jarvis, W1:Watcher, W2:Ennoia, W3:Virgil, W4:Commands, W5:Jarvis-dev, W6:MLX-Embed, W7:LiteLLM, W8:Ollama, W9:HUD, W10:Bridge, W11:Protos

### 2026-06-04 — Monorepo Migration — ALL PHASES COMPLETE

Phases 0-6 complete. 14 commits total (`715f7dd` → `1e665bf`).

**Symlinks (permanent):**
- `~/Claude/Jarvis → Project_Aion` — project slug identity anchor
- `~/Claude/Alfred-Dev → Project_Aion/alfred` — Protos/chain-executor git-tree isolation
- `~/.claude/projects/-Claude-Project_Aion → -Claude-Jarvis` — dual-path UUID resolution

### Reference paths

- Launcher: `.claude/scripts/launch-aion.sh`
- Credentials: `.claude/secrets/credentials.yaml` (gitignored)
- Pulse: `localhost:8800` (dev), `localhost:8700` (prod)
- Proxy: `localhost:9800`
- GitHub: `CannonCoPilot/Project_Aion` (origin)
