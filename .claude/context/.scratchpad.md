# Jarvis Scratchpad (L1 session) — RESUME DOC
Pruned 2026-08-19 (216 → 77). Prior `.scratchpad.md.pre-prune-20260819`. Dropped as **grep-verified
durable**: R2.2b/R2.2c/R2.2d-of-08-18/R11.2c/R2.1k + Sir's rulings 1/2/3/5 (`OCR-ROADMAP.md`) · CENTRAL
ARGUMENT (`OCR-MASTERPLAN.md`: content 0.9448 / surface 0.451, +57 cells → 0.8669) · file inventory
(`git status`) · gold-addressing/enumeration + standing-corrections blocks (→ L2 memory).
Repo `Projects/palimpsest` branch main; OCR `projects/originaldr/ocr-spike/`, venv `../ocr-venv/bin/python`.

## 🔴 RESUME HERE — 2026-08-19. R2.2d step (1) ✅ DONE · **R2.2e NEW and it is the big one**
### ➡️ FIRST ACTION: **R2.2e** — 49 rows swallowed whole → typed MarginNote, **44 of them SCRIPTURE**
Outranks step (2): a **shipping-pipeline defect measured with `ARGUMENT_RULE` OFF**, it explains 6 of D1's
29 misses, and **the 121-token gold cannot see it** (head band vs mid-page) ⇒ **0.8760 is not evidence
against it**. § + register row in `OCR-ROADMAP.md`; probe `.scratch/r2/probe_unsplit_rows.py`.
**Mechanism**: ONE token covers a whole printed line (412 r33: 63 glyphs, 90% of the row) ⇒ `l < L or r > R`
**necessarily** fires ⇒ MarginNote — yet `region_segments` splits that row correctly (side-note + argument):
**two splitters disagree and the COARSER one decides the label.** Residual after R2.1h fixed `k=2`; R2.1k =
the same handoff from R3's side.
### ✅ R2.2d STEP (1) DONE — GOLD-ARGUMENT 24 → **81 rows**, 4 → **10** chapter openings
Full detail in `OCR-ROADMAP.md` § "R2.2d STEP (1) DONE" — read it there, do NOT re-derive.
`witness/build_argument_gold.py` **NEW, tracked** (`BLOCKS`/`NEGATIVES` ARE the adjudication; `--check`
re-derives every address) · **15 adjudicated negatives** · `score_argument_region` re-keyed to
**best-overlap**. `ARGUMENT_RULE` still **False**. **D3 ✅ exact AGAIN** (gold TRIPLED,
0.8760/1.0000/0.8947/0.8375 unmoved) · **D1 16/24 → 52/81** · **D2 2 FPs/4 leaves → 13 FPs WHOLE WINDOW** ·
**D4 2/89 → 3/327** · **unadjudicated 46 → 0** ⇐ the caveat on every R2.2d number is DISCHARGED.
⚠️ **D2 got WORSE and that is the step working** — 2-of-4-leaves flattered the rule. No bar moved.
🔴 **FOUNT IS REGION-DEPENDENT**: in ANNOTATIONS (417) italic = **QUOTED SCRIPTURE**, the INVERSE of the body
⇒ 3 FPs **no threshold can reach** ⇒ **D2 has a floor of 3 contiguity cannot touch**, and ANNOTATIONS is a
missing region as ARGUMENT was. Other 11 FPs = italic patristic marginalia inside `in_block`'s tolerance.
**D1's 29 misses**: **20** under the component floor (→ step 2 = CONTIGUITY — a short segment between two
italic ones is italic; **NOT** a lower floor, which re-admits the marginal FPs) · **6** where the segment DOES
fire (**= R2.2e**) · **3** at 6–8 comps.
### ➡️ THEN: step (2) contiguity → re-run D1–D4 · **R2.1k** (`MIN_GLYPHS` = speck filter doing duty as span
estimator; 3 candidates REFUTED, `SPAN_MODE="tokens"` unchanged; next = the region run **FLUSH TO L OR R**,
C2) · **CONTINUITY** (`r2_1d_continuity.py`, caveat `audit_prefix_rule` still exit 1) · **R2.1j-b** ·
R2.2b's band BOTTOM (A1 18/20; A4 moot while A1 fails).
⚠️ **R2.1f fired on the CATCHWORD CANDIDATE, not on R2** ⇒ R2 is at ALERT, NOT exhausted; it still blocks
Gate 0b stage 2, R3 and all transcription. ⚠️ **R2.2c still BLOCKS every region number reaching the reader.**

## ⚠️ ALL WORK IS UNCOMMITTED WORKING TREE (palimpsest, branch main). NOTHING STAGED. `git status` = inventory
* ⚠️ **NEVER `git add -A`** at the palimpsest root — staging is always a deliberate, scoped act.
* Both golds were REWRITTEN in place; backups = the only unrecoverable state:
  `.scratch/r2/gold-{argument_rows.pre-extend,head_regions.pre-r22c-rekey}.json`. gitignored+safe:
  `.scratch/r2/{probe_*.py,plates/,*.log}`.

## ✅ SUITE STATE (2026-08-19) — `test_verification_standard.py` **exit 0, 31 commands**, ALL GREEN
`.scratch/r2/suite-20260819.log`. 9 audits exit 1 = healthy/open · register holds **64** ·
`score_head_regions` 0.8760 · `score_head_tokens` 0.8125 · `gold_rekey_pagefrac` 125/125. Now executes and
verifies `score_argument_region` (`prints 52/81`) + new guard `build_argument_gold.py --check`.
* ⚠️ **Block membership, not claim text, decides whether exit 1 is healthy** — I filed the new guard in the
  AUDITS fence and the standard caught it. A claim runs **only if its line has `->`**; the fraction regex is
  digit-only ⇒ **word claims so the FIRST fraction is real**. **Editing the OPEN register ⇒ ALWAYS re-run
  `audit_prereq_ceilings`** or the standard fails on a stale denominator. (R11.2c)
* ⚠️ `r2_1d_continuity` · `test_verification_standard` · `score_argument_region` each exceed 2 min ⇒ run in
  the BACKGROUND **with `-u`** (a redirected stdout is buffered; without it nothing appears until exit).
## 📜 Artifact — living doc, republish SAME path to keep the URL
`scratchpad/originaldr-architecture.html` → https://claude.ai/code/artifact/9b5a44c2-6efe-4a51-bd11-7ed0a0904f62
§ "The same page, as the machine actually sees it" = 9 plates of REAL leaf scans by tracked
`witness/render_pipeline.py`; archetype plates A–H are still hand-drawn SVG. ⚠️ **Source lives in the
SESSION scratchpad, not the repo — re-extract via WebFetch on the artifact URL before editing.**

## 🔴 TEN INSTANCES OF THE SIGNATURE DEFECT — a correct rule nothing reads
**#10 R2.2e** (`region_segments` splits the row right; the label rides the unsplit token) · **#9 the
argument-gold SCORER** (gold said match by overlap; scorer used float equality) · Gate 0f · Gate 0d · §7.7
ladder · `audit_prereq_ceilings` string + section granularity · **R9.8** · register strikethrough ≠ removal
(guarded) · **R2.2c** (scorers/gold on a different band).
**Most were found by BUILDING THE CONSUMER — repeatedly, by RENDERING THE PAGE.** ⚠️ A test that doesn't
move is not evidence until you confirm it COULD have moved.
## ⚠️ OPEN ENVIRONMENT ITEMS (scratchpad-only — recorded nowhere else)
* 🔴 **Repo declares pre-commit hooks that are NOT installed** (`.pre-commit-config.yaml`, ruff over
  `^core/`; `.git/hooks/` = samples). Manual run: **24 unfixable ruff errors + a 429-line reformat**
  (reverted). **Finding for Sir — not mine to silently fix.**
* `.archive-consensus-full.pre-r9.4b/` **263 MB** untracked, deliberately NOT gitignored so its fragility
  stays visible. `OriginalDRnotes` 192 KB untracked, believed Sir's — leave both.
* **R11.2b**: the verification standard parses only `witness/`-prefixed commands, so the two repo-root R11
  entries sit **uncovered** and are run manually with the repo `.venv`.
* §0.5 ceilings rise on PLANNING and fall on COMPLETION — coverage of the plan, never a progress number.
* ⛔ **R7.5a-2 NOT STARTED** — 261 artefacts from many generators, not one command; survey producers first,
  then regenerate (NEVER edit, R7.5d). Full § in roadmap.
