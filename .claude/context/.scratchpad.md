# Scratchpad — Short-Term Working Memory

**Purpose**: Transient details needed within this session or the next 1-2 sessions. Force-loaded via CLAUDE.md. Survives /clear and JICM compression.

**Rules**:
- Write here for: credentials locations, active paths, custom functions, transient gotchas, session-specific commands
- Do NOT write here for stable facts (use MEMORY.md) or work progress (use session-state.md)
- Entries should have dates; prune when stale (AC-08 maintenance + scratchpad-rotate hook)
- **Keep under 80 lines (~2K tokens) to limit force-loaded token cost.** Older entries auto-rotate to `archive/scratchpad-YYYY-MM-DD.md`.

---

## Active Notes

_(Older entries auto-rotated to /Users/nathanielcannon/Claude/Jarvis/.claude/context/archive/scratchpad-2026-05-22.md on 2026-05-23T05:49:08Z.)_

### 2026-05-29 — Pipeline Infrastructure: Chain-Executor + Test System Audit

**Chain-Executor Architecture (VALIDATED)**:
- Warm AlfDev-Seed interactive session → per-chain forked windows → tmux paste-buffer injection → sentinel file completion
- cache_write reduced 125× vs cold `claude -p` starts (582 vs 72,730 tokens)
- Both parallel probes (A+B) closed successfully in serialized dispatch mode
- 3 bugs fixed: HOST_PROJECT_DIR, reviewer path translation, bridge log→stdout pollution

**Test System Audit COMPLETE**: `Alfred-Dev/docs/test-system-audit-2026-05-29.md`
- 60% of tests outdated (target dead code: dispatcher.sh, pipeline-runner.sh, executor.sh)
- 10 critical gaps identified (signal-delegation, reviewer, chain-executor, FSM, sentinel)
- 8 new test suites cataloged (4 active, 4 planned)
- `_catalog.yaml` created as master registry

**Test Cockpit Dashboard**: `/test-cockpit` in OPS > Monitor menu
- Reads `_catalog.yaml` via `/api/v1/test-catalog` endpoint
- Shows suite status, coverage matrix, run button, deprecated archive
- Pulse container rebuilt with test-suites volume mount

**Files modified (Alfred-Dev)**: host-executor-bridge.sh (v2 rewrite), chain-executor.sh (NEW), executor.py, reviewer.py, docker-compose.dev.yml, pulse/app.py, TestCockpitPage.tsx (NEW), App.tsx, AppShell.tsx, _catalog.yaml (NEW), docs/test-system-audit-2026-05-29.md (NEW)

**Critical Bug Fixed (2026-05-29 session 2)**: executor.py signal-delegation result parsing discarded bridge's `result_text` (which carries `<context-summary>` with `files_modified`) in favor of `json.dumps(cli_data)` when `cli_data` was truthy. Fix: detect bridge-generated results (cli_data lacks `result` and `session_id` keys).

**Test Suite Execution Results (2026-05-29 session 2)**:
- 10 active suites (was 4), 6 deprecated, catalog + dashboard live
- All P0 suites: PASS. P1 suites: 3 PASS, 1 partial (chain-predecessor child 3 formatting). P2: 1 PASS, 1 partial
- Coverage matrix: 17/19 components covered (was 7/19). Only Pulsar + auth-breaker remain NONE
- Key pipeline fixes: executor.py context_summary propagation, per-task timeout_minutes metadata, PYTHONUNBUFFERED for container logging, DNS alias after Pulse restart
- Alfred-Dev commit: `4b503ca` on feature/personas-rebuild (20 files, +3072/-321)

**ACTIVE WORK — Cost Baseline Measurement (paused at 13:08 MDT)**:
- Sir about to manually run each test suite via Test Cockpit "Run" button
- Baseline 5h util: **61%** (allowed), window resets 21:40 UTC (15:40 MDT)
- 462 calls, $134.91, 73.4M cache_read, 1.07M cache_write in window
- NEXT: after suites run, compute per-suite Δutil%. Then add cost-estimate display to Test Cockpit suite chips
- Gotcha: Pulse container needs DNS alias `pulse` re-added after every restart

### Reference paths (stable across sessions)

- Jarvis credentials: `.claude/secrets/credentials.yaml`
- Pulse API: `localhost:8700` (prod), `localhost:8800` (dev)
- Reverse proxy: `localhost:9800`
- Live watcher PID file: `.claude/context/.jicm-watcher.pid`
