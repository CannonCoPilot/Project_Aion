# Scratchpad — Short-Term Working Memory

**Purpose**: Transient details needed within this session or the next 1-2 sessions. Force-loaded via CLAUDE.md. Survives /clear and JICM compression.

**Rules**:
- Write here for: credentials locations, active paths, custom functions, transient gotchas, session-specific commands
- Do NOT write here for stable facts (use MEMORY.md) or work progress (use session-state.md)
- Entries should have dates; prune when stale (AC-08 maintenance)
- Keep under 80 lines to limit force-loaded token cost

---

## Active Notes

### 2026-05-03 ~16:00Z (post-JICM-RESUME #4): Phase 2 CoD prep — 4 artifacts shipped

**Step 4 of User-approved 4-step sequence — IN PROGRESS**.

This cycle completed all 6 prep tasks for Phase 2 CoD (Tasks 2.1-2.4 prep; Task 2.5 benchmarking remains gated on Phase 1.1 Stage-2 PASS at 2026-05-15).

**Artifacts shipped (full paths)**:
1. `/Users/nathanielcannon/Claude/Jarvis/.claude/metrics/token-compression/pre-registration-phase-2-cod.yaml` — FIRST CLEAN pre-deploy pre-registration in token-compression family. Per-task-type thinking-token reduction hypothesis (code_review -55, bug_diagnosis -50, planning -45, research -35, session_mgmt -40 ±15pp each); novel register failure mode (stray `<draft>`/`<answer>` XML tags); skip-rule compliance as Stage-1 axis with zero tolerance; effort_class=per_prompt_telemetry (Stage-1 PT48H, Stage-2 P14D = 7× ratio per pattern §5).
2. `/Users/nathanielcannon/Claude/Jarvis/.claude/skills/token-compression/templates/chain-of-draft-single-line.txt` — arxiv-verbatim seed only ("Think step by step, but only keep a minimum draft for each thinking step, with 5 words at most."), ~25 tokens. Existing `chain-of-draft.txt` retained as "full" variant (paraphrased seed + 4 generic few-shots).
3. `/Users/nathanielcannon/Claude/Jarvis/projects/project-aion/designs/cod-task-type-taxonomy.md` (~10KB) — defines 5 Jarvis task types per roadmap §3 Phase 2.2. Each: definition, distinguishing features, reasoning shape, baseline thinking-token range, CoD fit, quality regression risk, sample prompts. Structure-fit theory: structured-checklist tasks (code_review) compress best; multi-source-synthesis tasks (research) compress least.
4. `/Users/nathanielcannon/Claude/Jarvis/projects/project-aion/designs/cod-injection-architecture.md` (~14KB) — combines Task 14 (task-type routing) + Task 15 (runtime wiring). Recommendation: Option A (user-tagged routing via `[task: <type>]` prefix or frontmatter) + Option I/II in parallel (UPS hook for main-session reasoning + per-subagent system-prompt prepend). detect-phase.sh assessed as v0.1.0 stub; not suitable for proactive routing. Hook design mirrors jicm-gate.sh (sensing-only, additionalContext, no decision:block).

**Existing skill skeleton inventory (pre-cycle)**:
- `apply-cod.sh` (5KB, functional) — template injector with --dry-run, --force, idempotency, metrics-log
- `templates/chain-of-draft.txt` (existing) — paraphrased seed + 4 generic few-shots
- `benchmark-harness.sh` (14KB) — sophisticated cache_ratio-based session classifier; per-mode aggregation; vs-baseline computation
- `token-extractor.py`, `cache-telemetry-extractor-v2.py`, `pipeline-telemetry-extractor.py` — telemetry pipeline complete
- `config.yaml` — phase × level → strategy routing matrix; reasoning row maps to cod strategy already

**Three OPEN QUESTIONS for User decision before Phase 2.4 wiring** (§8 of architecture doc):
1. Prefix-tag (`[task: code-review]`) vs frontmatter (`---\ntask: code-review\n---`) for task-type signaling — preference?
2. Subagent prepend strategy: per-agent edit (12 files) vs new central agent-prompt-builder — build now or defer to Phase 4?
3. Default opt-in (always-on for tagged prompts) vs opt-out (`JICM_COD_ENABLED=1` required) — Stage-1 conservatism preference?

**Suggested commit (Jarvis production)**: `feat(token-compression): Phase 2 CoD prep — pre-reg + single-line variant + task-type taxonomy + injection architecture`

Files in commit: 4 new artifacts + active-plan + scratchpad = 6 files.

**Next on resume / after User answers open questions**:
- Task 2.1.b (apply single-line CoD to 5 reasoning sessions; capture baseline thinking-token counts)
- Tasks 2.2 (gated on 2.1.b results; only proceed if quality regression observed with single-line)
- Task 2.3.a (extend apply-cod.sh with --task-type / --variant flags per architecture §2.2)
- Task 2.4 wiring (deploy cod-inject.sh hook; register in settings.json)
- Stage-1 deploy → 48h window → Stage-1 verdict → if STAGE_1_CLEAR, proceed
- Phase 1.1 Stage-2 PASS gate at 2026-05-15 → unblocks Task 2.5 benchmarking

---

### 2026-05-03 ~15:10Z (JICM-HALT #2 in this trajectory — User chaining cycles before next workstream)

**State at HALT**: No material change since 14:50Z save. User issued back-to-back HALT after just resuming from the prior cycle, before responding to the "Available next moves" menu (A-E options offered). Reading: User wants a third fresh context window, likely to start Step 4 (Phase 2 CoD) or unrelated work with maximum headroom.

**HUD-Live in W7**: Still active per `tmux list-windows` check. Survived the prior JICM cycle. Will likely survive this one too. Cleanup command if/when User wants: `$HOME/bin/tmux kill-window -t jarvis:HUD-Live`.

**Cycle health**: This is now the 3rd v7.9 production cycle (1st natural ~05:00Z, 2nd User-triggered ~14:50Z, 3rd this one). Per 7.9.6c readiness criterion ("a few clean v7.9 cycles validate operational confidence"), we're building toward shim removal. Each cycle uneventful — prep script + signal chain + session-start hook + resume signal all fire correctly.

**On RESUME**: present the same A-E menu (or User will indicate directly). Most likely path: Step 4 — Phase 2 CoD prep — read `projects/project-aion/reports/token-compression-roadmap.md` + `token-compression-implementation-guide.md` for Phase 2 scope.

**Outstanding (unchanged from 14:50Z entry below)**:
1. Side-quest commits suggested in overnight-projects-2026-05-03.md (3 commits drafted, awaiting User bless)
2. Insights snapshot chore commits dirty in both repos
3. Production main 3 commits ahead of origin/main
4. Step 4 (Phase 2 CoD) is the next core-dev workstream

---

### 2026-05-03 ~14:50Z (JICM-HALT mid-side-quest-review): Watcher HUD + Statusline-v9 BUILT + REVISED + LIVE-DEMOED

**State at HALT**: User-approved overnight side-quest projects shipped through full review/refine/demo cycle. Both projects in production-space (per User permission constraint), additive-only, untouched: jicm-watcher.sh, jarvis-statusline-v8.sh, settings.json, hooks.

**Files created (full paths)**:
- `/Users/nathanielcannon/Claude/Jarvis/.claude/scripts/jicm-watcher-hud.sh` (~1265 lines after revision; htop-style sidecar dashboard, 13 sections including new Project/Pulse/Git strip, --once / --demo / --demo-state=N / live modes)
- `/Users/nathanielcannon/Claude/Jarvis/.claude/scripts/jarvis-statusline-v9.sh` (~840 lines after revision; 3-row JICM-aware drop-in for v8, 22 panels, 8 demo states)
- `/Users/nathanielcannon/Claude/Jarvis/.claude/scratch/statusline-research-2026-05-03.md` (deep-research synthesis, 14 sources)
- `/Users/nathanielcannon/Claude/Jarvis/.claude/scratch/overnight-projects-2026-05-03.md` (comprehensive morning-review report)
- `/Users/nathanielcannon/Claude/Jarvis/projects/project-aion/designs/watcher-hud-design.md`
- `/Users/nathanielcannon/Claude/Jarvis/projects/project-aion/designs/statusline-v9-design.md`

**Revision pass added (vs initial overnight build)**:
- HUD: Project Focus + Pulse counts + Git status strip; cycles-completed count from log; NLP standalone metrics row
- Statusline: "idle  " replaces "Δ0/m" (aligned padding); session_name display (`@SessionName` magenta when /rename'd); Project:SubProject hierarchical pattern when CWD is subdir of project_dir; NLP compression panel reading `.jicm-nlp-compression.json`; WORKTREE_BRANCH wired for non-demo path with ⌐ pink prefix

**Live demo status (at HALT)**:
- HUD-Live running in tmux W7 (jarvis:HUD-Live, 191x45). User can switch via Ctrl-B 7. Press q to quit.
- Statusline-v9 demo gallery printed inline to W0 conversation.
- Statusline-v9 NOT activated in settings.json — v8 stays wired.

**On RESUME — outstanding items**:
1. User reviewing demos. May ask for further refinements.
2. HUD-Live window 7 may still be running. To clean up: `$HOME/bin/tmux kill-window -t jarvis:HUD-Live`
3. If User blesses, suggested commits drafted in overnight-projects-2026-05-03.md (3 commits: HUD feat, statusline feat, designs docs)
4. AFTER side-quest review concludes: return to Step 4 of User-approved 4-step sequence — Phase 2 CoD initial work (2.1-2.4 prep; 2.5 gated on Phase 1.1 Stage-2 PASS)
5. Insights snapshot chore commits still outstanding (`.captured-hashes.json` + `insights-log.md` dirty in both repos)
6. Production main is 3 commits ahead of origin/main (57cb3ed sensing, 2a559b5 watcher swap, 9e3afea Phase 0.2)

**HALT signal context**: User invoked HALT mid-conversation after demo review (single-line signal arrived in same prompt as `/clear` — user is doing a manual JICM cycle to reset context for whatever comes next). No scratchpad clutter to clean; just save trajectory and acknowledge per protocol.

---

### 2026-05-03 ~05:00Z (JICM-HALT): FIRST v7.9 cycle in production — validation moment

**Significance**: This is the first organic JICM-HALT fired by the v7.9 slim watcher (PID 74731) since cutover at 04:46Z. Approach C back-compat shim is being exercised end-to-end for the first time. If the cycle completes cleanly through CLEARING → RESTORING → WATCHING, it validates the entire 7.9.6a + 7.9.6b architecture.

**Trajectory state at HALT** (all SHIPPED this session):
- Commits production: 57cb3ed (7.9.6a sensing) → 2a559b5 (7.9.6b watcher swap) → 9e3afea (Phase 0.2 refactor)
- Commits Jarvis-Dev: 32a592f (harness fix) → 746b6dd (Approach C shim) → 1cddeda (Phase 0.2 mirror)
- Net production change: +1663 / -1588 = +75 lines (3 architectural improvements at flat footprint)

**User-approved 4-step sequence — status at HALT**:
1. Phase 0.5 to completion ✓ (prior session)
2. JICM v7.9 to completion ✓ (this session — 7.9.6a + 7.9.6b shipped; 7.9.6c queued)
3. Phase 0.2 refactor ✓ (this session)
4. **Phase 2 CoD initial work (2.1-2.4 prep; 2.5 gated on Phase 1.1 Stage-2 PASS) — NEXT WORKSTREAM ON RESUME**

**State-hook last sample** (from validation run at ~04:58Z):
- cache_hit_rate: 0.9886 (98.86%)
- eph_1h_adoption: 100% (4332/4332)
- tokens: 379506 / 1M window (38% used)
- model: claude-opus-4-7
- New schema fields all populating correctly

**7.9.6c (DEFERRED, pre-v8.0)**:
- Remove v73_shim_write_state() helper from jicm-watcher.sh
- Re-gate session-start.sh on .jicm-clear-now.signal (instead of state=CLEARING)
- Retire .jicm-state file
- Trigger: a few clean v7.9 cycles validate operational confidence — TODAY'S CYCLE COUNTS AS #1 IF SUCCESSFUL

**Live cycle observation plan**:
1. After /clear: verify session-start.sh JICM v7 branch fired (gate read state=CLEARING from shim write)
2. Verify .jicm-resume-complete.signal was written (line 417 patch from 7.9.6a)
3. Verify slim watcher consumed resume signal and sent RESUME prompt
4. Verify shim wrote state=RESTORING then state=WATCHING for cycle completion
5. Read jicm-watcher.log for any errors or timeouts

**Stage-2 14d passive observation** (DEMOTED to data-gathering, not gating):
- Window: 2026-05-03T03:25Z → 2026-05-17T03:25Z
- Verdict doc target: .claude/metrics/jicm/v7-9-stage-2-result-2026-05-17.md
- Role: feed v8.x design decisions, NOT gate 7.9.6c

**On resume**: continue Step 4 (Phase 2 CoD initial work). Read token-compression-roadmap.md and token-compression-implementation-guide.md for Phase 2 scope. CoD = compression directive scaffolding for the next layer of brevity work.

**Open suggestions from prior turns** (no User decisions pending):
- Insights snapshot chore commits (both repos have dirty `.captured-hashes.json` + `insights-log.md`)
- session-state.md refresh (last updated reflects pre-deploy state)
- Push Jarvis production main branch (3 commits ahead of origin/main: 57cb3ed, 2a559b5, 9e3afea)
- Push Jarvis-Dev dev branch (3 commits ahead of origin/dev: 32a592f, 746b6dd, 1cddeda)

---

### 2026-05-03 ~04:46Z: Phase 7.9.6b SHIPPED — slim watcher live in production (Approach C)

**User decisions**:
- Stage-2 DEMOTED from gate → passive data-gathering (informs v8.x design, not 7.9.6b gate)
- Approach C chosen for cutover (back-compat shim on slim watcher; minimal blast radius)
- Final state target = Approach A (re-gate session-start.sh, retire `.jicm-state`); pre-v8.0 cleanup planned as 7.9.6c
- /schedule offers suppressed for the duration of this trajectory

**Slim watcher patch (Jarvis-Dev jicm-watcher.sh, 171→187 lines)**:
Added v73_shim_write_state() helper at line 100, called at 3 transition points:
- Before /clear: `state: CLEARING` (line 146)
- Before RESUME: `state: RESTORING` (line 163)
- After cycle complete: `state: WATCHING` (line 172)

Shim writes minimal 3-field text to `.jicm-state`:
```
state: %s
timestamp: %s
v79_shim: true
```
Satisfies production session-start.sh JICM v7 branch parser. `v79_shim` field marks shim-written entries for downstream auditing.

**Production cutover sequence (this session, ~04:42–04:46Z)**:
1. Backed up v7.3 artifacts:
   - `/Users/nathanielcannon/Claude/Jarvis/.claude/scripts/jicm-watcher-legacy-v7-3.sh` (57574B)
   - `/Users/nathanielcannon/Claude/Jarvis/.claude/scripts/jicm-config.sh.pre-v7-9b-2026-05-03` (2340B)
2. Copied 5 v7.9 files from Jarvis-Dev to Jarvis production:
   - `jicm-watcher.sh` (slim+shim, 7807B)
   - `jicm-config.sh` (v7.9, 4008B; backward-additive — preserves all v7.x fields)
   - `jicm-inject.sh` (1993B)
   - `jicm-inject-tmux.sh` (2762B)
   - `jicm-inject-pty.sh` (1197B placeholder; v7.9 returns exit 2)
3. Sent Ctrl+C to W1 tmux → v7.3 PID 78594 received SIGINT → trap fired → PID file cleaned
4. tmux W1 window dropped (remain-on-exit off; Ctrl+C also killed wrapper zsh)
5. Created new W1: `tmux new-window -t jarvis:1 -n Watcher`
6. Launched slim watcher via send-keys (text + Enter as separate calls per CLAUDE.md)
7. Verified: PID 74731 alive; log shows `watcher v7.9 started (pid 74731, project Jarvis)` and `main loop (poll 1s, target jarvis:0, backend tmux)`

**Live verification** (deferred — first JICM cycle will exercise the new chain):
The slim watcher hasn't yet completed an actuation cycle in production — it's polling `.jicm-clear-now.signal` (which jicm-stop.sh writes when threshold crossed). When the first cycle fires, expect:
- `state: CLEARING` written to `.jicm-state` (shim) just before /clear
- session-start.sh JICM v7 branch fires on detecting state=CLEARING
- `.jicm-resume-complete.signal` written (the line 417 patch from 7.9.6a)
- Slim watcher consumes resume signal, sends RESUME prompt
- `state: WATCHING` shim write returns file to baseline

**Timezone observation**: v7.3 logged in UTC (`%Y-%m-%dT%H:%M:%SZ`); v7.9 logs in local with offset (`%Y-%m-%dT%H:%M:%S%z`). Both visible in jicm-watcher.log; useful boundary marker for first-v7.9-cycle audit.

**7.9.6c (DEFERRED)**: remove back-compat shim before v8.0. Trigger: operational confidence (a few clean cycles). Scope: remove v73_shim_write_state(); re-gate session-start.sh on `.jicm-clear-now.signal`; retire `.jicm-state`. Required before v8.0 work.

**Suggested commits**:
- Jarvis-Dev: `feat(jicm-watcher): Approach C back-compat shim for v7.3 session-start.sh gating`
- Jarvis production: `feat(jicm): v7.9 slim watcher promoted to production (Phase 7.9.6b; Approach C)`

**Files touched** (full paths):

Jarvis-Dev:
- `/Users/nathanielcannon/Claude/Jarvis-Dev/.claude/scripts/jicm-watcher.sh` (3 Edits: helper + 3 call sites)

Jarvis production (new):
- `/Users/nathanielcannon/Claude/Jarvis/.claude/scripts/jicm-inject.sh`
- `/Users/nathanielcannon/Claude/Jarvis/.claude/scripts/jicm-inject-tmux.sh`
- `/Users/nathanielcannon/Claude/Jarvis/.claude/scripts/jicm-inject-pty.sh`
- `/Users/nathanielcannon/Claude/Jarvis/.claude/scripts/jicm-watcher-legacy-v7-3.sh` (backup)
- `/Users/nathanielcannon/Claude/Jarvis/.claude/scripts/jicm-config.sh.pre-v7-9b-2026-05-03` (backup)

Jarvis production (overwritten):
- `/Users/nathanielcannon/Claude/Jarvis/.claude/scripts/jicm-watcher.sh` (57574B v7.3 → 7807B v7.9 slim+shim)
- `/Users/nathanielcannon/Claude/Jarvis/.claude/scripts/jicm-config.sh` (2340B v7.3 → 4008B v7.9)

---

### 2026-05-03 ~03:35Z (post-/clear): Phase 7.9.6a SHIPPED — Stage-2 14d window OPEN

**Verdict on integration question** (the gating concern from ~02:30Z entry below):

Verified via grep against `/Users/nathanielcannon/Claude/Jarvis/.claude/scripts/jicm-prep-context.sh`:
- Line 754: `JICM_STATE_FILE="${JICM_STATE:-$PROJECT_DIR/.claude/context/.jicm-state}"` (variable definition only)
- Lines 759-761: READ-only access (`grep -oE 'context_tokens=...'` for telemetry)
- **NO write operations** to `$JICM_STATE_FILE` anywhere in prep script

Verified via grep against `/Users/nathanielcannon/Claude/Jarvis/.claude/scripts/jicm-watcher.sh`:
- Line 1001: `transition_to "CLEARING"` — watcher writes the state
- Line 11 (header comment): full state machine documented

**Decision: Approach B confirmed**. Phase 7.9.6 SPLIT:
- **7.9.6a (THIS SESSION, 2026-05-03T03:25Z)**: sensing layer only — hooks + statusline + session-start.sh patch. v7.3 watcher untouched.
- **7.9.6b (DEFERRED post Stage-2 PASS)**: watcher swap with chosen integration strategy (Approach A re-gate session-start, or Approach C back-compat watcher write).

**Files deployed this session** (full paths):
- `/Users/nathanielcannon/Claude/Jarvis/.claude/hooks/jicm-gate.sh` (9377B; UPS#1, sensing-only)
- `/Users/nathanielcannon/Claude/Jarvis/.claude/hooks/jicm-stop.sh` (4388B; Stop#1, no-ops in Approach B)
- `/Users/nathanielcannon/Claude/Jarvis/.claude/hooks/jicm-precompact.sh` (3099B; PreCompact#3, fires only at native 70%+ compact)
- `/Users/nathanielcannon/Claude/Jarvis/.claude/scripts/jarvis-statusline-v8.sh` (9951B)
- `/Users/nathanielcannon/Claude/Jarvis/.claude/scripts/jicm-state-update.sh` (2338B)

**Files patched this session** (full paths):
- `/Users/nathanielcannon/Claude/Jarvis/.claude/settings.json` — statusLine block + 4 hook insertions; jq-validated; MD5 changed `51a0650e…` → `2303a853…`
- `/Users/nathanielcannon/Claude/Jarvis/.claude/hooks/session-start.sh` — 4-line `.jicm-resume-complete.signal` block at line 417 (inside JICM v7 branch, after jq output, before `exit 0`); bash -n OK

**Backups** (full paths):
- `/Users/nathanielcannon/Claude/Jarvis/.claude/settings.json.pre-v7-9-2026-05-03` (8644B)
- `/Users/nathanielcannon/Claude/Jarvis/.claude/hooks/session-start.sh.pre-v7-9-2026-05-03` (21954B)

**New documentation**:
- `/Users/nathanielcannon/Claude/Jarvis/.claude/metrics/jicm/v7-9-stage-2-baseline-2026-05-03.md` — 6 acceptance criteria, coexistence behavior table, monitoring plan, rollback procedure, Stage-1 evidence reference, 7.9.6b prerequisites.

**Stage-2 14d window**: 2026-05-03T03:25Z → 2026-05-17T03:25Z. Verdict at window close: STAGE_2_PASS unblocks 7.9.6b; STAGE_2_REGRESSION_CATCH triggers rollback (one `cp` per file from `.pre-v7-9-2026-05-03` backups).

**Coexistence assertion**: v7.9 hooks write to `.jicm-state-hook.json`; v7.3 watcher writes to `.jicm-state`. Different files. v7.9 hooks do not intersect the v7.3 actuation chain. Stop hook in particular is permanently no-op until a v7.9 watcher writes `pending_action` (which it doesn't, since watcher swap deferred).

**Mid-session activation note**: Hook script files cached at session start per MEMORY.md. Settings.json reload behavior undocumented; worst case is observation-only firing (gate is sensing, stop no-ops, precompact only at 70%+). Stage-1 PASSED 5/5; risk bounded.

**Suggested commit (Jarvis production)**: `feat(jicm): v7.9 hooks + sensing + statusline deployed (Stage-1 CLEAR; Stage-2 begins)`

Files in commit: 5 new artifacts + 2 patched + 1 baseline doc + active-plan + this scratchpad = 10 files. Watcher untouched: PID 78594 still v7.3, 27 cycles complete, last cycle manual JICM trigger 2026-05-03T03:25:13Z.

**Mirror status to Jarvis-Dev**: pending — User confirmed 2026-05-03 ~03:35Z to do production-side first then mirror Jarvis-Dev planning docs.

---

### 2026-05-03 ~03:23Z (JICM-HALT #2): manual /clear trigger fired

**Context**: Watcher was in 160s post-cycle-#26 cooldown; User asked to manually drive a fresh JICM cycle. Pre-/clear state preserved:
- `.compressed-context-ready.md` regenerated 03:22:45Z (225 lines, 19625B, llm-enriched, qwen3:8b) — captures session-state refresh + Phase 7.9.5 PASS + Phase 7.9.6 deploy BLOCKED
- `.jicm-state` overwritten to `state: CLEARING` (manual; pid: manual-trigger-jicm-halt; trigger_source: manual_user_request_post_halt). session-start.sh will see this on /clear and take the JICM v7 branch (lines 354-416) which injects compressed context as additionalContext.
- `.command-signal` written at 03:22:45Z with `{"command":"/clear","args":"","source":"manual-jicm-post-halt"}` — command-handler PID 78611 (W4) polls every 3s.
- session-state.md refreshed with Phase 7.9 status table; active-plan + scratchpad already current.

**Possible duplicate /clear**: User then typed /clear directly. If the command-handler hadn't yet fired, it may inject a SECOND /clear into the new session ~3s after restart. After resume, **delete `.command-signal` immediately** if present to prevent stale duplicate fire.

**Resume directive**: Phase 7.9.6 still BLOCKED on the integration question (see entry below). Recommended next step: re-verify whether `jicm-prep-context.sh` writes `.jicm-state` (grep for `> "$JICM_STATE_FILE"`); decide Approach A vs B; deploy hooks/sensing/statusline regardless.

---

### 2026-05-03 ~02:30Z (JICM-HALT mid-Phase-7.9.6): production deploy BLOCKED on integration question

**Status**: Stage-1 PASSED 5/5 (User-confirmed). Phase 7.9.6 production deploy IN PROGRESS, halted before any production file was modified — survey only.

**Production current state surveyed**:
- W1 watcher PID 78594 RUNNING (`/Users/nathanielcannon/Claude/Jarvis/.claude/scripts/jicm-watcher.sh --interval 3`); last successful cycle 19:00:56 (RESTORING→WATCHING), idle checkpoint #217 at 21:07:07Z
- Production scripts present: `jicm-watcher.sh`, `jicm-config.sh`, `jicm-prep-context.sh`. NO jicm-*.sh hooks in production yet.
- Production settings.json: UPS=`pre-clear-context-prep.sh`; Stop=4 hooks (no jicm-stop); PreCompact=`precompact-analyzer.js`; statusLine=null
- Production session-start.sh: 507 lines (Jarvis-Dev: 586). JICM v7 branch at lines 354-416 (gated on `.jicm-state` file with state=CLEARING/RESTORING)
- Roadmap §4.6 acceptance: criteria 1-6 met by Stage-1 work; criterion 7 (14-day clean window) is what Stage-2 tracks
- Tmux W6 has lingering `JICM-test-` window — leftover, can be killed

**CRITICAL COMPATIBILITY GAP discovered before any production file was touched**:
- v7.9 jicm-watcher.sh waits for `.jicm-resume-complete.signal` (step 7) after sending /clear
- Jarvis-Dev session-start.sh writes that signal at line 436, but ONLY inside the JICM v7 branch (gated on `.jicm-state` file with state=CLEARING)
- v7.9 jicm-watcher.sh does NOT write `.jicm-state` (only writes `.jicm-clear-now.signal`)
- Production `jicm-prep-context.sh:754` only references `.jicm-state` as a path — needs to verify whether it WRITES (legacy v7.3 watcher behavior) or just reads
- IF prep script writes `.jicm-state` with state=CLEARING when invoked → integration works, JICM v7 branch fires, resume signal written, watcher unblocks
- IF prep script does NOT write `.jicm-state` → v7.9 watcher takes the timeout path and sends RESUME anyway (eventually works but loses the resume-signal handshake)

**Deploy approach decision (deferred to next session)**:
- **A. Full v7.9 swap** (per active-plan): copy hooks + scripts + statusline; backup current watcher as `jicm-watcher-legacy-v7-3.sh`; update settings.json; surgical session-start.sh patch; restart W1 watcher. RISK: live UPS/Stop/watcher swap on the working session.
- **B. Conservative incremental** (RECOMMENDED): deploy v7.9 hooks + statusline + settings.json updates + session-start.sh patch this session; defer watcher swap to phase 7.9.6b after explicit integration validation. Delivers criteria 1-6 immediately; criterion 7 (watcher ≤ 8KB) deferred. RISK: lower — keeps working v7.3 watcher; new hooks add observability without touching the actuation chain.
- **C. Hybrid**: also patch the v7.9 watcher to write `.jicm-state` for back-compat with current session-start.sh. Adds complexity beyond what was Stage-1-validated.

**Strong recommendation: Approach B**. Phase 7.9.6a = hooks/sensing/statusline (this session intent); phase 7.9.6b = watcher swap (separate session with explicit integration validation: drive a real JICM cycle end-to-end on Jarvis-Dev with the new watcher + production-equivalent session-start.sh, observe context restoration succeeds).

**Files NOT yet modified in production** (all reads only this session):
- `/Users/nathanielcannon/Claude/Jarvis/.claude/hooks/*` — untouched
- `/Users/nathanielcannon/Claude/Jarvis/.claude/scripts/jicm-*.sh` — untouched
- `/Users/nathanielcannon/Claude/Jarvis/.claude/settings.json` — untouched

**Files in Jarvis-Dev READY to copy/source** (Stage-1 PASS validated):
- Hooks: `jicm-gate.sh` (9377B), `jicm-stop.sh` (4388B), `jicm-precompact.sh` (3099B)
- Scripts: `jicm-watcher.sh` (6726B), `jicm-config.sh` (4008B), `jicm-state-update.sh` (2338B), `jicm-inject.sh` (1993B), `jicm-inject-tmux.sh` (2762B), `jicm-inject-pty.sh` (1197B), `jarvis-statusline-v8.sh` (9951B)
- session-start.sh patch (Jarvis-Dev lines 433-436): writes `.jicm-resume-complete.signal` after JICM v7 jq output succeeds — small additive block, surgical-applicable

**Next-session resume directive**:
1. Re-verify whether `jicm-prep-context.sh` writes `.jicm-state` (grep for `> "$JICM_STATE_FILE"` or `echo.*state:.*CLEARING`)
2. If it writes → Approach A is safe; proceed with full deploy
3. If it doesn't → Approach B (deploy hooks/statusline/settings.json/session-start patch only; defer watcher swap to 7.9.6b). The session-start.sh patch should be applied to BOTH the JICM v7 branch (lines ~414) AND the fallback path (lines ~470+) so the resume-signal fires regardless of which branch handles the /clear
4. Backup convention: `<file>.pre-v7-9-2026-05-03` for any overwritten file
5. Start Stage-2 14d window: write baseline doc at `.claude/metrics/jicm/v7-9-stage-2-baseline-2026-05-03.md` capturing pre-deploy state for diff against post-deploy observations
6. Suggested commit (Jarvis production): `feat(jicm): v7.9 hooks + sensing + statusline deployed (Stage-1 CLEAR; Stage-2 begins)`

---

### 2026-05-03 ~01:50Z: Harness fix v2 — AC-01 queueing race resolved

**Run #2 result**: 4/5 PASS — T1/T2/T4/T5 PASS, T3 FAIL (`gate_ok=0 stop_ok=0`).

**T3 verdict captured state**: `tokens=0, model_id="", action=WATCHING, hard_threshold_tokens=1, transcript_path=.../9e743fb2-...jsonl`. The `hard_threshold_tokens=1` proved the env var propagated. The `tokens=0` was the WARMUP UPS write — i.e., the test prompt's UPS hook never fired in that session.

**JSONL forensics** (`9e743fb2-...jsonl`): 50 entries, 6 assistant messages. Warmup "Hi" triggered Jarvis AC-01 autonomic behavior (read session-state.md, current-priorities.md; ran `git log`, `git status`). Multiple tool_use loops before turn ended. The harness's `wait_for_assistant_turn` returned on the FIRST assistant entry (a tool_use), then sent "Hello again." while Claude was still mid-AC-01. Claude Code TUI **queued** the prompt (entry 24 = `last-prompt`) instead of firing UserPromptSubmit. The queued prompt eventually ran AFTER the harness gave up — final `stop_reason: "end_turn"` arrived past T3's 90s clear-signal timeout.

**Why T1/T2 passed run #2**: identical AC-01 happened (~110K tokens consumed during warmup turn in both), but the timing happened to land such that wait_for_assistant_turn + 1s sleep + send-keys hit a moment when Claude WAS input-ready. Race condition, not deterministic correctness. T3 just lost the race.

**Fix v2 (this session)**:
- New helper `wait_for_idle(transcript, timeout=180)` — polls JSONL until `(map(select(.type=="assistant")) | last | .message.stop_reason)` ∈ `{end_turn, stop_sequence, max_tokens}`. Tested directly against the failed-run JSONL: returns `"end_turn"` correctly, meaning had the harness used wait_for_idle, the test prompt would have fired *after* AC-01 completed.
- `warmup_session_and_send` now calls `wait_for_idle` instead of `wait_for_assistant_turn`. The latter is retained as a building block but no longer in the warmup flow.
- 180s timeout absorbs heavy AC-01 sequences (Jarvis-Dev's CLAUDE.md is large; 30-90s typical).
- USAGE comment updated: warmup now consumes 50-150K tokens (full AC-01 work to end_turn). Total Stage-1 budget: ~$0.50-1.00 (was ~$0.30-0.60).
- 429 → 458 lines; `bash -n` clean.

**T1/T2/T4/T5 expected behavior unchanged** — they always passed, will continue to pass; the fix only changes WHEN the warmup-to-test transition happens, not whether the underlying check is correct.

**User re-run command** (cwd unchanged):
```
cd /Users/nathanielcannon/Claude/Jarvis-Dev
bash .claude/scripts/dev/jicm-v7-9-stage-1-harness.sh
```
Wall-clock ~6-12 minutes (warmup turns are now slower but deterministic). Cost ~$0.50-1.00.

**Suggested Jarvis-Dev commit**: `fix(jicm-harness): wait_for_idle (end_turn) so AC-01 autonomic work completes before test prompt`

**Files touched (Jarvis-Dev)**:
- `/Users/nathanielcannon/Claude/Jarvis-Dev/.claude/scripts/dev/jicm-v7-9-stage-1-harness.sh` (3 edits: USAGE comment, +wait_for_idle helper, warmup_session_and_send call site)

**Files touched (Jarvis production)**:
- `/Users/nathanielcannon/Claude/Jarvis/.claude/context/.active-plan` (Phase 7.9.5 fix-v2 block, cost, commit suggestion #3)
- `/Users/nathanielcannon/Claude/Jarvis/.claude/context/.scratchpad.md` (this entry)

**Insight worth keeping (candidate for insights-log)**: Claude Code TUI queues prompts during tool loops; `select(.type=="assistant")` is too lenient for "input-ready" — must check `stop_reason` of the most-recent assistant entry. Any harness or watcher that injects user input during a session must respect this.

---

### 2026-05-03 ~01:10Z (post-/clear): Harness fix LANDED in Jarvis-Dev — ready for User re-run

**Fix applied** to `/Users/nathanielcannon/Claude/Jarvis-Dev/.claude/scripts/dev/jicm-v7-9-stage-1-harness.sh`:

Three new helpers + T1/T2/T3 refactor:
- `wait_for_assistant_turn(transcript, timeout=60)` — polls JSONL for `select(.type=="assistant")` exit 0
- `wait_for_state_condition(jq_filter, timeout=30)` — polls `.jicm-state-hook.json` for arbitrary jq predicate (catches the SECOND state write after warmup, not the warmup's tokens=0 write)
- `warmup_session_and_send(win, test_prompt)` — sends "Hi", waits for state file → reads transcript_path → polls for assistant turn → sends real test prompt; sets `WARMUP_FAIL_REASON` for caller diagnostics
- T1 / T2 / T3 each refactored: cold-start sleep → `warmup_session_and_send` → `wait_for_state_condition` for verification
- T3 clear-signal timeout 60s → 90s (Stop hook only fires after Claude finishes responding to test prompt; needs latency budget)

**Diff summary**: 350 → 429 lines; `bash -n` clean. Per-test cost ~$0.05-0.10 → ~$0.10-0.20 (two prompts each). Total Stage-1 budget: ~$0.30-0.60.

**T4 / T5 unchanged** — T4 PASS confirmed watcher mechanics; T5 PASS confirmed defensive stdin path.

**User invocation when ready** (unchanged):
```
cd /Users/nathanielcannon/Claude/Jarvis-Dev
bash .claude/scripts/dev/jicm-v7-9-stage-1-harness.sh
```

Expected outcome: 5/5 PASS → STAGE_1_CLEAR → unblocks Phase 7.9.6 production deploy.

**Suggested Jarvis-Dev commit**: `fix(jicm-harness): warmup-prompt + assistant-turn polling so T1-T3 see populated JSONL`

**Files touched this session (Jarvis production)**:
- `/Users/nathanielcannon/Claude/Jarvis/.claude/context/.active-plan` (Phase 7.9.5 status block updated; cost estimate updated; commit suggestions updated)
- `/Users/nathanielcannon/Claude/Jarvis/.claude/context/.scratchpad.md` (this entry)

**Files touched this session (Jarvis-Dev)**:
- `/Users/nathanielcannon/Claude/Jarvis-Dev/.claude/scripts/dev/jicm-v7-9-stage-1-harness.sh` (5 edits: USAGE comment, +3 helpers, T1/T2/T3 refactor)

---

### 2026-05-02 ~19:00Z (JICM-HALT): Stage-1 harness ran — 2 PASS / 3 FAIL — TEST DESIGN FLAW (not impl bug)

**Commits done before run**: Jarvis-Dev `67b9b9a` (16 files); Jarvis `7b725fe` (4 files). Insights still dirty in both (chore commit later).

**Run output**: `/Users/nathanielcannon/Claude/Jarvis-Dev/.claude/scratch/stage1-20260502-185625.log`
**Verdict**: `/Users/nathanielcannon/Claude/Jarvis-Dev/.claude/metrics/jicm/v7-9-stage-1-result-2026-05-02.md` → STAGE_1_HALT
**Per-test JSON**: `/Users/nathanielcannon/Claude/Jarvis-Dev/.claude/scratch/jicm-v7-9-test-runs/T?.json`

**Results**:
- T1 FAIL — state has `tokens=0, model_id=""` despite valid `transcript_path`
- T2 FAIL — same (action=WATCHING, not SOFT_NUDGE despite JICM_SOFT_TOKENS=1)
- T3 FAIL — gate_ok=0, stop_ok=0
- T4 PASS — watcher actuator works (signal-driven, manual injection)
- T5 PASS — defensive stdin handled

**ROOT CAUSE — TEST flaw, not IMPL flaw**: jicm-gate.sh JSONL parsing finds no `type:"assistant"` turns on the FIRST UPS of a fresh Claude session — there's no assistant message yet. So `last(.[] | select(.type=="assistant") | .message.usage)` returns null → `tokens=0, model_id=""`. The state file IS correctly written; the canonical formula simply has no data to parse on first prompt.

**FIX for harness**: T1-T3 each need a TWO-PROMPT sequence:
1. Send warmup prompt ("Hi"), wait for Claude to respond (poll JSONL until `select(.type=="assistant")` returns ≥ 1)
2. Send test prompt
3. Check state file AFTER the second UPS — which will have a valid assistant turn to parse

Rough helper to add to harness:
```bash
wait_for_assistant_turn() {
    local transcript="$1" timeout="${2:-30}" elapsed=0
    while [[ "$elapsed" -lt "$timeout" ]]; do
        if [[ -f "$transcript" ]] && \
           jq -e 'select(.type=="assistant")' "$transcript" >/dev/null 2>&1; then
            return 0
        fi
        sleep 1; elapsed=$((elapsed+1))
    done
    return 1
}
```

The `transcript_path` IS already in `.jicm-state-hook.json` from the first UPS, so harness can grab it from there for the polling.

**Alternative (simpler) — give harness more time**: send a warmup prompt, sleep ~25s for Claude to respond, then send test prompt. Skip the polling. But polling is more robust.

**T4 PASS confirms watcher mechanics are correct** — signal → idle wait → HALT inject all worked. The actuator chain doesn't depend on JSONL parsing; that's the gate hook's job. So the v7.9 implementation is sound; only the harness's first-prompt assumption needs revisiting.

**Phase 7.9.5 status**: not STAGE_1_CLEAR yet. Fix harness, re-run, revalidate. Then 7.9.6 (production deploy).

**Suggested next-session commit (Jarvis-Dev)**: `fix(jicm-harness): add warmup-prompt + assistant-turn polling so T1-T3 see populated JSONL`

---

### 2026-05-02 ~18:35Z (post-/clear): JICM v7.9 phases 7.9.2 / 7.9.3 / 7.9.4 SHIPPED + VALIDATED

**Correction first**: prior entry's "Phase 7.9.2 INTERRUPTED MID-WRITE / User REJECTED" claim was incorrect. The 4 backend-interface scripts already existed in Jarvis-Dev with 0755 perms (from the prior session before HALT). Only validation was outstanding.

**7.9.2 — backend interface validated**:
- `.claude/scripts/jicm-inject.sh` (1993B) — dispatcher, JICM_INJECTION_BACKEND env (default tmux)
- `.claude/scripts/jicm-inject-tmux.sh` (now ~2.6KB after capture-fix) — 4-entry contract, mirrors v7 watcher:307-322
- `.claude/scripts/jicm-inject-pty.sh` (1197B) — placeholder, exit 2 in v7.9
- `.claude/scripts/dev/test-inject-backend.sh` (3300B) — PTY: PASS (exit 2 confirmed); tmux: 4/4 PASS after fix
- **Bug fix this session in jicm-inject-tmux.sh**: capture-pane returns full pane buffer (~50 rows padded with blanks); `tail -n N` was landing in the dead zone. Fixed with `awk 'NF{last=NR} {a[NR]=$0} END{for(i=1;i<=last;i++) print a[i]}'` to strip trailing blanks before tail. Portable to bash 3.2 / BSD awk on macOS.

**7.9.3 — slim watcher** (heaviest of the three):
- `.claude/scripts/jicm-watcher.sh`: 1559→171 lines, 55321→6726 bytes (≤8KB acceptance ✓)
- 0 direct send-keys calls; 1 grep hit on "capture-pane" but it's the header comment, no code path
- Singleton guard (PID file + kill -0 + EXIT trap)
- All 11 cycle steps from roadmap §4.3 implemented (idle wait, HALT inject, ack wait, prep launch, prep wait, /clear inject, resume wait, RESUME inject, cleanup)
- Legacy preserved: `.claude/scripts/jicm-watcher-legacy.sh` (1559 lines / 55KB)
- `.claude/scripts/jicm-config.sh` NEW (4008B) — v7.9 signal protocol (JICM_STATE_HOOK_FILE, JICM_CLEAR_SIGNAL, JICM_RESUME_SIGNAL); token-primary thresholds (JICM_SOFT_TOKENS=300000, JICM_HARD_TOKENS=650000)

**7.9.4 — status line v7.9** (mostly done from prior v8 prototype, patched this session):
- `.claude/scripts/jarvis-statusline-v8.sh` patched: reads `soft_threshold_tokens` / `hard_threshold_tokens` (state file), derives pct for bar ticks (User encoding directive)
- Model-id formatter bug fixed: `*"[1m]"*` glob class issue → `*\[1m\]` literal match. Now "opus-4-7[1m]" → "opus-4-7·1M" not "opus-4-7[1m]·1M"
- 9 panels render correctly: WATCHING (green), SOFT_NUDGE (yellow), HARD_HALT (red)
- Graceful fallback (no state file): defaults to WATCHING + 30%/65% ticks
- settings.json statusLine block already pointed to script; no change

**7.9.5 — Stage-1 harness AUTHORED (T5 PASS this session; T1-T4 require User trigger)**:
- `.claude/scripts/dev/jicm-v7-9-stage-1-harness.sh` (executable, syntax OK)
- `.claude/metrics/jicm/v7-9-stage-1-result-template.md` (verdict format)
- `.claude/scratch/jicm-v7-9-test-runs/` directory created
- T5 (defensive stdin, free) PASS during spot-check
- T1-T4 each spawn `claude --dangerously-skip-permissions` in throwaway tmux window;
  total cost ~$0.20-0.40 — User must run manually (Claude-inside-Claude not appropriate)
- Bug caught + fixed during T5 spot-check: PROJECT_DIR resolution off by one `..`
  (was resolving to `.claude/` instead of workspace root). Fix in commit.
- Modes: `--list` (catalog), `--verdict-only` (re-render from prior runs), `T<n>` selection
- Verdict auto-rendered as STAGE_1_CLEAR / STAGE_1_HALT / STAGE_1_INCOMPLETE

**User invocation when ready**:
```
cd /Users/nathanielcannon/Claude/Jarvis-Dev
bash .claude/scripts/dev/jicm-v7-9-stage-1-harness.sh
```

**Phase 7.9.6 — gated on STAGE_1_CLEAR**: promote Jarvis-Dev v7.9 artifacts to
Jarvis production (hooks, scripts, settings.json), preserve current production
watcher as `jicm-watcher-legacy-v7-3.sh`, begin Stage-2 14d passive observation
against roadmap §4.6 acceptance criteria.

**Suggested Jarvis-Dev commit (covers 7.9.0-7.9.5)**: `feat(jicm): v7.9 hook layer + backend abstraction + slim watcher + status line + Stage-1 harness`

---

### 2026-05-02 ~22:35Z: Phase 0.5 BUNDLED EXECUTION — COMPLETE (post-HALT resume)

All 8 sub-steps shipped this session:
- (a) Pipeline-telemetry extractor (Jarvis): `.claude/skills/token-compression/scripts/pipeline-telemetry-extractor.py` — 19KB, 24-col CSV, engine-aware NULLs.
- (b) Reviewer Claude-CLI route (AIFred-Pro-Dev): `+127/-16` in `.claude/jobs/services/reviewer.py`, mirrors `executor.py:425-510`. Committed `af73a46` on `nate-dev`.
- (c) Phase 1.3.5 pre-reg: `pre-registration-phase-1-3-5-reviewer-claude-route.yaml`. New axis `cost_per_review_usd`. Stage-1 `2026-05-04T23:04:12Z`, Stage-2 `2026-05-16T23:04:12Z`.
- (d) Comprehensive David debrief: `Shared_Projects/Debriefs/AIFred-Pro/2026-05-02-pipeline-v2-and-token-compression-progress.md` (~3,200 words). Replace-or-evolve framing included.
- (e) Smoke-test passed: 35 rows from 7 dev-DB tasks, classes tool_only=7/brief=19/interactive=7/analysis=2.
- (f) `.active-plan` and this scratchpad updated.
- (g) Jarvis-side commit pending (extractor + pre-reg + active-plan + scratchpad + debrief).
- (h) AIFred-Pro-Dev side already committed at `af73a46` per ordering (deploy hash needed for pre-reg sealing).

Suggested Jarvis commit: `feat(token-compression): Phase 0.5 pipeline-telemetry extractor + Phase 1.3.5 pre-reg + David debrief`

**Next per User-approved 4-step sequence**: Step 2 — JICM v7.9 to completion (Stage-1 user validation in Jarvis-Dev; Stage-2 14d passive). Phase 3.2 Signal-notation folded into Phase 7.9.1.

**Final 24-col schema (decided)**: task_id, service, ts, closed_at, status, labels, engine, model, persona, prompt_tokens, completion_tokens, duration_ms, cache_read, cache_creation (aggregate, NOT split into 5m/1h — Claude CLI doesn't expose split), hit_rate, intent_class, tool_use_count, code_lines, prose_lines, markdown_table_rows, bullet_lines, text_block_count, register_violations, cost_usd. **Pipeline data has aggregate cache_creation only**, unlike chat-corpus extractor v2 which has eph_5m/eph_1h split.

**Pulse metadata key map** (verified against AION-7c80c1b5):
- `metadata.telemetry.{prompt_tokens, completion_tokens, duration_ms, log_lines, files_touched, ollama: {full ollama telem}}` — executor (ollama route)
- `metadata.telemetry.{cache_read_tokens, cache_creation_tokens, input_tokens, output_tokens, total_cost_usd, num_turns, model_usage, ...}` — executor (claude-cli route, executor.py:445-461)
- `metadata.review_telemetry.{prompt_tokens, completion_tokens, total_duration_ms, ...}` — reviewer (currently Ollama-only)
- `metadata.executor_engine` ∈ {`ollama`, `claude-cli`}
- `metadata.executor_log` (JSONL stream-json log path); `metadata.output_file` (Ollama route response text)
- Service `*_at` timestamps: `staged_at`, `evaluated_at`, `executed_at`, `reviewed_at`, `orchestrated_at`

**Text retrieval per service** (for intent_class + register):
- executor (ollama): read `metadata.output_file`
- executor (claude-cli): parse `metadata.executor_log` JSONL (assistant message events, content[].text)
- evaluate / stage / review: serialize `metadata.{evaluate,stage,review}_output` JSON
- orchestrator: text-poor; classify as `tool_only` always

**Pulse dev DB access**: `aifred-dev-postgres` container, NOT exposed on host port. Access via `docker exec aifred-dev-postgres psql -U pulse_dev -d pulse_dev`. Production Pulse on `jarvis-postgres` exposed on `localhost:5432` (user `pulse`, pw `MSvfm3BjI9BujVD01K2ctFOJ`).

**Real-data finding**: 8 closed tasks with telemetry in dev DB, ALL `executor_engine: ollama`. Zero Claude-CLI tasks. Per-User: artifact of recent Qwen3 testing; future test fixtures should mix routing (≥2 Claude-CLI + ≥1 Ollama per test suite).

**Reviewer Claude-route patch shape** (mirror executor.py:425-510):
- Add `_review_claude(task, model)` function — subprocess `claude -p --output-format stream-json` with REVIEW_PROMPT
- Routing decision at top of main(): `engine = metadata.get("review_engine", "ollama")` — default Ollama, opt-in Claude
- Capture cache_read_tokens, cache_creation_tokens, input_tokens, output_tokens from cli_data
- Write to `review_telemetry` with engine='claude-cli' marker; preserves backward compat for Ollama path

**Phase 1.3.5 pre-reg (to file)**:
- intervention_id: `phase-1-3-5-reviewer-claude-route`
- deploy_repo: `davidmoneil/AIFred-Pro` (nate-dev branch)
- baseline_lineage: [phase-1-1-jeeves-brief, phase-1-5-alfred-brief, phase-1-2-pipeline-executor-brief, phase-1-3-pipeline-reviewer-brief]
- Hypothesis: per-class brevity small effect (reviewer outputs already short JSON); cache_stability now **measurable** (was N/A pre-route); cost meaningful if widely deployed (default Ollama keeps zero by default)
- Stage-1 thresholds: trivial to clear (additive code; default routing unchanged → no existing behavior impact)
- Stage-2 axes: cache_hit_rate on Claude-routed reviewer; cost-per-task delta; per-class brevity

**David debrief scope** (User explicit): NOT small-change permission. Comprehensive project progress covering:
- Pipeline-redesign-v2 (E2E test passes, stress test 220 cycles, all 16 Observed-issues sections resolved, gospel-synopsis fixture, telemetry plumbing, activity timeline, dashboard live panels, dependency chain component, Project Creator streaming UI)
- Token-compression initiative (roadmap v3, multi-pass architecture, Jeeves+Alfred persona variants, Phases 0.2-0.4 complete, 1.1+1.5 deployed, 1.2+1.3 deployed)
- JICM v7.9→v8.0 vision (autonomy invariant, hardening + portability path)
- Two-stage gating pattern (universal Jarvis pattern)
- Project_Aion architecture (Hippocrenae AC-01-09, Aion Quartet, ProjectIntel collaboration, git topology migrated)
- Functional + philosophical improvements emphasis
- **Mention** upcoming side-by-side architectural diff vs current AIFred-Pro pulse+nexus+dashboard
- Frame David's eventual decision (replace or evolve)
- Path: `Shared_Projects/Debriefs/AIFred-Pro/2026-05-02-pipeline-v2-and-token-compression-progress.md`
- Use `Shared_Projects/Debriefs/_template.md` structure

**Resume directive (post-clear)**: continue Phase 0.5 bundled execution from step (a). All design decisions are recorded above; do not re-litigate. Just write the code + docs + commits.

**Suggested commit on completion** (Jarvis side): `feat(token-compression): Phase 0.5 pipeline-telemetry extractor + Phase 1.3.5 pre-reg`
**Suggested commit on completion** (AIFred-Pro-Dev side): `feat(reviewer): add Claude-CLI route mirroring executor pattern (token-compression Phase 1.3.5; default Ollama, opt-in Claude)`

---

### 2026-05-01 ~20:55Z: Post-JICM-RESUME continuation — synthesis doc landed, validation runbook written

**Project 1 update**: validation runbook written for User to execute the 3 Stage-1 tests on the Jarvis-Dev prototype:
- Path: `/Users/nathanielcannon/Claude/Jarvis/projects/project-aion/plans/jicm-v8-validation-runbook.md`
- 3 fresh-session tests: baseline state-write, soft-nudge (JICM_SOFT_PCT=1), hard-halt (JICM_HARD_PCT=2)
- Includes prereq check, env overrides, sticky-flag cleanup between tests, rollback procedure
- Stage-1 verdict decision table at §6; Stage-2 plan preview at §7

**Project 2 update**: synthesis doc was already written at `/Users/nathanielcannon/Claude/Jarvis/projects/project-aion/designs/local-model-suite-2026-q2.md` (30K, v1.0) — apparently authored during AFK work by an agent-led pass. It's high quality: 15-model recommended suite + bio add-ons, three findings worth front-loading (Qwen3.6-27B as everyday driver; DeepSeek-R1-0528-Qwen3-8B AIME 87.5%; CUDA-locked 2026 flagships excluded), Apple Silicon compatibility matrix, two-stage rollout plan, summary in five lines.

**Scope expansion noted, deferred to v1.1**: 5 additional category reports completed during AFK that aren't in v1.0:
- `.claude/scratch/models-tts-voice-2026-q2.md` (31K) — TTS gap-fill
- `.claude/scratch/models-robotics-vla-2026-q2.md` (36K) — new category
- `.claude/scratch/models-computer-use-deep-2026-q2.md` (32K) — extends vision §4.2
- `.claude/scratch/models-time-series-tabular-2026-q2.md` (42K) — new category
- `.claude/scratch/models-math-science-2026-q2.md` (42K) — extends language §4.1
Task #5 captures the v1.1 integration work for a future session.

**Agent rate limit**: hit ~14:50 MDT during attempted language-agent re-launch; resets 18:40 MDT. No agent calls possible until then.

**Today's burn**: $63 / 76M tokens through 14:50; opus-4-7 dominant (74%); cache-hit ratio strong (94%); active block projecting $50.92 with 3.5h remaining.

**Suggested commit when User approves**: `feat(jicm,models): v8 prototype validation runbook + ack v1.0 local-model-suite synthesis (5-category v1.1 deferred)`

---

### 2026-05-01 ~19:42Z: Post-JICM-RESUME — Project 1 prototype build COMPLETE; Project 2 agents launched

**Project 1 (JICM v8 portable rewrite) — Jarvis-Dev prototype now COMPLETE**:
- `Jarvis-Dev/.claude/hooks/jicm-gate.sh` (252 lines, 0755, UserPromptSubmit)
- `Jarvis-Dev/.claude/hooks/jicm-precompact.sh` (76 lines, 0755, PreCompact adjunct)
- `Jarvis-Dev/.claude/scripts/jarvis-statusline-v8.sh` (NEW this turn, 9KB, 0755) — single-line, reads `.jicm-state-hook.json`, 9 panels (model · stack-bar · % · burn · ETA · cache-hit · cost · 5h/7d · ACTION)
- `Jarvis-Dev/.claude/settings.json` (UPDATED): added top-level `statusLine` block; jicm-gate.sh inserted as FIRST hook of 10 in UserPromptSubmit (so `decision: "block"` short-circuits before downstream); jicm-precompact.sh appended as 3rd peer in PreCompact
- JSON validated via `jq -e`: statusLine present, gate.sh first in UPS chain, 10 UPS hooks total, 3 PreCompact hooks

**Validation pending**: launch a fresh Claude Code session in `/Users/nathanielcannon/Claude/Jarvis-Dev/`, type a prompt, observe `.claude/context/.jicm-state-hook.json` is written. Then export `JICM_SOFT_PCT=1` to force soft trigger and verify nudge text + sticky flag. Then `JICM_HARD_PCT=2` to verify hard block. (User to perform; I cannot launch a Claude session from inside Claude.)

**Suggested commit when User approves**:
`feat(jicm): v8 portable architecture (design doc + audit + Jarvis-Dev prototype)`
Files: design doc + audit doc + 4 Jarvis-Dev artifacts (hooks/jicm-gate.sh, hooks/jicm-precompact.sh, scripts/jarvis-statusline-v8.sh, settings.json).

**Project 2 (local model suite design doc) — 5 deep-research agents launched at 13:42 MDT (post rate-limit reset)**:
- Vision/multimodal/computer-control → `.claude/scratch/models-vision-2026-q2.md`
- Coding/tool-use/thinking/general-purpose → `.claude/scratch/models-language-2026-q2.md`
- Embeddings/reranking → `.claude/scratch/models-embeddings-2026-q2.md`
- Image/video generation → `.claude/scratch/models-image-gen-2026-q2.md`
- Genomics/molbio (User especially excited) → `.claude/scratch/models-genomics-2026-q2.md`

Each agent: writes full report to scratch, returns ≤300-word summary. Constraints: Apple Silicon, ≤100GB warm RAM, Q3 2025 - Q2 2026 release window, MLX preferred, Qwen family preferred but include best-in-class.

Already complete (pre-HALT): `.claude/scratch/models-audio-video-2026-q2.md` (audio/STT/TTS/video, 43KB).

**Synthesis target on agent completion**: `projects/project-aion/designs/local-model-suite-2026-q2.md`. Will merge across all 6 scratch files using HDS skill or direct synthesis.

---

### 2026-05-01 ~19:10Z: JICM-HALT received during AFK side-project work — RESUME ANCHOR (HISTORICAL — see entry above for current state)

**State at HALT**: User invoked JICM-HALT to test the JICM cycle itself with work mid-flight. The chmod +x for the new hook prototypes was rejected immediately before HALT.

**Two AFK side-projects in flight (User granted full autonomy, full creative license, full technical review/approval clearance):**

#### Project 1 — JICM portable rewrite + status line redesign
- **Q1=B**: full portable rewrite in Jarvis-Dev for review-then-promote
- **COMPLETE**:
  - Status line research: `.claude/scratch/status-line-reference.md` (verified token data IS in statusLine JSON stdin via `context_window` object — Claude Code source `src/components/StatusLine.tsx:90`, `src/tools/AgentTool/built-in/statuslineSetup.ts:54`)
  - Design doc: `projects/project-aion/designs/jicm-portable-architecture.md` (v8 architecture: 3 hooks + prep + restoration; no daemon, no tmux; soft 30% nudge → hard 65% block → native 70% compact fallback)
  - Audit doc: `.claude/context/designs/jicm-v7-audit-2026-05-01.md` (3 P0, 5 P1, 5 P2, 6 P3 issues; recommendation: full v8 rewrite)
  - Prototype hook 1: `Jarvis-Dev/.claude/hooks/jicm-gate.sh` (UserPromptSubmit; reads context_window from stdin; soft/hard threshold logic; writes `.jicm-state-hook.json`)
  - Prototype hook 2: `Jarvis-Dev/.claude/hooks/jicm-precompact.sh` (PreCompact adjunct; runs prep script before native compact)
- **REMAINING** (uncommitted, not chmodded):
  - `Jarvis-Dev/.claude/scripts/jarvis-statusline-v8.sh` — redesigned status line reading `.jicm-state-hook.json`
  - `Jarvis-Dev/.claude/settings.json` — register UserPromptSubmit hook for jicm-gate.sh; add jicm-precompact.sh to PreCompact
  - chmod +x on the two hook prototypes (rejected pre-HALT; needs approval)
  - Validation in Jarvis-Dev session
- **Two-stage gating per pattern**: design doc §5 plans Stage 1 (24-48h on Jarvis-Dev) + Stage 2 (7-14d) before promotion

**Critical findings recorded in design doc** (not lost on /clear):
- Hook stdin payload contains `context_window.{total_input_tokens, context_window_size, used_percentage, current_usage.{cache_read_input_tokens, cache_creation_input_tokens}}`, `cost.{total_cost_usd, total_duration_ms}`, `rate_limits.{five_hour, seven_day}.used_percentage`, `model.id`, `session_id`
- Hook output supports `hookSpecificOutput.additionalContext` (REQUIRED for UserPromptSubmit per Claude Code source line 434), `decision: "block"` + `systemMessage`, and SessionStart-only `initialUserMessage`
- `CLAUDE_CONTEXT_JSON` env var does NOT exist (the agent's earlier claim was wrong; verified via source)
- Hard threshold MUST be < native auto-compact threshold (else native pre-empts; recalibrated to 65% in design doc §6)

#### Project 2 — Local model suite design doc
- **Q2=A**: design doc only (no infrastructure deploys)
- **COMPLETE**:
  - Audio/STT/TTS/video models: `.claude/scratch/models-audio-video-2026-q2.md` (43KB)
- **REMAINING** (5 deep-research agents needed; rate limit hit at 12:03 Denver time, **resets 13:40 Denver**):
  - Vision/multimodal/computer-control models
  - Coding + tool-use + thinking + general-purpose models
  - Embeddings + reranking models
  - Image generation models
  - Genomics/bioinformatics/molbio models (User specifically excited about this)
- **Synthesis target**: `projects/project-aion/designs/local-model-suite-2026-q2.md` — to be written after agents complete
- **Constraint**: Apple Silicon, <100GB warm RAM ceiling, Q3 2025-Q2 2026 release window, MLX preferred, Qwen family preferred but open to best-in-class

**Agent rate-limit context**:
- Denver time reset: 13:40 (one already passed at ~12:03 if my read of the message was right; check `date "+%H:%M"` on resume — if past 13:40, agents are spawnable again)
- Failed agents to re-spawn: vision/multimodal, JICM audit (audit done manually so skip), genomics, coding/tool-use, embeddings/reranking, image-gen
- Working pattern: deep-research subagent type, batches of 4, each prompt self-contained with category + tier list + table format + write-to-scratch instruction

**On resume after /clear**: Per User's `/loop` self-pace pattern, this is an autonomous-execution mid-work HALT. Acknowledge restoration, refresh time, check agent rate-limit state, then continue Project 1 + 2 in parallel from the REMAINING items above.

**Suggested commit message when ready** (pending User approval):
`feat(jicm): v8 portable architecture (design + audit + Jarvis-Dev hooks prototype)`

---

### 2026-05-01 ~18:45Z: Two-Stage Gating pattern — SCOPE-SCALING REFINEMENT applied

User clarification: window durations scale with **scope of dev work**, not calendar.
- Automation-testable deterministic changes: Stage 1 in minutes, Stage 2 in hours.
- Workflow-bound human-in-loop interventions: Stage 1 in days, Stage 2 in weeks.
- The structural separation is what matters; the clock is set by the data.

**Pattern updates** (already applied to `.claude/context/patterns/two-stage-validation-gating.md`):
- §1 (Purpose): removed "hours to ~48h" / "days to ~14d" as fixed defaults; added scope-scaling principle as core framing
- §4 (Stage 1): replaced single-paragraph window guidance with 5-row Effort Class table (automation-testable / service-level / per-session / per-deploy / human-in-loop) + heuristic
- §5 (Stage 2): replaced single-paragraph window guidance with same 5-row Effort Class table; added "Stage 2 ≥ 5× Stage 1" rule so long window genuinely covers edge-cases
- §11 (Operational Templates): YAML pre-reg now requires `effort_class` field on Stage 1; uses ISO-8601 durations (`PT2H`, `P2D`) instead of `window_hours` / `window_days`; explicit `stage_2.window ≥ 5× stage_1.window` constraint

**Token-compression family interpretation** (canonical example):
- Phase 1.1 / 1.5 / 1.2 / 1.3 are all **per-session telemetry** class
- Stage 1 = 48h (correct per the table)
- Stage 2 = 14d (correct per the table; ratio = 7×)

**Other docs unaffected** by the refinement: capability-map, nous-map, _index, milestone-review-pattern, self-evolution-pattern (their cross-references describe the *structural separation*, which is unchanged).

---

### 2026-05-01 ~18:35Z: Two-Stage Validation Gating pattern — DOCUMENTATION COMPLETE

**Outcome of resume from JICM-HALT**: pattern generalized + wired into all discovery surfaces.

**Files created**:
- `.claude/context/patterns/two-stage-validation-gating.md` (v1.0.0, ~210 lines, 13 sections)

**Files updated**:
- `.claude/context/patterns/_index.md` — added entry under Testing & Validation; updated total 51→52
- `.claude/context/psyche/capability-map.yaml` — added `pattern.two-stage-gating` entry; updated date
- `.claude/context/psyche/nous-map.md` — added Validation category + notable-pattern callout; updated count
- `.claude/context/patterns/milestone-review-pattern.md` — composition section explaining how AC-03 + two-stage gate compose; bumped to v1.3.1
- `.claude/context/patterns/self-evolution-pattern.md` — core-principle step 6 references the gate
- `.claude/context/.active-plan` — recorded pattern work; corrected stale Phase 3 claim

**Files NOT modified (intentionally)**:
- AC component specs (AC-03, AC-06, AC-08): pattern docs reference them; reverse references not needed yet (the components are stable; pattern composition is documented from the pattern side).
- `psyche/_index.md`: no new traversal path needed — pattern is reachable via patterns/_index and capability-map. Will revisit if traversal patterns emerge.
- `prompts.yaml`, `jarvis-identity.md`: no prompt-template change required.
- token-compression-experimental-design.md §10.4: source remains canonical; new pattern explicitly cites it.

**Pattern key design choices** (recorded for future authors):
- **Recommended**, not Mandatory: requires *measurable* behavioral effect; trivial changes exempt
- Stage 1 verdict states: `STAGE_1_CLEAR` / `STAGE_1_HALT` / `STAGE_1_DEFERRED`
- Stage 2 verdict states: `STAGE_2_PASS` / `STAGE_2_PARTIAL` / `STAGE_2_FAIL` / `STAGE_2_REGRESSION_CATCH`
- Asymmetric authority: Stage 1 can HALT but never CLEAR-for-promotion
- Strict separation: Stage 1 axes MUST NOT overlap effect predictions (preserves Stage-2 pre-reg purity)
- Orthogonality test: 3-question check (same axis? dependent on merge? rollback invalidates?)
- Audit-trail requirement: parallel work must document orthogonality in its own pre-registration
- **Scope-scaling**: window durations track effort class, not calendar (5-row table in §4 and §5)

**Anti-patterns explicitly named** (§12 of new pattern):
- Including effect axes in Stage 1 (hindsight calibration)
- Promoting on Stage 1 clearance (bypass)
- Skipping pre-registration ("we'll write it after")
- Treating DEFERRED as CLEAR (hidden risk)
- Applying to no-behavior refactors (ceremony fatigue)
- Rolling back orthogonal work along with failed change (loses velocity benefit)

**Ready to commit** (User has not yet requested commit). Suggested message: `feat(patterns): two-stage-validation-gating pattern + cross-refs (generalized from token-compression §10.4; scope-scaled windows)`.

**Next step** (after commit, if approved): resume Phase 3 JICM compression work — discovery from this session shows 3.1+3.3 already shipped; only 3.2 (Signal notation) and 3.4 (validation across 5 cycles) remain.

---

### 2026-05-01 ~18:00Z: JICM-HALT — RESUME ANCHOR — generalize two-stage gate to universal Jarvis pattern

**User directive at HALT**: "the two stage gate/sign-off is even more impactful than I first realized. All Jarvis dev work should proceed under that paradigm: short-term data gathering for functional or performance regression -> rollback; long-term data-gathering for edge-case bugs and rigorous performance metrics. Document this as a Jarvis pattern, ensure patterns are properly mapped and documented in the psyche and other self-awareness systems of Jarvis."

**Scope of work after /clear**:
1. Generalize two-stage gating beyond token-compression. Currently lives in `projects/project-aion/reports/token-compression-experimental-design.md` §10.4. Promote to a first-class Jarvis pattern.
2. Create `.claude/context/patterns/two-stage-validation-gating.md`. Pattern strictness: **Recommended** (not Mandatory) — applies to dev work that introduces functional or performance change worth tracking. Stage 1 = short-term regression-catch (hours to ~48h depending on signal volume); Stage 2 = long-term edge-case + rigorous metrics (typically ~14d but tunable).
3. Add to `.claude/context/patterns/_index.md` under "Development & Git" or a new "Validation" category.
4. Map into Psyche: update `.claude/context/psyche/nous-map.md` (patterns inventory) and `.claude/context/psyche/_index.md` if pattern adds a new traversal path.
5. Cross-reference from existing milestone-review-pattern (AC-03) and self-improvement patterns — those already do staged review; the new pattern formalizes the regression-catch vs final-validation split that AC-03 doesn't make explicit.
6. Update `capability-map.yaml` patterns block with the new entry.
7. Consider whether autonomic component AC-XX (a new component? or under AC-03?) — likely under AC-03 / AC-08 reference rather than a new AC; the pattern is methodological, not a runtime loop.

**Pattern body draft notes**:
- Trigger: any dev change with measurable behavior (output, perf, register, stability)
- Stage 1 axes: regression-catch only (cache, error rate, register, latency), short window
- Stage 1 verdict states: `STAGE_1_CLEAR` / `STAGE_1_HALT` / `STAGE_1_DEFERRED`
- Stage 1 NOT a promotion gate; only unblocks orthogonal-stream parallel work
- Stage 2 axes: full pre-registered effect verdict, long window
- Stage 2 IS the formal promotion gate
- Rollback semantics: parallel orthogonal-stream work survives Stage-2 rollback (different streams = independent)
- Source reference: token-compression design doc §10.4 (canonical example)
- Mechanism rationale: token-compression-experimental-design.md §4.7 stacking rules (orthogonality determines what survives rollback)

**Phase 3 work-in-progress (paused for this generalization)**:
- DISCOVERY: Phase 3.1 (NLP preprocessing) and Phase 3.3 (compression metrics) ALREADY SHIPPED. `compress-input.py` is wired into `jicm-prep-context.sh:412-439`. `NLP_COMPRESS=true` default. Last cycle metric: ratio 1.0 (compression had no effect on the 4895-token Tier 1 — the input was already de-duplicated structured prose).
- REMAINING Phase 3 work: 3.2 Signal notation (NOT done; whether to do it depends on Phase 3.4 validation showing existing compression is insufficient), 3.4 validation across 5 cycles.
- Active-plan claim "starter at compress-input.py — not wired" is STALE. Update on resume.

**Commits this session (Project_Aion)**:
- `43adc5d` Phase 0.4 quote-aware register filter
- `5046897` Phase 1.5 pre-reg + AIFred-Pro-Dev baseline
- `824992c` chore insights
- `b95348c` two-stage gating §10.4 + Phase 1.2/1.3 pre-regs

All pushed to origin/main (CannonCoPilot/Jarvis) before HALT.

**Session-state.md still stale (731 mins)**: should be refreshed during resume — may want to defer until pattern work is complete to avoid double-edit.

---

### 2026-05-01 ~17:30Z: Phase 0.4 + Phase 1.5 setup + two-stage gating + Phase 1.2/1.3 pre-regs — ALL COMPLETE; starting Phase 3 next

**Phase 0.4** (commit `43adc5d`): quote-aware register filter shipped.

**Phase 1.5 setup** (commit `5046897`): pre-registration filed at zero post-deploy turns. AIFred-Pro-Dev baseline doc + register-markers override committed.

**Two-stage gating + Phase 1.2/1.3 pre-regs** (committing as γ next):
- Design doc §10.4: Stage 1 (deploy+48h, regression-catch only) + Stage 2 (deploy+14d, formal pre-reg sign-off). Stage 1 unblocks orthogonal-stream work per §4.7 stacking rules.
- Phase 1.1 + 1.5 pre-regs updated with `interim_check` blocks + reworded `gate_to_next_phase`.
- Phase 1.2 (executor) and Phase 1.3 (reviewer) pre-regs filed against deploy `f15f6a2` (post-hoc; first clean pipeline-corpus pre-reg comes after Phase 0.5 ships).
- Phase 0.5 (pipeline-telemetry extractor) added to schedule as READY backlog item — gates Phase 1.2/1.3 evaluation.
- Schedule rewritten: 9 items now organized around Stage-1 / Stage-2 / Phase-0.5 backlog.

**Next**: Phase 3 JICM compression. Plan:
1. Review starter `.claude/scripts/compress-input.py` (or wherever it lives).
2. Plan integration with `.claude/scripts/jicm-prep-context.sh`: NLP preprocessing (spaCy/NLTK) + Signal notation in machine-only sections.
3. Add compression metrics to `.jicm-state`: `original_tokens`, `compressed_tokens`, `ratio`, `time_ms`.
4. Validate quality across 5 resume cycles.
5. Milestone 3 gate: checkpoint ≤60% of current size; resume quality unchanged.

---

### 2026-05-01 ~15:00Z: Phase 1.1 rerun under new protocol — INCOMPLETE

**Result of running new experimental-design protocol on Phase 1.1 data**:
- Run report: `.claude/metrics/token-compression/phase-1-1-jeeves-brief-result-2026-05-01.md`
- Pre-registration: `.claude/metrics/token-compression/pre-registration-phase-1-1-jeeves-brief.yaml` (post-hoc, marked as such)
- Cache: PASS (-1.93pp). Register: PASS post-manual-review (2/99 raw, both false positives). Brevity: ALL PARTIAL — zero ordinary post-deploy sessions; all 3 sessions tagged atypical_analysis.
- Overall: **INCOMPLETE**. Hold. Do NOT promote to Phase 2.

**Protocol-level findings surfaced by this rerun**:
1. §7.3 ordinariness placeholders were significantly off (interactive ~17% → 27.01% actual, code_dump ~9% → 0.61% actual). Design doc updated with measured values.
2. Register classifier produces false positives on meta-mentions of register patterns inside double-quoted illustrative examples. Phase 0.4 backlog item: quote-aware register filter.
3. Heavy-prose sessions cluster (3 in a row); ordinary-session collection is workload-dependent.

**Phase 0.3 status**: COMPLETE (extractor v2 + 4 templates committed at 2de70c2 / pushed earlier).
**Phase 0.4 status**: BACKLOG (quote-aware filter for extractor v2).

**Next eligible runs under new protocol**:
- Phase 1.5 (Alfred-Brief on AIFred-Pro-Dev nate-dev) — *first clean pre-registered run* would be here, since Phase 1.1 protocol-rerun is post-hoc
- Phase 1.1 final sign-off when 3+ ordinary post-deploy sessions accumulate

---

### 2026-05-01 ~05:15Z: Phase 1.4 — PROVISIONAL PASS (post-JICM resume)

**Report**: `.claude/metrics/token-compression/phase-1-4-comparison-2026-04-30.md`
**Cutoff**: `2026-05-01T03:27:28Z` (commit `75c9d97`).

**§3.3 results**:
- Hit rate: 89.74% post-deploy vs 93.59% baseline → -3.85pp, **within ±5pp band** ✅
- Register spot-check: **0 / 91 violations** across both post-deploy sessions ✅ (over-delivers vs 5 samples required)
- Median output_tokens reduction: **INCONCLUSIVE** — population mismatch. Both post-deploy sessions are atypical (migration, telemetry analysis). At ≥1000 threshold, post is +72.6% vs pre, but those are commit-summary turns not ordinary prose. Pre-deploy median 25 is dominated by 56% of turns being 1-49 tokens (tool-call-only).

**eph_1h adoption**: 84.6% baseline → 100% post-deploy (improvement).

**Decision**:
- Do NOT roll back Jeeves-Brief.
- Do NOT advance to Phase 2 (CoD validation) yet.
- **Gate to Phase 2**: collect 3 ordinary post-deploy sessions (single questions, small tasks, reactive debugging — NOT migration / commit-organization / telemetry analysis), rerun extractor + partition analysis with substantive-turn filter (output ≥ 100 minimum, intent-class tagging preferred).

**Files modified this resume**:
- `.claude/metrics/token-compression/phase-1-4-comparison-2026-04-30.md` (NEW, full report with criteria table, sample composition, methodological notes for rerun)
- `.claude/context/.active-plan` (UPDATED — Phase 1.4 provisional-pass; gate to Phase 2 documented; Phase 0.3 extractor v2 queued)

**Methodological fix queued (Phase 0.3)**: extractor v2 should tag turns by intent class (status/answer/code/analysis) for matched-population comparison. Current `output_tokens` is bimodal because tool-call-only turns inflate the corpus; brevity-per-prose-turn is what the directive actually shapes.

**Other token-compression Phase status (unchanged)**: Phase 0/1 deployed and pushed. Phase 2 CoD: BLOCKED. Phase 3 JICM: `compress-input.py` starter not wired. Phase 4 pipeline COMPRESSION_MODE: not started. Phase 5 dashboard: scaffolded in AIFred-Pro-Dev `8ee3f37`, router not wired.

**Pre-JICM commits pushed**:
- Jarvis: `4e9fba7` on `origin/main` (CannonCoPilot/Jarvis)
- AIFred-Pro-Dev: `a46806a` on `origin/nate-dev` (davidmoneil/AIFred-Pro)

**launchd jobs active**: `com.aion.david-nexus-sync-fetch` (6h recurring), `com.aion.david-nexus-sync-health-check` (one-shot 2026-05-14 09:00 MDT, self-cleans), `com.aion.token-compression-reminder` (one-shot 2026-05-03 09:00 MDT).

---

### 2026-04-30 22:35: Jarvis git topology migration — COMPLETE (Stages A-E)

- `origin` → `git@github.com:CannonCoPilot/Jarvis.git` (PAT removed from `.git/config`)
- `origin/main` HEAD: **d63421b** — Project_Aion local tracks origin/main; `push.default=upstream` set
- Jarvis-Dev origin also re-pointed to SSH (security parity)
- launchd `com.aion.david-nexus-sync-fetch` loaded (6h interval, writes `Shared_Projects/Status/david/nexus-sync-2026-04-recent.md`)
- `/sync-aifred-pro-dev` slash command available (Layer 2 deep review)
- davidmoneil/AIfred:Project_Aion (7d0e9f5) left as fossil — not deleted, not pushed to again
- Plan file kept for record: `projects/project-aion/plans/jarvis-git-topology-migration-2026-04-30.md`

**Token-compression Phase 1 (parallel workstream)**:
- Phase 1.5/1.2/1.3 on davidmoneil/AIFred-Pro:nate-dev (commits c31b2bd, f15f6a2)
- Phase 1.1 on Jarvis Project_Aion at 75c9d97 (now part of pushed CannonCoPilot/Jarvis:main history)
- Baseline: 93.6% global hit rate, 84.6% eph_1h adoption, 177 sessions / 31,027 turns
- Remote follow-up routine `trig_01EtBi9X7q42owtUCWzmSgLH` fires 2026-05-04T03:00:00Z
- Local launchd reminder `com.aion.token-compression-reminder` fires 2026-05-03 09:00 MDT
- Manual-capture runbook: `.claude/metrics/token-compression/manual-capture-task.md`


### 2026-04-30: Observed-Issues.txt — COMPLETE. Project Creator — IN PROGRESS (Session 57)
- Observed-issues.txt: ALL 16 sections complete
- **Project Creator streaming UI**: Backend SSE endpoint WRITTEN (`project-creator.ts` `/api/project-creator/generate-stream`), frontend rewritten with streaming panel + sessionStorage persistence (`ProjectCreatorPage.tsx`). NOT YET BUILT/DEPLOYED. Need: `docker build + run` of dashboard.
- **PULSE_API_URL fix**: Must include `/api/v1` suffix: `PULSE_API_URL=http://aifred-dev-pulse:8700/api/v1`
- **VAPID keys** (dashboard container): `VAPID_SUBJECT=mailto:nathaniel.cannon@gmail.com`, `VAPID_PUBLIC_KEY=BGsoJ4Wrz9bo...`, `VAPID_PRIVATE_KEY=eEtMYB__bNi9...`
- **Dashboard deploy command**: `cd /Users/nathanielcannon/Claude/AIFred-Pro-Dev/dashboard && docker build -t aifred-dashboard:dev . && docker stop aifred-dev-dashboard && docker rm aifred-dev-dashboard && docker run -d --name aifred-dev-dashboard --network aifred-dev-network -p 8701:8600 -e PULSE_API_URL=http://aifred-dev-pulse:8700/api/v1 -e OLLAMA_URL=http://host.docker.internal:11434 -e EXECUTION_LOGS_DIR=/logs/executions -e VAPID_SUBJECT=mailto:nathaniel.cannon@gmail.com -e VAPID_PUBLIC_KEY=BGsoJ4Wrz9boMYrVv-ZqJiH2pvUgHHnMw7U3ZkH4E_6N1bM4eo0qF8ZPCfHLgR6TFrxV3BqM5rCp2uHjS2IWcRA -e VAPID_PRIVATE_KEY=eEtMYB__bNi9sqZ0hq5yWd6LcGZfcLy9TXyCAaVJMB0 -e NODE_ENV=production -v /Users/nathanielcannon/Claude/AIFred-Pro-Dev/.claude/logs/headless/executions:/logs/executions:ro --restart unless-stopped aifred-dashboard:dev`

### 2026-04-30: Commits pushed to nate-dev this session
- `77145a9`: Pipeline fixes (telemetry, filesystem verification, path validation, archival, blocker badge)
- `c0a0319`: Ollama executor routing + telemetry capture for all services
- `5569e6c`: Claude CLI structured telemetry via --output-format json
- `a1b648a`: Project Creator — chat UI for local LLM ticket generation
- `029570c`: Fix text visibility (text-primary) + default to local LLM
- **UNCOMMITTED**: Streaming SSE endpoint + frontend rewrite (project-creator.ts, ProjectCreatorPage.tsx). Need build+deploy+commit.

### 2026-04-30: Key Agent Findings (from Section 4-5 audit) — ALL FIXED
- **P0 FIXED**: Decomposed parent guard at pipeline-watcher.py:339-352
- **P1 FIXED**: Orchestrator chain_id preservation already in orchestrate.py:106-132 (group_tasks separates pre-chained from unchained)
- **P1 FIXED**: Reviewer now does filesystem verification (reviewer.py:_verify_filesystem)
- **P2 FIXED**: Stage now validates output paths against PROJECT_DIR (stage.py:119-128)
- **Note**: Two pipeline codebases coexist — old (event-watcher-v2.py, evaluator.py, orchestrator.py, stager.py) and new (pipeline-watcher.py, evaluate.py, orchestrate.py, stage.py). Old event-watcher-v2.py is deleted from working tree.

### 2026-04-30: GitHub Actions — All Pre-existing
- 13/378 failures on nate-dev AND main branch (since March 29)
- Causes: opencode.json JSONC comments, shellcheck in David's scripts, hardcoded /home/davidmoneil paths
- NOT from our nate-dev changes

### 2026-04-28: Dev DB creds + ports
- Dev DB: pulse_dev / JzmggkPyb8f3NiOy7Z51lV5PDcP15NZS @ aifred-dev-postgres
- Pulse dev: 8800; production: 8700; proxy: 9800
- Dashboard dev: 8701 (aifred-dev-network, proxies to production 8700)
- Test runner: `/Users/nathanielcannon/Claude/Jarvis/infrastructure/.venv/bin/python -m pytest .claude/jobs/tests/test_pipeline_v2.py`

---

### 2026-05-03 ~00:25Z: JICM v7.9 implementation IN-FLIGHT (HALT received mid-Phase-7.9.2)

**Step 2 of User's 4-step sequence**. Started this session post-resume.

**Phase 7.9.0 — COMPLETE**:
- Baseline doc: `/Users/nathanielcannon/Claude/Jarvis/.claude/metrics/jicm/v7-9-baseline-2026-05-02.md` (271 lines, 12,690 bytes)
- 40-function watcher audit complete (KEEP/RETIRE/REWRITE classified)
- **P0 finding**: UPS hook stdin LACKS `context_window` (verified empirically + via official docs). Roadmap §4.1 cited statusLine source by mistake.
- **CORRECTION** (approved by User): JSONL transcript parsing as canonical sensing source.
- Roadmap doc updated: `projects/project-aion/designs/jicm-roadmap-v7-9-to-v8.md` §4.1 (CORRECTION 2026-05-02 footnote)
- Plan doc updated: `projects/project-aion/plans/jicm-implementation-plan-v7-9-to-v8.md` §7.9.1 task #2 (JSONL parsing instructions)

**CANONICAL FORMULA**:
```
current_context_tokens = usage.input_tokens
                       + usage.cache_read_input_tokens
                       + usage.cache_creation_input_tokens
```
Verified within ~3.6% of v7 capture-pane (220,665 vs 228,637); JSONL is conservative lower bound (past-tense; TUI is future-tense). Δ = unsubmitted prompt + system reminders. Approved.

**ENCODING DIRECTIVE** (User, 2026-05-02): token counts strictly > percentages. Thresholds in tokens (`JICM_HARD_TOKENS=650000`, NOT pct). State-file primary fields are token integers; `used_percentage` derived for display only.

**Phase 7.9.1 — COMPLETE & VALIDATED**:
All in `/Users/nathanielcannon/Claude/Jarvis-Dev/`:
- `.claude/scripts/jicm-state-update.sh` (72 lines, 2,338 bytes) — atomic write helper (`--write` from stdin OR `--clear-pending`); POSIX rename atomicity
- `.claude/hooks/jicm-gate.sh` (198 lines, 9,330 bytes) — UPS hook, JSONL parsing, state write only (no actuation; no decision:block)
- `.claude/hooks/jicm-stop.sh` (95 lines, 4,388 bytes) — Stop hook, reads pending_action, writes `.jicm-clear-now.signal`, `--clear-pending` atomically; recursion guard via `stop_hook_active`
- `.claude/hooks/jicm-precompact.sh` — existing prototype satisfies spec, NO CHANGES NEEDED
- `.claude/hooks/session-start.sh` — patched (line ~431) to write `.jicm-resume-complete.signal` after JICM v7 clear branch jq output succeeds
- `.claude/settings.json` Stop chain — `jicm-stop.sh` registered FIRST (5 entries total; jq-validated)

**BUG FIXED in jicm-gate.sh during testing**: opus-4-7 model id has no `[1m]` suffix in JSONL `message.model` field. Original case had `*opus-4-7*) WINDOW=200000` which produced 144% used_percentage at 290K tokens. Fixed: `*opus-4-7*) WINDOW=1000000` (Jarvis exclusively runs 1M variant).

**Validation tests PASS** (this session):
- T1: jicm-gate.sh on real Jarvis JSONL → tokens=292,996, window=1M, pct=29% (sane)
- T2: jicm-stop.sh with pending=HALT_AFTER_RESPONSE → wrote signal `{"threshold_type":"HARD_HALT","tokens":660000,"threshold_tokens":650000,...}`; pending_action cleared atomically
- T3: stop_hook_active=true → recursion guard skipped correctly

**Phase 7.9.2 — INTERRUPTED MID-WRITE (this HALT)**:
User REJECTED the bash command writing 4 scripts (jicm-inject.sh, jicm-inject-tmux.sh, jicm-inject-pty.sh, .claude/scripts/dev/test-inject-backend.sh). On resume: replay the write of these 4 scripts. The contract per roadmap §4.5 is the 4-entry interface (escape/text/submit/capture); tmux backend mirrors current jicm-watcher.sh:307-322; pty backend exits 2 (not implemented in v7.9). Test harness creates throwaway `jarvis:jicm-test-inject-$$` window.

**Remaining v7.9 phases** (post-resume):
- 7.9.2: backend interface (interrupted; replay the 4-script write)
- 7.9.3: slim watcher rewrite (1612 lines → ≤200; signal-driven actuator) — heavy
- 7.9.4: status line v7.9 spec (port `jarvis-statusline-v8.sh` to read new state schema, token-primary)
- 7.9.5: Stage-1 autonomous harness (`.claude/scripts/dev/jicm-v7-9-stage-1-harness.sh`; spawns 5 throwaway tmux+claude sessions to test T1-T5) — heavy
- 7.9.6: production deploy + Stage-2 14d passive observation start

**ccusage open followup** (from baseline doc §8): `.ccusage-blocks.json` is structured as object (not array); `.[0]` query fails. Pursue separately to understand schema and decide if Jarvis can leverage for cost/billing display in `.jicm-state-hook.json` (currently those fields are null). Not blocking v7.9.

**Stage-1 harness verdict format** (per plan §7.9.5): `.claude/metrics/jicm/v7-9-stage-1-result-YYYY-MM-DD.md` with 5 test verdicts (T1 baseline state-write / T2 SOFT nudge / T3 HARD halt full cycle / T4 hard halt via ENV / T5 invalid stdin defensive). 5/5 PASS = STAGE_1_CLEAR; advance to 7.9.6 deploy.

**Suggested commit when resumed + phases 7.9.2-7.9.5 done**:
`feat(jicm): v7.9 hook layer + backend abstraction + slim watcher + Stage-1 harness (Jarvis-Dev)`

**Pulse task to file** (followup): "Investigate .ccusage-blocks.json schema for cost/rate-limit population in .jicm-state-hook.json" — agent:jarvis, project:jarvis-dev, low priority.

