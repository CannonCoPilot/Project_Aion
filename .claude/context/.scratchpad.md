# Scratchpad — Short-Term Working Memory

**Purpose**: Transient details needed within this session or the next 1-2 sessions. Force-loaded via CLAUDE.md. Survives /clear and JICM compression.

**Rules**:
- Write here for: credentials locations, active paths, custom functions, transient gotchas, session-specific commands
- Do NOT write here for stable facts (use MEMORY.md) or work progress (use session-state.md)
- Entries should have dates; prune when stale (AC-08 maintenance + scratchpad-rotate hook)
- **Keep under 80 lines (~2K tokens) to limit force-loaded token cost.** Older entries auto-rotate to `archive/scratchpad-YYYY-MM-DD.md`.

---

## Active Notes

_(Older entries auto-rotated to /Users/nathanielcannon/Claude/Jarvis/.claude/context/archive/scratchpad-2026-05-16.md on 2026-05-16T22:31:56Z.)_

### 2026-05-16 LATE (Phase 2B FULLY IMPLEMENTED + Dashboard LIVE — pending commit)

**Phase 2B Memory System Architecture — FULLY IMPLEMENTED** in single session. All 12 tasks delivered:

**2B-α Autonomic Pipeline (7 tasks)**:
- Task 1: `jicm-auto-ingest.py` — L3→L4 consolidation (checkpoint→RAG). Dedup dial at 0.92. Validated: 19 chunks ingested, 3.2s.
- Task 2: `relevance-retrieval.js` — L5→L2 autonomic recall (UPS hook). 12 pattern triggers, session dedup. Validated.
- Task 3: Scratchpad rotation wired to JICM cycle (SessionStart source=clear). 752→54 lines.
- Task 4: NLP compression disabled (0.99 ratio = no-op on structured output).
- Task 5: `memory-consolidation.sh` — insights-log rotation (1118→200 entries) + corrections→RAG. 918 entries archived + ingested.
- Task 9: Design doc at `projects/project-aion/designs/current/memory-layer-architecture-2026-05-16.md`.
- Task 10: tmux scrollback capture (watcher step 5.6) + injection via SessionStart additionalContext.

**2B-β Dashboard + Meta-Memory (5 tasks)**:
- Task 6: JarvisMemoryPage.tsx on Alfred-Dev `/jarvis-memory` route + server endpoints.
- Task 7: HUD already functional (statusline v9 + watcher refresh confirmed).
- Task 8: pre-compact.sh + precompact-analyzer.js archived, stale state files removed.
- Task 11: `log-rotation.sh` — 421MB→30MB logs. Retention: 5MB diag, 2MB JSONL, 7d archives.
- Task 12: `context-health-monitor.js` rewritten — surveys all 6 layers, writes `.memory-health.json`.

**Config additions** (jicm-config.sh): `JICM_RAG_ENABLED`, `JICM_RAG_DEDUP_THRESHOLD=0.92`, `JICM_RAG_COLLECTION`, `JICM_GRAPHITI_ENABLED=false`.

**Files touched (Jarvis)**: jicm-config.sh, jicm-watcher.sh, jicm-prep-context.sh, session-start.sh, settings.json, context-health-monitor.js + 4 new scripts (auto-ingest.py, relevance-retrieval.js, memory-consolidation.sh, log-rotation.sh) + design doc.
**Files touched (Alfred-Dev)**: App.tsx, server/index.ts + 2 new files (JarvisMemoryPage.tsx, jarvis-memory.ts).

**Dashboard LIVE at `http://localhost:8702/jarvis-memory`** (vite proxy) or `http://localhost:8701/jarvis-memory` (direct). Polls every 2s. All 6 memory layers, file fullness gauges, connection health (Qdrant + MLX), force-loaded token budget, archives, JICM cycles, watcher log tail.

**Docker compose note**: must use explicit `-f` flags for nexus-dashboard to pick up Jarvis mount:
```bash
cd ~/Claude/Alfred-Dev && docker compose -f docker-compose.yml -f docker-compose.dev.yml -p aifred-pro-dev up -d --no-deps nexus-dashboard
```

**Key metrics confirmed live**:
- Force-loaded: 85KB ≈ 21,819 tokens (2.2% of 1M window)
- Qdrant: UP 5ms, 14 collections
- MLX Embed: UP 2ms, Qwen3-Embedding-4B
- Context: 269K (26%), SOFT_NUDGE action
- Scratchpad: 82/120 (68%), Insights: 201/200 (100%)

**Resume protocol**: 
1. Commit Jarvis Phase 2B on `Project_Aion`, push to CannonCoPilot/Jarvis:main
2. Commit Alfred-Dev dashboard on `feature/personas-rebuild`, push to CannonCoPilot/Alfred
3. Sir approved full autonomous execution — all 12 tasks complete + dashboard enrichment done
4. Next: Sir may want visual refinements or to review the live page in browser

### 2026-05-06 — Reviewer Dash (ARCHIVED — see session-7 RAG + PR#3 notes)
Reviewer Dash R1+R2 shipped but misframed. Options 1/2/3 surfaced for Sir. PR#3 opened as 110-commit batch. Full context in RAG `sessions` collection.

### Reference paths (stable across sessions)

- Jarvis credentials: `.claude/secrets/credentials.yaml`
- Pulse API: `localhost:8700` (prod), `localhost:8800` (dev)
- Reverse proxy: `localhost:9800`
- Live watcher PID file: `.claude/context/.jicm-watcher.pid`
- Recent scratchpad archive: `.claude/context/archive/scratchpad-2026-05-07.md` (339-line snapshot pre-meditation prune)
