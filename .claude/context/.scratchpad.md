# Scratchpad — Short-Term Working Memory

**Purpose**: Transient details needed within this session or the next 1-2 sessions. Force-loaded via CLAUDE.md. Survives /clear and JICM compression.

**Rules**:
- Write here for: credentials locations, active paths, custom functions, transient gotchas, session-specific commands
- Do NOT write here for stable facts (use MEMORY.md) or work progress (use session-state.md)
- Entries should have dates; prune when stale (AC-08 maintenance + scratchpad-rotate hook)
- **Keep under 80 lines (~2K tokens) to limit force-loaded token cost.** Older entries auto-rotate to `archive/scratchpad-YYYY-MM-DD.md`.

---

## Active Notes

_(Older entries auto-rotated to /Users/nathanielcannon/Claude/Jarvis/.claude/context/archive/scratchpad-2026-05-22.md on 2026-05-23T05:49:08Z.)_

### 2026-05-23 — api_aware.md committed (008e413)

Force-loaded doc covering three-tier metric hierarchy (burn weight > tokens > $), no-conversion rule, header glossary, surface inventory + traps, and rules for headless `claude -p` work. "Burn weight" = Δ `unified_5h_utilization` (pp) is Sir-coined; used throughout. Companion memory `feedback_empirical_grounding_for_claims.md` still pending Sir's nod.

### 2026-05-23 ~09:46Z — E/F v6 COMPLETE, synthesis written

**Outcome**: 31 cells, $5.44 actual (projection $10.35 was 1.9× over), pre→post util 28%→40% (Δ 12pp). Post-run + synthesis-writing util now 47%.

**Pass-rate matrix (post fold-in from v5)**:
```
         A1   A2   A3   A4   A5   P1   C1
M-D    | ✓†   ✓†   ✓    ✓    ✓    ✗    ✓
M-S    | ✓†   ✓†   ✓    ✓    ✓    ✗    ✓
M-SF   | ✓    ✓    ✓    ✓    ✓    ✗    ✓
M-A    | ✓†   ✓†   ✓    ✓    ✓    ✗    ✓
M-B    | ✗‡   ✗‡   ✗‡   ✗‡   ✗‡   ✗‡   ✗‡
```
† = v5 T-N fold-in (all PASS) · ‡ = `--bare` auth failure ("Not logged in")

**Key findings** (full report: `projects/project-aion/reports/cache-mechanics-v5-strip-effect-v6-findings.md`):
1. `--bare` strips auth, not just content — UNUSABLE as content-strip control
2. `--append-system-prompt` is a no-op for cache (cw identical to M-D within 0.1%)
3. **MCP invocation works in M-S/M-SF** — system-prompt strip does NOT block MCPs
4. P1 (plugin awareness) uniformly FAIL including M-D control — plugins not surfaced to model in `claude -p` children
5. Strip effect quantified: M-S/M-SF write ~44K cw vs M-D's ~70K (27K of Anthropic-side content removed, project @-imports survive)
6. Per-cell cost savings for M-S vs M-D: ~32%

**api_aware.md proposed updates** (§7 of findings doc): rule #5 quantified delta, rule #6 plugin/MCP clarification, NEW rule #7 on `--bare`.

**Pre-flight projection model** (script §9): need topology-aware EXPECTED_COST split (TR_FIRST / TR_LATER / TN) to avoid 2× overestimate aborts.

**Awaiting Sir's return** for:
- Review of findings doc + api_aware.md edits
- Commit of: api_aware.md (008e413), CLAUDE.md edit, v6 script, v6 findings doc, scratchpad updates
- Decision on follow-up probes (P1 active-use test, hooks propagation, --bare deconfusion)

### Reference paths (stable across sessions)

- Jarvis credentials: `.claude/secrets/credentials.yaml`
- Pulse API: `localhost:8700` (prod), `localhost:8800` (dev)
- Reverse proxy: `localhost:9800`
- Live watcher PID file: `.claude/context/.jicm-watcher.pid`
