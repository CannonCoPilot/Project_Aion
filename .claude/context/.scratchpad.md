# Scratchpad — Short-Term Working Memory

**Purpose**: Transient details needed within this session or the next 1-2 sessions. Force-loaded via CLAUDE.md. Survives /clear and JICM compression.

**Rules**:
- Write here for: credentials locations, active paths, custom functions, transient gotchas, session-specific commands
- Do NOT write here for stable facts (use MEMORY.md) or work progress (use session-state.md)
- Entries should have dates; prune when stale (AC-08 maintenance + scratchpad-rotate hook)
- **Keep under 80 lines (~2K tokens) to limit force-loaded token cost.** Older entries auto-rotate to `archive/scratchpad-YYYY-MM-DD.md`.

---

## Active Notes

_(Older entries auto-rotated to /Users/nathanielcannon/Claude/Jarvis/.claude/context/archive/scratchpad-2026-05-16.md on 2026-05-16T22:31:56Z.)_

### 2026-05-18 (Phase 2C — 7/7 DONE, all phases implemented)

**Phase 2C Memory System Wiring v2.1** — ALL 7 phases implemented.

**Phases I-V (committed 9fe5bfa, pushed)**:
- I: Graphiti ENABLED + 34/34 corpus ingested (3 re-ingested this session)
- II: Watcher steps 5.6b-5.9 (NLP scrollback, RAG ingest, consolidation, Graphiti episode)
- III: NLP compression repositioned (raw→compressed→structured)
- V: BOOT mandatory MCP retrieval instructions

**Phases IV, VI, VII (implemented this session, pending commit)**:
- IV: REST stage — watcher idle detection (30min no prompt OR 50 tools) → R1-R5 functions. VALIDATED in-vivo: R1+R2+R5 fired, marker written.
- VI: T4 MCP search injection in relevance-retrieval.js — 5 signal types (domain_shift, recall, methodology, error_debug, reference_lookup). Session dedup + 1 per prompt cap.
- VII: MAINTAIN health pings every 100 polls — M2 (Qdrant/MLX/Neo4j), M3 (collection size), M4 (identity file change detection).

**New file**: `.claude/scripts/graphiti-auto-ingest.py` — dedicated L3→L5 episode ingestion.
**Watcher**: PID 13001 (restarted, running new code with all phases).

**Design doc**: `projects/project-aion/designs/current/memory-system-wiring-v2-2026-05-16.md` (v2.1 FINAL)
**Key principle**: Store operations bypass Claude (direct API); Retrieve operations MUST pass through Claude (Context Window)

### Reference paths (stable across sessions)

- Jarvis credentials: `.claude/secrets/credentials.yaml`
- Pulse API: `localhost:8700` (prod), `localhost:8800` (dev)
- Reverse proxy: `localhost:9800`
- Live watcher PID file: `.claude/context/.jicm-watcher.pid`
