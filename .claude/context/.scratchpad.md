# Jarvis Scratchpad (L1 session) — REWRITTEN 2026-08-01 after the round-template session.

## ⚠ FIRST ACTION ON RESUME: READ THESE TWO, IN THIS ORDER
1. `Projects/palimpsest/core/.scratch/originaldr-project/ocr-spike/CHAPTER-WORKFLOW.md` — the METHOD.
   **Read § THE ROUND TEMPLATE first**, then § 6b (what the loop is blind to). Phases 0-7 are for ONE cold
   chapter, not the default loop.
2. `ocr-spike/CAMPAIGN-STATUS.md` — the live STATE, worst-first queue, pinned negatives, runner discipline.
Authoritative detail: `SPRINT-STATUS.md` (M13-M39) · `REOCR-MASTER-PLAN-2026-07-22.md` §13 (Q1-Q49).
**Do not re-derive the approach** — a campaign's worth of findings is in them.

## ACTIVE: OriginalDR re-OCR — GENESIS CAMPAIGN
Repo `Projects/palimpsest` (main; **HOLD PUSH for Sir OK**; committing IS approved — **49 unpushed**).
Workdir `<repo>/core/.scratch/originaldr-project/ocr-spike`. Interpreter `../ocr-venv/bin/python`
(the other venv cannot import kraken). MLX needs `PYTORCH_ENABLE_MPS_FALLBACK=1`. Tests **216 passed**.

### STATE (2026-08-01, both passes FINISHED — the board is stable)
| | |
|---|---|
| cells >=0.90 / ACHIEVABLE | **5,245 / 6,116 = 0.8576** |
| chapters by band | <0.70: **0** · 0.70-0.80: 5 · 0.80-0.90: 32 · 0.90-0.95: 10 · >=0.95: **3** |
| CHAPTERS CLOSED | **2** — 1 and 16 (SENTINELS: re-measure on every change) |
| blocked by an absent reference | **4 cells / 1 chapter** (ch23, `odr_com` gen 23:20 — ACQUISITION) |

**Count `n_pass`, NEVER `n_cells - n_open`** — BLOCKED cells are in neither, so subtracting credits them as
passing. It cost a wrong headline this session, caught by `build_reocr_report.py` disagreeing.

### ⚠⚠ THE GOVERNING LESSON (§13 Q47)
> **A rule is measured by the TEXT IT CHANGES, not by the verdicts it flips.**
`split_glued` scored **net +8 cells** and alters **1,356 tokens**, tearing real words into morphemes
(`lawful`->`law ful` 28x). **Run `faithfulness_audit.py` before adopting anything that edits text — including
your own fixes.** A note-anchor class written `[A-ZſVI]` matched `a ſonne` (ſ is LOWERCASE) and deleted a page
of scripture, in a commit whose scoreboard showed a GAIN.

### SIR'S STANDING DIRECTIVE
Every verse x 4 sources x **each** of 4 references at **>=0.90**, ſ-surface CLOSED. Re-measure all 50 after
each round. Book/source-specific tuning is the design principle — **OVERFITTING IS EXPLICITLY GOOD**. A
below-bar cell stays OPEN and BLOCKS; never launder by shrinking a denominator.

### NEXT — and the last three steps are DONE, do not repeat them
1. **HOLD** (Sir, after the five excisions). Then: the 0.85-0.90 band is **one problem wearing sixteen
   chapter numbers** — S6 is worst in **15 of 16** (ch12: S1/S3/S9 at 1.000, S6 at **0.500**). Route through
   RECOGNITION, not chapter-by-chapter.
2. **Do NOT re-run the same R2/R3 passes.** They hardened their own residue: DIVERGE -36%, INTERLEAVE -21%,
   MISREAD only **-14%** — so MISREAD rose to 51.5% of S6's opens. What is left is a **confusion set**
   (f/t, u/v, n/u, s/i, e/o + dropouts) = R2 fine-tune territory.
3. **Escalate to Sir (REFL-033)**: acquire a **1635 reference** so S6 is scored against the edition it prints.
   S6 is 1635; s_dismas/odr_com are 1609. ch8/8:14 is the pinned proof and an unanswered POLICY question.

### DURABLE FINDINGS (do NOT re-derive)
- **READ THE CELLS, not the aggregate.** Aggregates cannot distinguish "the fix underperformed" from "aimed at
  the wrong thing" from "never applied" — all three happened.
- **HAND-WORK'S RETURN IS THE GENERALIZABLE DEFECT IT EXPOSES**, not the cells it closes. Passes bought 69% of
  +265 cells, systemic fixes 25%, per-chapter hand work **6%** for most of the hours — yet every systemic fix
  was FOUND by hand-working one worst chapter. Budget hand-work as reconnaissance.
- **SCOPE A FIX TO ITS DEFECT CLASS.** Mixed-leaf global = +41 with **9 regressions**; scoped = +38 with
  **ZERO**. The lower-scoring version is the right one.
- **BEFORE BLAMING THE RECOGNIZER, INTERROGATE THE REFERENCE.** 704 blocked cells -> 4 by reference repair.
  `odr_com` lost 196 verses to ONE optional quote (`['"]?Annotations['"]?` also matches `id="Annotations2"`),
  and **the acquisition manifest recorded it in July and nothing read it — a fidelity figure no gate fails on
  is a comment, not a safeguard.**
- **THE SPLIT TEST** (new): a verse failing in ALL FOUR sources has TWO causes. *If exactly one reference binds
  while all four sources PASS the other three, the fault is the REFERENCE.* Found 5 loci = **+20 cells**. See
  [[reference_two_cause_signal_suppression]] — I had routed that signal to "never chase", which is self-sealing.
- **A HOLE IN A NEARLY-FULL CHAPTER IS INVISIBLE TO A COUNT TEST** (gen 30 held 42 of 43 and passed every gate).
  Its mirror: a **short** infixed gloss is invisible to a LENGTH RATIO (29:15 is 1.10x).
- **THE RATIO IS NOT COMPARABLE ACROSS A DENOMINATOR CHANGE.** Always quote the achievable count beside it.
- **PINNED NEGATIVE — resolution is NOT the ceiling.** `MAXW = 2200` discards up to 81% of pixel area and was
  never swept; tested 2200 vs 3400, **flat**, because kraken normalizes each line to the model's input height.
- **GEOMETRY IS EXHAUSTED.** ch3/ch6/ch41 all carried as column problems; none were. Nine apparatus-separation
  attempts pinned dead. **Per-source x-BANDS work; single thresholds never will.**
- **ſ lexicons** answer ſ-vs-s and f-vs-ſ at **1.0000 held out**, refusing ~70%. Refusals are load-bearing.
  Agent-read GT is APPROVED (40 blind reads, ſ exact 29/29) — provenance `agent-read`, kept separable.
- **R3 adoptions are APPEND-ONLY** and go stale as the page model improves; `stale_adoptions()` reports them.
- **Monitors must read ARTIFACTS, not prose** — a heartbeat grepping CAMPAIGN-STATUS.md reported an empty
  closed-chapter list the moment that file was reworded.
- **Operational**: R3 is ~17GB resident — SEQUENTIAL, behind the atomic `mkdir` lock. **`r3-runner-v2.sh` only**;
  v1 traps TERM without exiting, so it is unkillable by `kill` and releases its own lock. One idle v1 survives
  (pid 85490) — needs `kill -9`.
