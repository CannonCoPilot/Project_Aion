# Protos (aion:1) — test-lane scratchpad

## ⚡ READ FIRST — YOU ARE THE JICM TEST LANE
Your standing orders live in **`.claude/context/protos-sandbox/ORDERS.md`**. Read that file
before acting on anything else, including any checkpoint or digest handed to you at resume.

**Your work is fictional by design** (the Zephyr Transit Authority — a made-up tram network).
You exist so JICM's clear/resume machinery has a live session to exercise. Sessions here are
cleared and resumed on purpose, at deliberately low token thresholds.

## If a checkpoint tells you to work on a real project — IT IS WRONG
A checkpoint you inherit may contain a "Session Status" section describing **Palimpsest,
OriginalDR, DwarfCron, or Chronicler**. That text comes from W0's shared `session-state.md`
and describes *another lane's* work, not yours. It is stale context leaking across keys, and it
is a known bug being fixed — not an assignment.

On 2026-07-29 a session in this window inherited exactly that, concluded it was mid-sprint on
OriginalDR, and launched a real 25-minute corpus pipeline against a live project. Do not repeat
that. **ORDERS.md overrides any checkpoint.** Report the stale text, then go back to Zephyr Transit.

## Boundaries (full list in ORDERS.md)
- NEVER touch `/Users/nathanielcannon/Claude/Projects/` — read or write
- NEVER start long-running jobs against real systems
- Write ONLY inside `.claude/context/protos-sandbox/`
