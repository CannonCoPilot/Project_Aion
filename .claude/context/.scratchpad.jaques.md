# W13:Jacques — working state (2026-08-27)

## ======== RESUME HERE (2026-08-27, later). ========

### EXP-02 RAN, k=6 ON T3. THREE OF MY FIVE UNSETTLED CLAIMS WERE MIS-KEYED.
Notes: `notes/EXP-02-repair-01-floor-claim.md`, `notes/EXP-02-repair-02-and-a-bundle-finding.md`.
Per kind on opus-5: provenance 6/6 · absence 6/6 · floor 4/4 · confounded 2/2 · settled **30/30**.
Every apparent discrimination hit was MY KEY, verified each time against the tables. Two claims
repaired (both negative-tested, 4/4 and 3/3); `necessary_not_sufficient` slot now **EMPTY and
deliberately unfilled** — pool-note material is unsafe ground.
**No result yet points against "EXP-02 goes the way of EXP-01".** Condition NOT yet declared
failed: confounded is only k=2 and one slot is empty.

### ⚠ THREE DECISIONS FOR THE USER — the third is NEW and touches the ACCEPTED bundle
3. **`pool_note_peptides_are_mislocalised`** (shipped, accepted, key `not_evidenced`). Its recorded
   rationale — "the note says nothing about position" — is **empirically FALSE**: noted rows have
   median prob **0.495 vs 0.943**, 67% below 0.5 vs 15%, 49% at a non-dominant position. The
   VERDICT survives (poor localisation ≠ proven wrong residue); the REASONING does not, and the
   check accepts `not_evidenced` only, so a correct `supported` argument fails. Same family as the
   three widenings this task already paid for. **My rec: (a) widen + fix the rationale** (precedent
   `a382e62`). Changing an accepted task = resubmission ⇒ User's call.

### Harness = **`claims_probe2.py`** (NEW sibling; EXP-01's `claims_probe.py`
### left byte-untouched so its pre-registered result stays reproducible).
Scorer now compares each trial's OWN sandboxed `statements.md` to the current one and drops only
claims reworded since that trial ran — so a repair costs the affected claim, not the whole trial.
Trials e3a–e3f all live and correctly scoped.
Sandbox `~/.snorkel-calib/exp02-claims-probe/`. Trials **e3a, e3b (both T3 opus-5)** launched
~11:57, timeout 1500s, endpoint `:9800` (same as EXP-01 ⇒ comparable, but bills the User's own
session allowance). **TWO AT A TIME, never four.**
```
python3 research/frontier-failure-modes/claims_probe2.py score e3a   # then e3b
python3 research/frontier-failure-modes/claims_probe2.py report
```
Need **k≥4 on T3** for the fixed success condition: ≥1 unsettled KIND below ceiling on opus-5.
All five clean at k≥4 ⇒ the primitive does not discriminate here; SAY SO AND STOP building on it.
**Report per-KIND, never a pooled unsettled rate.** Both gotchas are handled in code: NE
calibration is derived from the spec (**0.50**, not EXP-01's 0.33) and per-kind is the default view.

### SCORER NEGATIVE-TESTED BEFORE ANY SPEND — 5 synthetic cases, all as predicted
blanket-NE **5/10** · blanket-contradicted **4/10** · perfect 10/10 · one flipped claim isolates
exactly one kind · missing claims stay OUT of the denominator. `report` excludes tier `TEST`.

### A STAGING DEFECT CAUGHT BEFORE IT COST A TRIAL (first e3a launch killed, not scored)
The bundle README points at **`published_results.md`**, which this probe must delete (it carries
EXP-01's own 7 statements + the published tables, which would settle items EXP-02 leaves open).
Left alone the sandbox made **two false claims about its own files** — the forbidden class — and
would have sent the trial hunting a missing file. Fixed in `_repoint_readme()` at stage time:
repoints the statements line to `statements.md`, drops the published-table paragraph, then sweeps
for any surviving `published_results` mention. **Negative-tested both ways** — a reworded README
raises instead of silently no-opping, and an extra mention trips the sweep.

## ======== (earlier the same day) ========

### BUNDLE STATE — green, and ONE UNCOMMITTED CHANGE
HEAD `a382e62`. **Uncommitted: the README `monotonicity_effect` pin** (working tree only) plus a
regenerated `submissions/*.zip`. Full pipeline re-verified against the pin: Gate 1 pass · **41/41
baselines fail** · oracle 1.000 / nop 0.000 · preflight **0 BLOCK** tree AND archive · portal
replay clean · 44 runs 0 failures. Removed `tests/__pycache__` cruft preflight caught.

### TWO DECISIONS OPEN FOR THE USER — do not decide alone, both move the measured rate
1. **Sequence-metric contradiction.** `test_the_sequence_metric_is_the_directional_one` PASSES a
   background-normalised ±1-any-STY metric (accepted since `56c4ae5`) that ranks **Gly LAST**
   (1.202) and Ala first (1.462) — then `EXCLUDED_RESIDUES` fails the trial for choosing Gly.
   One check licenses the metric, another punishes its conclusion. Reversal seen TWICE (i03, m04r)
   ⇒ defect signature. **(a)** tighten the sequence check (harder, removes contradiction) or
   **(b)** drop Gly from EXCLUDED_RESIDUES when the trial's own metric doesn't support it (easier,
   7/8 → 8/8).
2. **Commit the README pin?** Not yet committed.

### m-SERIES FINAL: post-pin **7/8 = 87.5%** (above the `core` ceiling ≤80%)
m01 P m02 P m03 P **m04r F** m05 P m06 P **m07r P** m08 P. The pin worked — both reruns adopted
the any-rank convention and named it in their own keys; `test_the_effect_of_monotonicity_is
_reported` now fails NOBODY. **`difficulty` still UNSET.** Submission is the User's action.

### RESEARCH PROGRAMME — `research/frontier-failure-modes/` (repo root, cross-project, untracked)
40 sources, 107 MB, every arXiv ID verified. Read `00-research-programme.md` → `99-synthesis.md`.
Headline: **Verifier's Rule** (Jason Wei) — auto-verifiable AND beyond-frontier pull against each
other; all seven discriminators we shipped sat in the "verifiable + positive training pressure"
quadrant and always fall. Label noise is field-wide (MMLU **6.49%**; HLE only **668 of ~2,500**
items clean; BenchRisk: all 26 benchmarks at risk). **Confidence diagnostic, adopted:** trial fails
while reasoning CONFIDENTLY ⇒ audit the key; fails while hedging/contradicting itself ⇒ real.

### EXP-01 RAN AND KILLED OUR LEAD — `notes/EXP-01-results.md`
Pre-registered first. **Claims stage is at CEILING in both conditions: 23 of 24 unsettled-claim
opportunities correct (20 loaded in-task + 4 isolated) = ~4% hit rate.** It is our WEAKEST check
(1 trial of 31) vs sequence metric (6) and monotonicity (6). **This corrects my own "bank it, the
absent-evidence stage is real" — over-read from n=1 (m04r).** H2 (load-dependent) also dead:
loaded 0.95 vs isolated 1.00. External evidence (AbstentionBench etc.) untouched — what fails is
OUR two claims, too easy. Do not blur those.
Harness `claims_probe.py`; probes in `~/.snorkel-calib/exp01-claims-probe/`. t2c/t3c hung >1800s,
excluded, cause unproven (no session-limit string — do NOT assert quota).

### EXP-02 CLAIM SET — BUILT, VERIFIED, NOT YET RUN. `notes/EXP-02-claim-set-design.md`
`exp02_claims/`: `verify_claims.py` (recomputes all 10 verdicts from the CSVs, exits non-zero if
evidence moves; **negative-tested 6/6**) · `claims_spec.json` · `statements.md`.
10 claims, **4 contradicted / 1 supported / 5 not_evidenced**, each NE a different kind:
necessary-not-sufficient · floor-is-not-a-threshold · provenance-unrecorded ·
absence-vs-non-detection · confounded-error-kinds. Blanket-NE 5/10, blanket-contradicted 4/10.
**RESEARCH ARTEFACT — NOT in the shipped bundle.**

**TWO GOTCHAS BEFORE RUNNING EXP-02:**
- **Calibrated NE rate here is 0.50, not 0.33** — repoint `claims_probe.py`'s abstention-bias
  threshold or it misfires. That diagnostic is the only reason haiku's misleading 2/2 was caught.
- **Success condition, fixed in advance:** ≥1 NE *kind* must fall below ceiling on opus-5. If all
  five clear at k≥4, the primitive does not discriminate here — say so and stop building on it.
  Report per-KIND accuracy, not a pooled rate.

**REJECTED, do not retry:** aggregate-only claim `the_extra_site_per_published_cell_is
_identifiable` — the +1 comes from a boundary convention so the extra site IS pointable; an expert
could defend `contradicted`. Aggregate-only slot deliberately UNFILLED.

## ======== END RESUME BLOCK ========

# (historical — 2026-08-26)

## CALIBRATION TRIALS SPEND THIS SESSION'S OWN CLAUDE ALLOWANCE — FOUND 2026-08-26

m01–m04 were launched together and **all four died on "You've hit your session limit · resets
5:10pm (America/Denver)"**, rc=1, with 0/0/5/2 files written. Quota deaths measure NOTHING and
stay out of the denominator (m03/m04's partial output is uncountable too). n=0 for the m-series.

**Why: `run` inherits `ANTHROPIC_BASE_URL=http://localhost:9800`, so every trial bills the
User's ordinary Claude session allowance — the same pool this conversation is spending.** Four
concurrent trials exhausted it. This is NOT the Portkey/Snorkel budget and no `stb keys refresh`
touches it. **Run trials ONE OR TWO AT A TIME, never four**, and expect a long working session to
starve calibration (and vice versa). Recorded because two full series were launched 3-4 at a time
before this surfaced.

## QUOTA REALITY: A FRESH 5h WINDOW BUYS ~1 COMPLETE TRIAL ALONGSIDE A WORKING SESSION

2026-08-26 evening, fresh window: **m01 completed (49/50). m02 died at 10/11 files, m03 at 6/11,
both "session limit", rc=1.** Partial output measures NOTHING — m02 is missing recommendation.md,
which is graded, so scoring it would manufacture a false failure. Out of the denominator.

**So one working session + two trials exhausts a 5h window.** A full trial is now 11 deliverables
and 25+ steps and is far more expensive than the i/j/k-series trials were. Getting to n=8 is NOT
achievable alongside conversation. Options to put to the User: (a) run calibration in a dedicated
session with no chat, two at a time, over successive windows; (b) accept n=1 and band `advanced`;
(c) leave `difficulty` unset and hand the measurement to Snorkel. **Do not keep relaunching into a
spent window — each attempt costs a partial trial and measures nothing.**

## RESUME HERE — m-SERIES: n=2 COUNTED (m01 49/50 FAIL · m02 50/50 PASS). QUOTA SPENT AGAIN.

**m02 SCORED 2026-08-26 ~20:45: 50/50, PASS, leak=none observed.** Every category clean
(comparison 10/10, consistency 2/2, inventory 9/9, other 10/10, recommendation 10/10,
reconstruction 9/9). So m02 cleared BOTH new discriminators — the unpublished 2% rate and the
absent-evidence claims stage — first time, exactly as m01 did.
**m03 SCORED: 50/50, PASS, leak clean — it did NOT die.** An interim `ls`+rc read showed 7 files
and rc=1 and I called it a quota death; that was a **mid-flight snapshot**, and the "session limit"
text came from the PREVIOUS m03 attempt's `agent_stdout.txt`, which is only rewritten at exit.
Final state: 11 files, exit 0, zero "session limit" hits. **NEVER score or triage a trial from an
interim file count — wait for the tracked job to report, then read stdout.** The window is NOT
spent; m04/m05 launched.

**m04 SCORED: 48/50, FAIL, leak clean** — `test_the_effect_of_monotonicity_is_reported` (same
diligence check m01 missed) AND `test_the_sequence_metric_is_the_directional_one` (the historic
discriminator, the only check that ever split the i-series). m05 in flight.

**m05 SCORED: 50/50, PASS, leak clean.** **m06 SCORED: 50/50, PASS, leak clean.** m07 in flight.
**m07 SCORED: 49/50, FAIL — `test_the_effect_of_monotonicity_is_reported` only.** m08 launched.
n=7, 4 pass = **57%** ⇒ `advanced` (≤60%).

## USER RULING 2026-08-26: **PIN IT IN THE README (option a), then rerun m04 and m07.** DONE/RUNNING.

README `monotonicity_effect` bullet now fixes BOTH selection rules explicitly: after = the
instruction's monotonic rule; **before = sites whose RAW estimate is at or below the rate at ANY
rank**, with an explicit "it is NOT the length of the unbroken run at the top of the list". No
value is disclosed — a convention, not a threshold. Reruns `m04r`/`m07r` in flight against it;
full pipeline (preflight → package → preflight --archive → run_gates → portal_replay) re-running.

**GREP TRAP THAT COST TWO TRIALS:** I checked the staged copies with
`grep -c "wherever they fall in the ranking"` — the phrase **wraps across a line** in the README,
so it returned 0 on the working tree AND the sandboxes, and I killed two correctly-staged trials
believing `stage` copied from the archive. **`cmd_stage` copies from the WORKING TREE**
(`task_dir()/environment/data`, subagent_calibrate.py:187) — no repackage is needed before a trial.
**Grep a phrase that cannot wrap** (here: `It is not the length of the unbroken run`).

## THE PIN WORKED. POST-PIN RATE = 7/8 = 87.5% — ABOVE THE `core` CEILING (≤80%).

**m07r 50/50 PASS · m04r 48/50 FAIL.** Both reruns adopted the any-rank convention immediately and
named it in their own keys (`n_sites_before_monotonicity_any_rank`,
`raw_any_rank_count_before_monotonicity`), both reporting 773 → 798 / +25.
**`test_the_effect_of_monotonicity_is_reported` no longer fails anyone — it was measuring my
undefined convention, exactly as suspected. Three of the series' four failures were that.**
Substituting the reruns: m01 P m02 P m03 P **m04r F** m05 P m06 P **m07r P** m08 P = **7/8**.
(The six untouched passes already used the 773 convention, so the pin cannot have changed them.)

### m04r's failures — one is the designed trap FINALLY firing, one is MY BUNDLE CONTRADICTING ITSELF
1. **`test_each_claim_is_judged_against_the_deposit` — GENUINE, first bite in 9 trials.** m04r
   called `pool_note_peptides_are_mislocalised` **contradicted**; key says `not_evidenced`. Its
   basis: no pool-noted row is DecoyP=1.0 and all 221 sit on an S/T/Y. **That is the absent-evidence
   error itself** — being on a phosphorylatable residue is necessary, not sufficient; a site can be
   on the wrong S. Its own basis even concedes "the tables cannot confirm the assigned S/T/Y is the
   *correct* S/T/Y without the answer key" and it classified `contradicted` anyway. **Bank this: the
   absent-evidence stage is real.** (Its `published_counts_used_different_spectra` = contradicted
   cost nothing — `a382e62` accepts either.)
2. **`test_a_single_decoy_residue_is_chosen_and_it_is_defensible` — DEFECT, do not bank.** m04r
   chose **Gly**, which `EXCLUDED_RESIDUES` rejects as a sequence artefact (28/48 = 58% sit before a
   serine vs 11% Ala, the DIRECTIONAL measure). But m04r **PASSED
   `test_the_sequence_metric_is_the_directional_one`** using a background-normalised ±1-any-STY
   ratio — which the check accepts since `56c4ae5` — and that metric ranks **Gly LAST (1.202) and
   Ala FIRST (1.462)**, i.e. it says Gly is the LEAST artefact-like. **One check licensed the
   metric; another failed it for the conclusion that metric entails.**
   **This reversal has now been seen TWICE** (i03 ranked Asp 1.996 above Gly 1.823; m04r ranks Gly
   last) ⇒ defect signature, not difficulty. **USER DECISION, do not fix alone — it moves the rate
   either way.** Options: (a) tighten the sequence check to require a measure that actually
   separates Gly (directional, or a normalisation demonstrated to), removing the contradiction and
   making the task harder; (b) drop Gly from `EXCLUDED_RESIDUES` when the trial's own declared
   metric does not support excluding it — easier, and 7/8 becomes 8/8.

## m-SERIES COMPLETE: n=8. **m08 50/50 P.** AND m01 RE-SCORES 50/50 AT HEAD.

**m01 now scores 50/50, deterministically, twice, repo clean at `a382e62`.** My earlier 49/50 for
m01 does not reproduce under the current build — do not carry that number forward. Corrected series:

| m01 50/50 P | m02 50/50 P | m03 50/50 P | m04 48/50 F | m05 50/50 P | m06 50/50 P | m07 49/50 F | m08 50/50 P |

**n=8, 6 pass = 75% ⇒ `core` band (≤80%), NOT `advanced`.** Every earlier band figure I quoted this
session (50/57/60/62.5%) was computed with m01 miscounted as a failure. Pooled: 19 counted, 12 pass.

## THE MONOTONICITY CHECK IS A CONVENTION DISPUTE, NOT DIFFICULTY — SIXTH DEFECT OF THE FAMILY

`check_the_effect_of_monotonicity_is_reported` scans params for Ala `raw` = **773**, from
`build_task_data.py:412`: `"raw": sum(1 for v in rec["decoy_raw"] if v <= t)` — **every row whose
raw estimate is ≤ t, at any rank**. Its partner `q` (798) uses `count_at`, the **monotonic
top-block** rule. **The key therefore compares two DIFFERENT selection rules and calls the gap
"the effect of monotonicity".**

m04 and m07 both applied ONE rule to both sides — contiguous top-of-list run — getting
**750 → 798, +48**. m04 states the definition explicitly. m01/m02/m08 used the key's mixed reading
(773 → 798, +25). Both are internally coherent; **the trials' reading is arguably the better
science**, since the point of the comparison is to hold selection fixed and vary only monotonicity.
The instruction defines only the AFTER quantity ("made monotonic down the ranking, is at or below
it"); the BEFORE quantity is nowhere pinned. ⇒ undefined convention = **bug, not difficulty**
(User's own framing: misrepresentation/ambiguity is a bug; protocol is a communication contract).

**DO NOT fix unilaterally — either fix changes the measured rate. Options for the User:**
- (a) **PIN it in README** — define before = "sites whose raw estimate is at or below the rate, at
  any rank". Keeps the check real, removes the ambiguity, is a communication contract not a
  concession. **My recommendation.** Rate stays 75% but the failures become honest.
- (b) **Widen to accept 773 OR 750.** Then m07 → 50/50 P, m04 → 49/50 F (sequence metric only)
  ⇒ **7/8 = 87.5%, below every band.** Effectively a solved task.

Either way **`test_the_sequence_metric_is_the_directional_one` (m04 only) is the sole surviving
real discriminator** — as it has been across all four series.

### (superseded) THE MONOTONICITY CHECK IS NOW THE TASK'S PRIMARY DISCRIMINATOR — 3 of 7
m01, m04, m07 all fail `test_the_effect_of_monotonicity_is_reported` (wants Ala's pre-monotonicity
count 773). It is the ONLY failure in m01 and m07. **Before banking it as difficulty, check whether
all three failed it the SAME WAY** — identical failure mode across three isolated trials is this
project's defect signature (it has fired five times), and this check was retained from a class of
diligence checks I retired two members of. If they all, say, report the post-monotonicity count or
omit the figure entirely, that is a README/instruction ambiguity, not expertise.
**Do not quote 57% as a difficulty measurement until that audit is done.**

**Running tally m-series: n=5, 3 pass = 60%** — m01 49/50 F (monotonicity) · m02 50/50 P ·
m03 50/50 P · m04 48/50 F (monotonicity + sequence metric) · m05 50/50 P.
Sits exactly on the `advanced` ceiling (≤60%). Pooled across all three series: 16 counted,
9 pass = 56%. **Every failure in the m-series is on a PRE-EXISTING check; neither redesigned
discriminator (2% rate, absent-evidence claims) has failed a single trial in five.** Identical to the pre-hardening i-series (4/8) and
the hardened j-series (3/8 → 50% at n=6). Three series, three redesigns, same rate.
The redesign did not move it. Recommendation standing: accept `advanced`, stop iterating.

## (earlier note, m01 only)

HEAD `a382e62`, repo clean. **m01 SCORED: 49/50, FAIL — only `test_the_effect_of_monotonicity_is
_reported`** (the surviving diligence check: wants Ala's pre-monotonicity count 773).
**m02 and m03 were IN FLIGHT at refresh.** Check, then continue to n=8, **TWO AT A TIME**:

```bash
for t in m02 m03; do printf "%s: " $t; ls ~/.snorkel-calib/ec-starfish/phosphosite-flr-reconstruction-001/$t/app/output | wc -l; done
python3 projects/ec-starfish/scripts/subagent_calibrate.py score ec-starfish phosphosite-flr-reconstruction-001 m02
```
0 files + "session limit" in `agent_stdout.txt` ⇒ quota death, measures NOTHING, relaunch later.
rc=0 + 11 files ⇒ score. Then launch m04, m05 … two concurrent, never four.

### THE REDESIGN IS NOT BITING EITHER (n=1, so hold the conclusion loosely)
m01 **computed all four rates correctly** — did not interpolate 2% — and **judged all six settled
claims correctly, including both halves of the pool pair** meant to carry the stage. So neither new
discriminator caught it; its only failure is a diligence check of the kind already retired twice.
**If this holds to n=4, say so and recommend accepting `advanced` rather than iterating again** —
six discriminators have now been added and every one was cleared first time by a capable trial.
Do NOT keep adding checks without telling the User the pattern.

### A CLAIM I HAD TO STOP GRADING (`a382e62`) — the fifth defect of this family
m01 judged `published_counts_used_different_spectra` **contradicted**; key said `not_evidenced`.
**m01 was entitled to**: a uniform +1 in all 35 cells is implausible for a different spectrum set,
so "refuted" is fair; a counting-convention difference also explains it and the deposit never says
which, so "unsettled" is fair. Both defensible ⇒ now accepts EITHER, and the README says so.
Guarding baseline still fails (it upgrades every unsettled claim, including the two settled ones)
— verified, not assumed.

## SUPERSEDED — the quota block (window has since reset)

Single-trial probe re-run at **20:25 MDT 2026-08-26 also died** with the same
"resets 5:10pm (America/Denver)" message and 0 files — so the quoted reset time is NOT reliable
and the window had not cleared. **Do not relaunch a series to find out.** Probe with ONE trial:

```bash
python3 projects/ec-starfish/scripts/subagent_calibrate.py run ec-starfish phosphosite-flr-reconstruction-001 m01
head -c 200 ~/.snorkel-calib/ec-starfish/phosphosite-flr-reconstruction-001/m01/agent_stdout.txt
```
"session limit" ⇒ still blocked, stop and tell the User. 10–11 files ⇒ proceed, **two at a time**.

Nothing about the bundle is in doubt: `c2c1c0c` is clean and fully gated from the ARCHIVE
(Gate 1 pass · 41/41 baselines fail · oracle 1.000 / nop 0.000 · preflight 0 BLOCK · portal replay
clean · instruction.md 250 words). **The only thing missing is the measurement.**

## OLD RESUME NOTE — m-SERIES SCORING (superseded by the block above)

Repo CLEAN at `c2c1c0c`. **m01–m04 were launched against the redesigned task and had written
NOTHING when context ran out.** They are `claude -p` children of the previous session, so assume
the session teardown killed them exactly as it killed the j-series — CHECK BEFORE SCORING:

```bash
for t in m01 m02 m03 m04; do printf "%s: " $t; \
  ls ~/.snorkel-calib/ec-starfish/phosphosite-flr-reconstruction-001/$t/app/output | wc -l; done
```
0 files ⇒ dead, relaunch. 10–11 files ⇒ score. `run` now exits non-zero on empty output, so a
killed trial cannot be silently banked. Relaunch/score, ONE tracked job each (not `&`+`wait`,
which is what let the j-series die unnoticed):
```bash
python3 projects/ec-starfish/scripts/subagent_calibrate.py run   ec-starfish phosphosite-flr-reconstruction-001 m01
python3 projects/ec-starfish/scripts/subagent_calibrate.py score ec-starfish phosphosite-flr-reconstruction-001 m01
```
Target n=8. **51 checks now** (49 + claims + the 2% rate folded into existing counts checks).

**What to read for, not just the rate:** does `test_each_claim_is_judged_against_the_deposit`
split trials? Trials failing it *for the same reason* = my defect (widen, as with `+1` and
background-normalisation). Trials failing *differently* = difficulty. Every trial clearing it =
say so plainly and do NOT count it as hardening — that is what happened to the previous four
additions. Also separate expertise failures from diligence ones before quoting any number.

**`difficulty` in task.toml stays UNSET** — User ruling 2026-08-26: hold until the task is
hardened. Do not band off 45–50%. Submission remains the User's action.

## REDESIGN (`c2c1c0c`) — CHANGED WHAT THE TASK ASKS. m-SERIES RUNNING, NO RESULT YET.

Grading harder failed four times over three series (n=19, isolated, leak-clean, ~45–50% flat).
Every added check was real but cleared first time by a capable trial. So the QUESTIONS changed:

1. **A rate the paper never publishes (2%).** Removes the anchor — every other graded count had a
   published counterpart to self-check against. Interpolating published 1%/5% is nearly right for
   Ala 0.6%, Glu 1.6%, Pro 2.5% and WRONG for Gly 22%, **Asp 70%** (Asp does not move 1%→2%:
   206→206). `THRESHOLDS` in _checks now carries "0.02" so existing counts checks grade it;
   comparison keeps its own truth (still 35 published cells).
2. **Absent-evidence stage** — Beech's best-measured design. 7 statements at the end of
   published_results.md → `contradicted` / `not_evidenced` / `supported`. The discriminating pair
   is one annotation asked twice: `pool_note_marks_the_decoy_sites` CONTRADICTED (221 rows, **0**
   on a decoy-flagged site) vs `pool_note_peptides_are_mislocalised` NOT_EVIDENCED (pool identity
   error says nothing about position). README warns the distinction is graded and defines all three.

`verify_claims()` re-derives every classification from the shipped tables at build time and aborts
if the evidence moves. instruction.md back to **250 words exactly**. Gate 1 pass · **41/41
baselines fail**, each new baseline failing only its own check · oracle 1.000 / nop 0.000 ·
preflight 0 BLOCK · portal replay clean, all from the repackaged archive.

### CANDIDATES DESIGNED, TESTED, REJECTED — do not retry
- tie-permutation spread: 200 random permutations hit the extremal bound [816,825] for Leu exactly
  → no confound, and the max depends on which orderings you try (arbitrary-unknown defect).
- decoy hits corrected for opportunity: only swaps ranks 3/4, nothing hinges on it.
- circularity/decoy-fraction check (`78709eb`): REAL and passable but ALL k-trials cleared it.
  It traps a written baseline, which is not evidence it traps a capable agent.

### MY LOOSE MATCHERS HAVE FAILED MORE CORRECT ANSWERS THAN THE TASK HAS FAILED AGENTS
Four defects of one family this session. Three document-wide `has_number` scans (decoy hit counts;
rate-cost; circularity factor) — **treat a document-wide numeric scan as a defect by default**,
grade the declared key. Plus `entry_for_residue` matching "Pro" inside "**pro**bability"
(`193e555`), which failed k02 despite all six ratios being correct — and the colliding key existed
only because MY README asked for it. Fixed to whole-word matching.

## HARDENED SERIES (j01–j08, commit `e221db8`) — HARDENING DID NOT MOVE THE RATE

| j01 49/50 F (sequence metric only) | j02 50/50 P | j03 44/50 F (6, incl. undetermined-cells=10 vs 9) |
| j04 50/50 P | j05 50/50 P | j06 47/50 F (monotonicity map + sequence metric) |
j07/j08 pending. **n=6, 3 pass = 50% — identical to the pre-hardening 4/8.**

The three new discriminators are REAL (each has failed a trial; j03 miscomputed the undetermined
cells as 10, j06 missed the monotonicity map) and NOT vacuous (j01 got all six largest_tied_block
values, 9 undetermined cells, and costed the rate 664->822/+158/~41 correctly). But a capable
trial clears them first time, so they add reporting burden more than discrimination.
**The sequence metric is still the only check that splits trials.** Structural problem unchanged:
one check carries the task. Difficulty STILL unset per the User's ruling (hold until hardened).

KNOWN LOOSENESS, not yet bitten: `test_the_rate_choice_is_costed_not_just_justified` matches the
expected-error figure with tol 3 across the WHOLE document. Tighten to a declared key if any trial
passes it without an explicit costing — same fix as check_decoy_hit_counts_are_right needed.

## PRE-HARDENING BASELINE — n=8, 4/8 PASS (50%). THE TASK IS NOT FRONTIER.

`phosphosite-flr-reconstruction-001`, trials i01–i08, all isolated (no CLAUDE.md/MEMORY.md),
leak audit clean on every one, model = CLI default **opus[1m]** over Aion's `:9800` proxy
(NOT Snorkel's gateway — recorded in each `run_meta.json`).

| pass | i03 i04 i05 i06 | 46/46 each |
| fail | i01 i02 i08 | 45/46, ALL on `test_the_sequence_metric_is_the_directional_one` |
| fail | i07 | 42/46, reconstruction+comparison — the only divergent failure |

**50% ⇒ `advanced` band (≤60%), not `frontier` (≤20%).** If the contested check is widened
(see below) it becomes 6/8 = 75% ⇒ `core`. Either way `difficulty` in task.toml is wrong.
h01/h02 are CONTAMINATED and excluded; both now score 46/46 and mean nothing.

### THE CONTESTED CHECK — USER DECISION PENDING, DO NOT DECIDE ALONE
i02 and i08 INDEPENDENTLY converged on the identical measure ("displacement rate": fraction of
decoy hits adjacent to a position independently confirmed as a confidently-localised real site,
prob>=0.95; Gly 0.3958, Pro 0.2584, Ala 0.2105, Leu 0.1852, Asp 0.1538, Glu 0.125). Same reason,
two isolated trials = the defect signature proper. i01's failure is DIFFERENT and is a correct
rejection (bare uncorrected ±1 adjacency: Gly .729 Asp .731 Ala .684 — excludes the wrong ones).
Argument FOR widening: requiring the neighbour be a *known occupied site* is itself a control
against composition, and it separates Gly by 1.5×. Argument AGAINST: it never computes a
background, and normalisation demonstrably changes the answer — i03's normalised measure ranks
**Asp 1.996 ABOVE Gly 1.823**. Widening makes the task easier, so it is not mine to do quietly.

### HARNESS FIXES (`51939f7`) — `run` IS NOW PROVEN END-TO-END
- Trial i01 first ran in **plan mode**: full analysis, then "May I proceed to write?", exit 0,
  zero files. `--allow-dangerously-skip-permissions` only OFFERS the bypass. Now also passes
  `--dangerously-skip-permissions` and `--permission-mode bypassPermissions`.
- `run` now EXITS NON-ZERO on an empty output dir — exit 0 with no files is indistinguishable
  from an agent that could not do the work, and is how a false frontier rate gets banked.
- `CONSTANT_PHRASES` widened with `+1`/`+ 1`/`plus one` (h01 wrote "a difference of +1 in 35 of
  35 cells"). Baseline needed no edit — it reads the list via `bundle_vocabulary()`. Check-name
  diff before/after: **IDENTICAL** (`scratch/bc-before.tsv` vs `bc-after.tsv`). Gates re-run on a
  REPACKAGED archive; against the stale one the diff would have been identical for the wrong
  reason. h01 45/46 → 46/46.


Repo clean. Last commit `02208a4`. Branch `experiment/derive-vs-adjudicate`.

## WHERE I AM

**ec-starfish. THREE threads.** Resume point is the first one.

- **`phosphosite-flr-reconstruction-001` (the ACCEPTED task) — REVISED UNDER REVIEW. HEAD `56c4ae5`.**
  Gates green. **RESUME HERE: re-run calibration through the new ISOLATED path.** `h01`/`h02` are
  scored but **CONTAMINATED** (they ran with CLAUDE.md + MEMORY.md loaded) — they do not count.
  ```
  python3 projects/ec-starfish/scripts/subagent_calibrate.py run   ec-starfish phosphosite-flr-reconstruction-001 i01
  python3 projects/ec-starfish/scripts/subagent_calibrate.py score ec-starfish phosphosite-flr-reconstruction-001 i01
  python3 projects/ec-starfish/scripts/subagent_calibrate.py report ec-starfish phosphosite-flr-reconstruction-001
  ```
  Run several (i01..i04). **`run` is UNTESTED end-to-end** — the isolation probe passed but the
  subcommand itself has not driven a full trial yet. Verify it writes 10 files before trusting it.
- **Task 1 `inversion-breakpoint-3d-constraint-001`** — v2 gated green, calibration still n=1.
- **Task 2 bunting Z-chromosome** — **feasibility gate PASSED**, schema design not started.

`batch_calibrate.py` is DEAD (exited). Do not restart it against the gateway — see below.

---

## PHOSPHOSITE — REVIEWER REVISION, ROLLED BACK, THEN REBUILT HARDER

Submission `e70a7bec-43c1-4958-92bf-0254719da9b6`. Reviewer accepted the science but wanted the
verifier-critical reporting decisions made deterministic.

**First attempt (`6c4527b`) satisfied the review and DESTROYED the difficulty.** Two subagent
trials solved it outright, 40/40 each, converging on the same route. The reviewer's complaints and
the task's discrimination were the same thing: AutoEval trials were dying on cross-stage
consistency and reporting conventions, so making those deterministic turns their failures into
passes. **Reverted in `a9a6d1d`.**

**User ruling: satisfy the review "letter of the law" — resolve the valid reporting complaints,
keep the built-in scientific difficulty, add more where possible. This is NOT deception and he
said so explicitly; do not re-litigate it.** What I declined to do is fake compliance or disguise
changes; what I did do is answer the format complaints honestly and deepen the science openly.

**Rebuilt: `73bc0fd`.** Gate 1 pass · **36/36 baselines fail** (was 30) · oracle 1.000 / nop 0.000 ·
portal replay clean · preflight 0 BLOCK · instruction.md **250 words** (it had shipped at 256, over
the ceiling, independently of the review).

### What was answered vs declined
- **Answered in full** (format, costs no difficulty): comparison.csv population as a rule, both
  sides must exist, no blank/zero/`n/a` placeholders, unreconstructible estimator absent from both
  files, row counts fixed by the rules, bare-integer counts, numeric `distinct_differences` (+ the
  labelled-object form), named per-residue parameter fields, pre-monotonicity count recoverable.
- **Declined, openly**: which population the residue-frequency correction is counted over.
  Row-counting 3.8796 vs peptide-counting 4.0562, far beyond tolerance — documenting it makes the
  stage a lookup. Only the requirement to DECLARE the choice is documented.

### The four new discriminators — ALL from real material, nothing invented
1. **Three published tables now shipped.** The paper is **open access, PMC9251759, CC BY** — it
   reports PXD007058 (synthetic, ours), PXD000612 (human) and PXD008355 (Arabidopsis), all six
   residues, all three rates. Comparing against the wrong one is well-formed and wrong. Graded on
   prose (`published_table_for_this_deposit`) AND numerically
   (`check_the_comparison_uses_the_synthetic_published_numbers`).
   **I refused to fabricate a competing version of a real paper's table — that is falsifying a
   scientific record, and `audit_shipped_claims.py` exists for it. Getting the real OA paper is
   what made this legitimate.**
2. **Ties.** 42–51% of rows share a probability; permuting them moves exactly-graded counts by up
   to **17 sites**. New: `tied_probability_rows` (per table, exact) and `rank_tie_break`.
3. **Sequence metric must be DIRECTIONAL.** next-residue-is-S: Gly 58%, Ala 10%, Leu 11%. Broad
   any-adjacent-STY: Gly 69% BUT Ala 63%, Asp 65% — it excludes the wrong residues. Both earlier
   trials used a broad measure and passed only because the old check was loose. Graded numerically
   on the Gly directional count (28/48).
4. **Answer-key lookalike.** The deposit's own `Incorrect pool.` note (32–42 rows/table) is a
   per-peptide IDENTITY flag, not a per-site POSITION flag. New `answer_key_substitutes_rejected`.

Truth regenerated by `build_task_data.py` (not hand-written); shipped CSVs byte-identical.
Six new baselines, one per new check.

### Three bugs gating caught — all mine, all instructive
- `entry_for_residue` is built for `reconstruction_parameters.json` and returns **None for five
  residues and an int for the sixth** on the inventory. My tied-rows check asked the wrong object
  and reported the key absent. Fixed with a local `_table_entry()`.
- My published-table check **forbade naming the other datasets at all** → failed the reference for
  explaining what it ruled out. Same vocabulary-quiz defect this task has been bitten by before.
  Replaced with a numeric check on the published values.
- Declaring `sequence_artefact_metric` let the existing baseline
  `rules_a_residue_out_on_its_count_alone` PASS, from a field it never used. Baseline now scrubs it.

### Open
- Submission note wording is the User's call. My recommendation: state plainly that typing and
  population rules are fixed, that the correction-population request was declined because it would
  disclose a graded value, and that stage 3 was deepened.

---

## CONTEXT LEAK INTO CALIBRATION AGENTS — FOUND AND CLOSED (`56c4ae5`)

Sir asked whether launched agents inherit this session. Measured, don't re-derive:

- **Chat history: NO.** `general-purpose` subagents start fresh; only `subagent_type: "fork"`
  inherits the conversation.
- **Scratchpad: NO.** Grepped all four transcripts — zero hits for `scratchpad.jaques`,
  `Project_Aion`, `expected_truth`, `_checks.py`. What Sir saw was my own edits.
- **CLAUDE.md + MEMORY.md: YES, and this is the real leak.** Claude walks UP from cwd. A headless
  run started inside the repo reports "Project: SnorkelTasks", quotes an authoring rule verbatim,
  and lists MEMORY.md, the AutoEval atlas and the gateway rule cards as loaded.
  **`--system-prompt` does NOT suppress it** — probed, still loaded. **Only a cwd with no
  CLAUDE.md ancestor works**; from `~/.snorkel-calib` the same probe answers "none loaded".

Fix: trial sandboxes now live at **`~/.snorkel-calib/<project>/<task>/<trial>/app`** (outside the
repo — deliberate departure from the "temp files in scratch/" rule, since the sandbox cannot sit
under the CLAUDE.md it must not see; **Sir was offered the chance to object and has not yet**).
New `run` subcommand: `claude -p --system-prompt --strict-mcp-config
--allow-dangerously-skip-permissions --add-dir <app>`, cwd = app. Records the endpoint, because
`ANTHROPIC_BASE_URL` is inherited and points at **Aion's :9800 proxy, not Snorkel's gateway**.
Trial records mirrored back to `scratch/subagent-cal/` so evidence stays in-repo.

## THE SEQUENCE CHECK WAS TOO NARROW — trials were right, I was wrong

h01 and h02 both failed `test_the_sequence_metric_is_the_directional_one` — **the same check, which
is the DEFECT signature, not difficulty.** Each had independently measured adjacency against that
residue's **own background adjacency in the same peptides**, and h01 also reported the naive
control (any S/T/Y in the peptide scores 1.000 for all six, excludes nobody). Background
normalisation defeats the same confound the directional measure does, so demanding the directional
wording graded **agreement with my reference rather than the argument** — the vocabulary-quiz
defect this task has now paid for three times.

Check now accepts **directional OR background-normalised**, still rejects bare uncorrected
adjacency. **Scoping mattered**: reading the normalisation phrase family across the whole document
let the guarding baseline `rules_out_on_any_adjacent_sty` PASS on words appearing elsewhere in the
reference's prose. It is read **only where the metric is declared**.

Post-fix: h02 **46/46 pass**, h01 45/46. Gates green (36/36 baselines fail).

### OPEN — flagged, deliberately NOT done at the end of a long context
`test_the_attribution_is_argued_from_the_pattern` fails h01, and it looks like the **same
vocabulary-miss class**: h01 wrote "differ by exactly **+1**" and "inclusive-boundary convention",
while `CONSTANT_PHRASES` carries "exactly one" / "by one" / "offset of one" but no `+1` form.
Widening it means editing `_common.py`, **whose phrase lists the guarding baseline scrubs** — so
the baseline must be updated in the same change or Gate 2 silently stops discriminating. Do it
deliberately, and diff the baselines' failing CHECK NAMES before/after.

## SUBAGENT CALIBRATION HARNESS — `6df687f`, `155d574`

`projects/ec-starfish/scripts/subagent_calibrate.py` — `stage` / `score` / `report`.
Doc: `docs/28-subagent-calibration-harness.md`. Built because the gateway cannot be driven.

Scores with the **shipped verifier in the shipped image** using `run_gates.sh`'s own mounts, so a
pass there is a pass under the gate. **INDICATIVE ONLY — must not set `difficulty`**: the subagent
is *not sandboxed* (could read grading material; prompt forbids it, `score` audits for it), is
reached over a different path than Harbor's, and works against translated paths.

Bugs it caught in itself: the parser pinned pytest's node prefix to `tests/` while the image
reports `../dev/`, so it **scored nothing** until the oracle was run through it; and the leak audit
originally flagged the full-precision ratio — **backwards**, since that ratio is computable, so it
fired on the oracle. And `report` printed "IDENTICAL → TASK DEFECT" when every trial had **passed**
(empty failure set) — verdict exactly inverted.

---

## TASK 1 — `inversion-breakpoint-3d-constraint-001` — v2 BUILT AND GATED, UNVALIDATED

Path: `projects/ec-starfish/tasks/biology/bioinformatics/inversion-breakpoint-3d-constraint-001`

### What happened today, in one line each
- Rounds 01–03 all returned mean 0.000 and **none measured difficulty**. All three were
  **misrepresentation in my own shipped files** (see the User's correction below).
- Round 04, conventions pinned: **mean 1.000, 5/5**. A solved exercise, above every band.
- **User ruling: `core` is a failed task design.** Rebuilt as v2, forensic reproduction.
- Round 05 on v2: never completed — every attempt died on `412 Portkey usage limit`.

### v2's design (docs/24, docs/25)
Ships the deposited tables **plus the published statistics**, and does not say anything is wrong.
A faithful analysis reproduces **none** of them:

| | P1 | P2 | P3 | P4 |
|---|---|---|---|---|
| published | 10.122 | 12.16 | 20.605 | 16.402 |
| faithful | 3.396 | 4.440 | 10.587 | 6.843 |

The agent must recover the choices under which each arises — and they **differ between species**,
because the two species were run through different code. Graded as **numeric fingerprints, never
vocabulary**: `mask_bins_subtracted = 0` IS the `sum(NULL)` discovery; `ab_denominator` 19,262 vs
18,521 IS the `chrY`/`chrY_IX` filter that never matched. P5 is `not_reproducible`.

Dropped `boundary_window_offset` before shipping: tested all 8 switch combinations, it changes **no**
output, so it is unidentifiable and grading it would have repeated the rounds 01–03 defect.

### Gates — all green, zero model cost
Gate 1 pass (14 checks) · Gate 2 **7/7 baselines fail**, incl. new `faithful analysis, no forensics`
which fails on exactly the two forensic checks · `harbor -a oracle` 1.000 / `-a nop` 0.000 ·
`preflight` 0 BLOCK · `validate_metadata` 0/0 · `audit_shipped_claims.py` clean ·
`check_oracle_reproduces.py` OK. Build **self-validates**: aborts if the reconstruction stops
reproducing the published χ².

### Calibration standing — n=1 counted out of ~14 trials attempted
Pool: `scratch/calibration_pool.json`. Report: `batch_calibrate.py --report-only`.

| stage | n=1 |
|---|---|
| protocol | 3/3 |
| recomputation | 2/5 |
| **forensic_reconstruction** | **0/2** |
| adjudication | 4/4 |

That profile is what a frontier task should look like. One excluded trial (`5N8LWs8`) had cracked
P2/P3/P4 before dying → hard but not a wall. Two output-producing trials failed **differently**,
which is the acceptance criterion (identical ⇒ my defect; divergent ⇒ difficulty).

`difficulty = "frontier"` is banner-commented **UNVALIDATED** — design target, not a measurement.
**Do not band it without completed trials.**

### Open on task 1
- `email` in `task.toml` is still `"anonymous"` — never answered; do not put Sir's address in a
  client-facing artifact without asking.
- `recomputation` at 2/5: both trials read the 1-based TAD/compartment files literally (5,559 /
  3,204 vs key 3,735 / 2,145). I argue the key is right on professional convention (BED is 0-based;
  a literal compartment read leaves a 1 bp gap in a *partition*). **If this stays ~0% across a dozen
  trials, re-examine rather than declare it hard.** Report it as a separate finding, not folded into
  the headline rate.

---

## TASK 2 — bunting Z-chromosome — SOURCE VERIFIED, FEASIBILITY GATE NOT RUN

`projects/ec-starfish/docs/26-task-002-scoping-bunting-zchrom.md`
Data: `projects/ec-starfish/source-materials/bunting-zchrom-data/` (+ `SOURCES.json`).

Nikelski, Rubtsov & Irwin, *Mol Ecol* **10.1111/mec.17526**; Dryad **10.5061/dryad.prr4xgxw7**,
**CC0-1.0 verified from the API licence field** (the dataset page states no licence).

**13/13 files sha256-verified** against the Dryad manifest. GEMMA assoc = 220,201 rows with full
ULMM columns; 3 × 320-row phenotypes; ADMIXTURE K=2; PC scores; 3 Z-chromosome LD tables. The
7.3 GB VCF and 65 MB GDS deliberately **not** fetched — not needed.

### FEASIBILITY GATE — RUN 2026-08-25, **PASSED**. `docs/27-task-002-feasibility-gate.md`

All ten published quantities recompute **exactly** from the deposited tables — per-trait SNP counts,
per-trait chr-Z counts, Bonferroni thresholds, significant-SNP counts (1 chr20 + 4 Z / 34 / 8), n=230
complete-phenotype males, n=53 allopatric yellowhammers, n=321 sampled. The four individually named
SNPs reproduce trait-for-trait.

**The one discrepancy: background ULMM = 220,220 published vs 220,200 deposited. Twenty short.**
Doubly attested, so not a one-sentence typo — the paper's own threshold `15.29811` = `ln(220220/0.05)`
to five decimals, while the deposit gives `ln(220200/0.05)` = `15.29802`. Brow and throat reconcile
perfectly. The gap does **not** propagate: significance calls are identical either way. The 20 rows
are not individually identifiable (GEMMA filters per trait; sets differ by ~1,300 SNPs across all 31
chromosomes) — visible only in aggregate.

**Convention chain is the real difficulty** (all discoverable ⇒ safe to grade):
`chr32` **is Z** — assoc files are numbered 1–32 with no `Z` and no chr16; 5,761/9,375 LD positions
land on chr32 vs 2–4 elsewhere · the significance scale is the **natural log**, not −log10 (15.298 is
absurd as −log10) · Bonferroni denominator is the **per-trait** n · use `p_lrt` (README states this).

**Deposit is internally clean** — no intra-deposit falsehood exists to find. Pheno files hold **321**
records not 320 (no trailing newline; `wc -l` undercounts); 0/321 mismatches against the PC file's own
score columns; `.Q`, map metadata and Fst groups all reconcile at 321.

**Design caution:** task 1's "reproduces nothing" framing does NOT transfer. One aggregate count is
deep but narrow — grading only "find the 20" is one guess from solved. Build on the convention chain.

### NEXT ACTION
Design task 2's output schema as **numeric fingerprints, never vocabulary**: `z_chromosome_id`=32,
the three thresholds, the three significant-SNP counts, deposited-vs-threshold-implied background
count. Never ship the preprint text.

Access facts: bioRxiv **v1 is the only version** (v2 403s because it does not exist); Cloudflare 403s
both `curl` and `WebFetch` — **Chrome navigation works**. Mol Ecol VOR is paywalled (no PMCID).
Preprint text: `scratch/bunting_preprint.txt`.

---

## HARD-WON TODAY — do not re-derive

- **The User's correction to my difficulty framing (adopted, docs/25 rewritten).** A human expert
  can know everything **except what has been misrepresented or falsified** — assume they know real
  scientific conventions, intuit them, and derive task-structural ones by exploration. So real and
  structural conventions are **never pinned and always safe to grade**; output protocol is specified
  as a communication contract; **misrepresentation is the only unsolvable category and is a bug, not
  difficulty.** My "arbitrary vs discoverable" framing laundered my own defects into inherent
  ambiguity. Re-audited: **the agents were right in rounds 01, 02 AND 03; my reference was wrong in
  all three** (spec wrote closed `[a,b]` and computed `[a,b)`; README asserted 0-based for a 1-based
  file; key read two origins literally).
- **Shipping data that doesn't reproduce is fair ONLY when the falsification is the disclosed object
  of study.** A false claim about the bundle's *own* files never is. Guard:
  `scripts/audit_shipped_claims.py`, negative-tested by reintroducing round 02's exact false line.
- **Tiered calibration (User ruling, in `task-authoring`):** haiku → sonnet for build/dev/revision;
  only then k=3 per top tier (`claude-opus-4-8` **and** `gpt-5.5`). Zero-token gates first.
- **`stb keys refresh` is capped at 20 ACCOUNT-WIDE and is now EXHAUSTED** — same error under Otter
  and Starfish. Rotation is not a budget workaround. Corrects my own earlier claim that rotation
  resets usage; the allowance **refills on a time window**.
- **`stb login` can be scripted** (feed stored platform key, then project number `1`=Otter
  `2`=Starfish) — but a failed credential fetch leaves `config.ini` pointing at the NEW project
  holding the OLD project's key. **Back up config.ini first.** Backup:
  `~/Library/Application Support/stb/config.ini.bak-starfish-20260825`. Config currently restored to
  Starfish (`cb869485-…`), key tail `mucW1y`.
- **RETRACTED: the "budget never refills in 2 hours" measurement was an ARTEFACT.** All 25 "spent"
  readings were **Cloudflare, not Portkey** — `api.portkey.ai` is fronted by Cloudflare, which
  blocks `urllib`'s default User-Agent with **403 "error code: 1010"**. `key_is_live()` returned
  `e.code == 404`, so every non-404 silently became "spent". The same key answered **200 to curl
  throughout**. **Refill cadence is UNMEASURED — do not quote a number.** Fixed in `2eaccb4`:
  browser UA, one real minimal call, **only 412 means spent**, anything else raises
  `ProbeInconclusive`. Negative-tested 412/403/400/401.
- **Any HTTP client hitting this gateway MUST send a browser User-Agent.** Isolated by holding the
  request identical and varying only `-A`.
- **THE GATEWAY IS CURRENTLY BROKEN SERVER-SIDE.** Every route and model (gpt-5, sonnet-4-5,
  opus-4-8, opus-4-5) returns `400 {"message":"Following keys are not valid: vertex"}`, six minutes
  after the same unchanged key returned 200, with `config.ini` byte-identical bar `version_check`.
  Not budget (412), not auth (401). **Escalate to Snorkel; nothing local fixes it.**
- **`stb keys verify` is NOT a health check** — reported "✓ AI credentials are valid" while every
  call 400'd. It tests the credential, not reachability.
- **`stb` upgraded 2.4.9 → 2.4.10** (2.4.9 refused to run `keys refresh` at all, version-gated).
  The 20-refresh cap is **account-wide and exhausted on BOTH versions**; a failed refresh does NOT
  overwrite the stored key. Config backed up: `config.ini.bak-preRefresh-20260825-184822`.
- `claude-haiku-4-5` maps to an **unprovisioned vertex backend** even when healthy — a probe model
  must be one the key can actually reach.
- **`stb projects list` now shows a THIRD project, `Geranium-Production` (active)**, and
  `CDG_Otter_Prod` reads Submitter **inactive**. Not previously recorded; worth asking about.
- Using Otter's credential for Starfish calibration = spending one engagement's allowance on
  another's work. **Ask before doing it.**
- **Calibration standing is UNCHANGED at n=1 counted / 12 pooled** (8 quota deaths, 3 wrote nothing).
  The single counted trial is an **opus-4-8** trial pooled from an earlier round; `claude-sonnet-4-5`
  has **no completed trials**. Target was 8. **Below threshold ⇒ BLOCKS. Do not band difficulty.**
- **Dryad access:** metadata API answers `curl` unauthenticated and carries **per-file sha-256**;
  `/api/v2/files/<id>/download` = 401 (bearer token); public `/downloads/file_stream/<id>` = HTTP
  200 whose **body is an Anubis interstitial** — read the body, not the status code. Chrome clears
  it, but the extension blocks `document.cookie` and page `fetch()`. **What works: batched
  `navigate` at each file_stream URL.** A 12-anchor-click burst is blocked and freezes the renderer.
- Harbor `jobs/` land under `scratch/harbor-jobs/`. Verifier logs: `<job>/<trial>/verifier/pytest.log`;
  quota deaths show `exception.txt` + `Usage Limit Exceeded` in `agent/trajectory.json`.

## STANDING RULES THAT KEEP GETTING TESTED
- **SUBMITTING IS SIR'S ACTION, NEVER MINE.** All projects, no exceptions.
- **No silent degradation** — a below-threshold or uninterpretable result BLOCKS. Today that rule is
  the only reason four uninterpretable 0.000 rounds weren't banked as a frontier rate.
- A trial that wrote no output, or died on quota, **measures nothing** and must stay out of the
  denominator. `batch_calibrate.py` enforces this and it caught its own first bug doing so.
- Never state a telemetry fact not read out of the JSON.
- Verify the assistance policy **per project**; never carry a ruling across.
