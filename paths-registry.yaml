# Jarvis Paths Registry (Project Aion)
#
# Source of truth for all external paths and infrastructure.
# Originally created by AIfred /setup on 2026-01-03
# Adopted by Jarvis (Project Aion) - see roadmap.md for evolution
#

version: "2.1.0"
last_updated: "2026-04-30"
created_by: "Jarvis (originally AIfred /setup)"

# =============================================================================
# HOSTS
# =============================================================================

hosts:
  local:
    hostname: "Nathaniels-MacBook-Air.local"
    ip: "192.168.5.9"
    role: "Primary Jarvis host"
    os: "macOS 26.2 (Darwin arm64)"
    cpu_cores: 10
    memory_gb: 16
    status: "active"
    notes: "Apple Silicon MacBook Air - Configured by Jarvis (originally AIfred setup)"

# =============================================================================
# DOCKER
# =============================================================================

docker:
  socket: "/var/run/docker.sock"
  version: "29.1.3"
  compose_base: "/Users/nathanielcannon/Claude/Jarvis/docker"

  # Populated by /discover command:
  containers: {}

# =============================================================================
# NETWORK
# =============================================================================

network:
  domain: "local"
  dns:
    primary: "auto"  # Using system DNS

# =============================================================================
# STORAGE
# =============================================================================

storage:
  local:
    root_disk: "228GB total, 169GB available"
  # No network mounts detected

# =============================================================================
# TOOLS
# =============================================================================

tools:
  git: "2.50.1"
  docker: "29.1.3"
  node: "24.12.0"  # via nvm
  python: "3.9.6"
  nvm: "0.40.0"

# =============================================================================
# DEVELOPMENT
# =============================================================================
# Jarvis is a "hub" that knows about projects but doesn't contain them.
# Code lives in projects_root. Jarvis tracks and orchestrates.
# Per PR-1.E workspace-path-policy:
#   - Project code: /Users/nathanielcannon/Claude/<ProjectName>/
#   - Project summaries: /Users/nathanielcannon/Claude/Jarvis/projects/

development:
  # Where project CODE lives (not in Jarvis)
  projects_root: "/Users/nathanielcannon/Claude"

  # Where project SUMMARIES live (in Jarvis)
  summaries_path: "/Users/nathanielcannon/Claude/Jarvis/projects"

  conventions:
    naming: "lowercase-with-dashes"
    summary_template: ".claude/context/templates/project-summary.md"
    context_template: ".claude/context/templates/project-context.md"

  # Registered projects (populated by /create-project or /register-project)
  projects: {}

# =============================================================================
# JARVIS (Project Aion Master Archon)
# =============================================================================

jarvis:
  base_path: "/Users/nathanielcannon/Claude/Jarvis"
  version: "1.9.5"
  # BEHAVIOR: How Jarvis operates
  context: ".claude/context"
  commands: ".claude/commands"
  agents: ".claude/agents"
  hooks: ".claude/hooks"
  logs: ".claude/logs"
  # EVOLUTION: How Jarvis improves (Project Aion)
  project_aion: "projects/project-aion"
  project_summaries: "projects"

# =============================================================================
# AIFRED BASELINE (READ-ONLY)
# =============================================================================

aifred_baseline:
  path: "/Users/nathanielcannon/Claude/Archive/AIfred"
  branch: "main"
  status: "read-only"
  current_commit: "a4088af"
  last_checked: "2026-04-30"
  last_synced_commit: "2ea4e8b"  # Major sync: 14 ADOPT + 7 ADAPT including JICM
  last_sync_date: "2026-01-09"
  sync_command: "/sync-aifred-baseline"
  port_log: ".claude/context/upstream/port-log.md"
  notes: "Upstream reference only — relocated to Archive/ on 2026-03-28. Never edit directly."

# =============================================================================
# AIFRED-PRO-DEV (Collaborative dev workspace with David O'Neil)
# =============================================================================

aifred_pro_dev:
  path: "/Users/nathanielcannon/Claude/AIFred-Pro-Dev"
  branch: "nate-dev"
  origin_remote: "davidmoneil/AIFred-Pro"
  status: "read-write"
  nexus_sync:
    branch: "nexus-sync-2026-04"
    last_reviewed_commit: "ee9b155"
    last_reviewed_date: "2026-04-30"
    review_command: "/sync-aifred-pro-dev"
    notes: "David's experimental Nexus refactor branch. Daily fetch + summary via launchd job com.aion.david-nexus-sync-fetch (Stage C)."

# =============================================================================
# DISCOVERED REPOSITORIES
# =============================================================================
# Found during setup - can be registered as projects

discovered_repos:
  - path: "/Users/nathanielcannon/Claude/Archive/AIfred"
    status: "archived-baseline"
  - path: "/Users/nathanielcannon/Claude/ClaudeCode"
    status: "unregistered"
  - path: "/Users/nathanielcannon/Documents/claude-flow"
    status: "unregistered"
  - path: "/Users/nathanielcannon/Documents/Jarvis"
    status: "unregistered"
