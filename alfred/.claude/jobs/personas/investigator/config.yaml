persona: investigator
engine:
  default: claude-code
  model: haiku
  fallback: null
output:
  format: json
  save_to: .claude/logs/headless/executions/
env_files:
- .claude/jobs/.env
session:
  persist: false
schema_version: 2
