persona: aurora-builder
engine:
  default: claude-code
  model: sonnet
  fallback: null
output:
  format: json
  save_to: .claude/logs/headless/executions/
session:
  persist: false
schema_version: 2
