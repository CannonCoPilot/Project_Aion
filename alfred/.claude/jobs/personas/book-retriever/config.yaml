persona: book-retriever

engine:
  default: claude-code
  model: sonnet
  fallback: null

limits:
  max_turns: 15
  max_budget_usd: 1.50
  timeout_minutes: 10

output:
  format: json
  save_to: .claude/logs/headless/executions/

session:
  persist: false

mcps:
  - annas-archive
