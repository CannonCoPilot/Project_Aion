persona: librarian
engine:
  default: claude-code
  model: sonnet
  fallback: null
output:
  format: json
  save_to: .claude/logs/headless/executions/
session:
  persist: false
add_dirs:
- /mnt/synology_nas/AudioBooks
schema_version: 2
