# Validate persona directories and registry references

Run the persona health check:
  bash ~/AIProjects/.claude/jobs/lib/persona-health-check.sh

Report the output. If any issues are found, create a Pulse task.
If all checks passed, output: "SEVERITY: info" followed by "Persona health: all checks passed"
