# Evaluate new tasks for pipeline routing — determine READY, NEEDS RESEARCH, or NEEDS INPUT

Run the task evaluation workflow. Find unevaluated tasks (no pipeline: label),
classify them, apply routing labels, and generate orchestration YAMLs for
multi-phase work. Write a JSON report when done.
