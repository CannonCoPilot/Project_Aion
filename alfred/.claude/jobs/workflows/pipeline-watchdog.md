# Deterministic pipeline integrity monitor — label validation, stuck task detection, mutual exclusion enforcement

This job runs entirely in pre_check. If you see this prompt, something is misconfigured.
