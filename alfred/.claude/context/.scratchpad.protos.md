# Protos scratchpad — resume doc

**Updated:** 2026-08-25 · **Lane:** Alfred/Protos · **Status:** task complete, follow-ups offered

---

## Active task: Lilly Round-2 interview prep (Master Wayne)

**Not a code task.** Document work in `~/Public/Drop Box/`. No repo changes made. Nothing committed.

### Deliverable (COMPLETE)
`/Users/nathanielcannon/Public/Drop Box/Lilly_Round2_Technical_Translation.md`

Created in turn 1, then fully verified against source archives in turn 2. All ten `[CONFIRM]`
placeholders resolved. Four new project cards added as **Part 1B**.

### Context
Master Wayne interviewed at Eli Lilly (Senior AI Application Full Stack Developer, 12-mo contract,
remote/EST). Round 1 went badly with the **lead IT R&D software developer** — questions like "what
platform / what API / what tools did you use?" felt unanswerable as a non-developer. Round 2 pending.

Their stated need: **an API library letting agents run complex queries across public + proprietary
databases, with an authorization layer enforcing data-federation rules per caller.**

### Document structure
- **Part 0** — decoder ring (platform/API/tools/stack/framework/deploy/database → what each layer
  means + answer shape), 4 memorized sentences, reframe line
- **Part 1** — 6 projects × 8 slots (Problem/Interface/Language/Libraries/Stores/APIs/Auth/Deploy)
- **Part 1B** — 4 additional projects found in archive sweep (NEW in turn 2)
- **Part 2** — their actual need decomposed into 5 sub-problems + 5 authorization talking points +
  the question to ask HIM
- **Part 3** — 6 likely round-2 questions with answers
- **Part 4** — pre-call checklist

---

## Verified findings (turn 2 archive sweep) — do not re-derive

Sources read: `~/Documents/DNAnexusExit` (972 files), `~/Documents/ThermoDownloadFolder`,
`~/Claude/Projects/ThermoThings`, live `Project_Aion` repo. **Read-only; no writes to ~/Documents.**

### Corrections to prior briefs
| Claim | Truth |
|---|---|
| VCF via pysam/cyvcf2 | **Hand-rolled streaming parser** `scan_vcf_for_variants` over SFTP handle; checks `FILTER==PASS`, parses `AF=`. pysam in requirements but UNUSED |
| PDF via pdfplumber/PyPDF2 | **PyMuPDF (`fitz`)** |
| Agent framework | **`agno`** (Agent, Ollama, Toolkit, SqliteStorage). LangGraph + langchain_ollama also present elsewhere |
| BOM handling | `--file-encoding` default `utf-8-sig` = documented CLI **default**, not a bugfix |
| Accumulate errors | `--halt-on-error` **flag**; rebinds module-level `report` pointer print→print_and_halt |

### Under-sold strengths (the lede)
1. **`entity.py`** 2,258 lines, ~40 methods + `dxdictionaries.py` 574 lines — internal library
   present in EVERY DNAnexus project dir. Automated schema inference → 16-col data dictionary +
   45-col per-column profile (type distribution, date-format detection, Grubbs outliers both tails,
   healthcare-code-system detection). Chunked >10GB, `chunksize=1_000_000`, Parquet path.
2. **MCP Assignment Matrix** — role→server RBAC (Memory Manager: 3 servers; Critic: 1; TDD/Scorer:
   none; Orchestrator/MCP-Integration: `*`) PLUS per-tool allowlists (playwright 25, excel-stdio 21,
   serper 13, memory 9, human-in-the-loop 6, calculator 1 …). **18 servers exactly.**
3. **`variant_finder/api.pyi`** — shipped type stub, `__all__` of 6 typed functions w/ documented
   raises. Library contract; CLI is one consumer.

### Thermo APIM (from 4 OpenAPI specs he holds)
- Azure OpenAI `…/dev/gpt4omini/openai`, **30 req/min** documented
- AWS Bedrock `…/dev/aws/chat/completions`, OpenAI-shaped, `apac.anthropic.claude-3-5-sonnet-20241022-v2:0`
- Vertex `…/dev/gemini-25-pro-vertex`, native shape, `:generateContent` / `:streamGenerateContent`
- Auth: `api-key` header (`apiKeyHeader`), `subscription-key` query alt. Azure Managed Identity →
  Google WIF, APIM does token exchange
- Errors: 400 / **401+403 same bucket** / 500. Streaming: SSE `data: [DONE]` vs close-on-`finishReason`
- Bedrock schema exposes `tools`/`tool_choice`/`parallel_tool_calls`

### New project cards added (Part 1B)
- **P7** `entity.py` ingestion library (⭐ lead with this)
- **P8** CMS/ACL federal claims — **only true regulated-PII/controlled-access story**. `ACL_PII.ipynb`
  documented per-file column suppression (one file excluded entirely: SSNs); `salt_faster()`
  de-identification; PyCaret per-state models; CMS↔APS record linkage 2014–2020
- **P9** `DAO_main` shared transformation core (671+687 lines) + per-partner adapters; Voilà/ipywidgets
  profiler dashboards = three consumers over one library
- **P10** PySpark cohort querying, raw SQL relationship validation, GDC `gdc_client`, ontology crosswalks

### ⛔ REJECTED attributions — do not let these back in
- `Cool_notebooks/clinical_knowledge_graph/` = **PrimeKG** (mims-harvard/Zitnik). 28 refs, imports
  `tdc.utils.knowledge_graph`, comment "updated PrimeKG: fix path". `Cool_notebooks/` is a 389-file
  COLLECTION folder incl. vendor tutorials — treat all as unattributed.
- `reprompter-mcp` author = **"Mnehmos"**, not him. `mcp_repos/` are third-party clones.
- ⇒ Thermo MCP = **configured/governed/tested**, NOT authored. Authored MCP = Project Aion only:
  3 FastMCP servers, 19 tools (`mcp_server.py` 491L/6, `graphiti_mcp_server.py` 479L/7,
  `pulse_mcp_server.py` 226L/6).

### Honest weakness to pre-empt
`variant_finder` takes SFTP password as `--password` flag + plaintext `var_find.cfg` → shell history
+ disk. JD explicitly requires "no hardcoded API keys". Own it first.

### Project Aion verified figures
53.8k lines Py / 28.9k TS / 41.9k TSX / 48.5k JS / 80k bash (first-party, venv+node_modules excluded).
5 infra containers (postgres, qdrant, neo4j, redis, n8n) + 6 app-tier (postgres, pulse,
nexus-dashboard, dashboard-dev, pipeline, usage-proxy). 42 hooks, 20 skills.
Frontend: React/Vite/Tailwind/TanStack Query/Recharts/XYFlow. Server: Fastify/better-sqlite3/ws.

---

## Open follow-ups (offered, not started)
1. Render updated brief to `.docx` + `.pdf` to match the existing Lilly file set (doc-ops skill)
2. **Compress Part 1B into a one-page hold-during-call card** — recommended as higher value
3. LinkedIn postings all fetched fine (no auth wall). JD #3 *Technical Lead, Data Foundry* is the
   Rosetta Stone: "agent-ready APIs with structured error handling, audit trails, monitoring" +
   "contribute to MCP servers exposing Data Foundry capabilities to AI agents"

## Housekeeping
- Repo had 143 uncommitted changes at session start; **none caused by this task**. Nothing staged.
- Prior brief flagged live credentials needing rotation (`github-recovery-codes.txt`,
  a `geministudioapi-*.json` GCP service-account key, `ai8_arch/.env` + `.pem` files). Still unaddressed.
